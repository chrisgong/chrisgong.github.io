<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title><![CDATA[Gc's blog]]></title>
  <subtitle><![CDATA[Discontent is the first step in progress.]]></subtitle>
  <link href="/atom.xml" rel="self"/>
  <link href="http://gcblog.cn/"/>
  <updated>2016-01-07T13:04:00.000Z</updated>
  <id>http://gcblog.cn/</id>
  
  <author>
    <name><![CDATA[Gc]]></name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title><![CDATA[RxJava调度器Scheduler]]></title>
    <link href="http://gcblog.cn/2016/01/07/rxjava-scheduler/"/>
    <id>http://gcblog.cn/2016/01/07/rxjava-scheduler/</id>
    <published>2016-01-07T12:35:55.000Z</published>
    <updated>2016-01-07T13:04:00.000Z</updated>
    <content type="html"><![CDATA[<p>如果你想给Observable操作符链添加多线程功能，你可以指定操作符（或者特定的Observable）在特定的调度器(Scheduler)上执行。</p>
<p>某些ReactiveX的Observable操作符有一些变体，它们可以接受一个Scheduler参数。这个参数指定操作符将它们的部分或全部任务放在一个特定的调度器上执行。</p>
<p>使用ObserveOn和SubscribeOn操作符，你可以让Observable在一个特定的调度器上执行，ObserveOn指示一个 Observable在一个特定的调度器上调用观察者的onNext, onError和onCompleted方法，SubscribeOn更进一步，它指示Observable将全部的处理过程（包括发射数据和通知）放在特定的调度器上执行。</p>
<h5 id="调度器的种类">调度器的种类</h5><p>下表展示了RxJava中可用的调度器种类：</p>
<table>
<thead>
<tr>
<th>调度器类型</th>
<th>效果</th>
</tr>
</thead>
<tbody>
<tr>
<td>Schedulers.computation( )</td>
<td>用于计算任务，如事件循环或和回调处理，不要用于IO操作(IO操作请使用Schedulers.io())；默认线程数等于处理器的数量。它也是许多RxJava方法的默认调度器：buffer(),debounce(),delay(),interval(),sample(),skip()。</td>
</tr>
<tr>
<td>Schedulers.from(executor)</td>
<td>使用指定的Executor作为调度器</td>
</tr>
<tr>
<td>Schedulers.immediate( )</td>
<td>这个调度器允许你立即在当前线程执行你指定的工作。它是timeout(),timeInterval(),以及timestamp()方法默认的调度器。</td>
</tr>
<tr>
<td>Schedulers.io( )</td>
<td>用于IO密集型任务，如异步阻塞IO操作，这个调度器的线程池会根据需要增长；对于普通的计算任务，请使用Schedulers.computation()；Schedulers.io( )默认是一个CachedThreadScheduler，很像一个有线程缓存的新线程调度器</td>
</tr>
<tr>
<td>Schedulers.newThread( )</td>
<td>为每个任务创建一个新线程</td>
</tr>
<tr>
<td>Schedulers.trampoline( )</td>
<td>当我们想在当前线程执行一个任务时，并不是立即，我们可以用.trampoline()将它入队。这个调度器将会处理它的队列并且按序运行队列中每一个任务。它是repeat()和retry()方法默认的调度器。</td>
</tr>
</tbody>
</table>
<h5 id="默认调度器">默认调度器</h5><p>在RxJava中，某些Observable操作符的变体允许你设置用于操作执行的调度器，其它的则不在任何特定的调度器上执行，或者在一个指定的默认调度器上执行。下面的表格个列出了一些操作符的默认调度器：</p>
<table>
<thead>
<tr>
<th>操作符</th>
<th>调度器</th>
</tr>
</thead>
<tbody>
<tr>
<td>buffer(timespan)</td>
<td>computation</td>
</tr>
<tr>
<td>buffer(timespan, count)</td>
<td>computation</td>
</tr>
<tr>
<td>buffer(timespan, timeshift)</td>
<td>computation</td>
</tr>
<tr>
<td>debounce(timeout, unit)</td>
<td>computation</td>
</tr>
<tr>
<td>delay(delay, unit)</td>
<td>computation</td>
</tr>
<tr>
<td>delaySubscription(delay, unit)    computation</td>
</tr>
<tr>
<td>interval</td>
<td>computation</td>
</tr>
<tr>
<td>repeat</td>
<td>trampoline</td>
</tr>
<tr>
<td>replay(time, unit)</td>
<td>computation</td>
</tr>
<tr>
<td>replay(buffersize, time, unit)</td>
<td>computation</td>
</tr>
<tr>
<td>replay(selector, time, unit)</td>
<td>computation</td>
</tr>
<tr>
<td>replay(selector, buffersize, time, unit)</td>
<td>computation</td>
</tr>
<tr>
<td>retry</td>
<td>trampoline</td>
</tr>
<tr>
<td>sample(period, unit)</td>
<td>computation</td>
</tr>
<tr>
<td>skip(time, unit)</td>
<td>computation</td>
</tr>
<tr>
<td>skipLast(time, unit)</td>
<td>computation</td>
</tr>
<tr>
<td>take(time, unit)</td>
<td>computation</td>
</tr>
<tr>
<td>takeLast(time, unit)</td>
<td>computation</td>
</tr>
<tr>
<td>takeLast(count, time, unit)</td>
<td>computation</td>
</tr>
<tr>
<td>takeLastBuffer(time, unit)</td>
<td>computation</td>
</tr>
<tr>
<td>takeLastBuffer(count, time, unit)</td>
<td>computation</td>
</tr>
<tr>
<td>throttleFirst</td>
<td>computation</td>
</tr>
<tr>
<td>throttleLast</td>
<td>computation</td>
</tr>
<tr>
<td>throttleWithTimeout</td>
<td>computation</td>
</tr>
<tr>
<td>timeInterval</td>
<td>immediate</td>
</tr>
<tr>
<td>timeout(timeoutSelector)</td>
<td>immediate</td>
</tr>
<tr>
<td>timeout(firstTimeoutSelector, timeoutSelector)</td>
<td>immediate</td>
</tr>
<tr>
<td>timeout(timeoutSelector, other)</td>
<td>immediate</td>
</tr>
<tr>
<td>timeout(timeout, timeUnit)</td>
<td>computation</td>
</tr>
<tr>
<td>timeout(firstTimeoutSelector, timeoutSelector, other)</td>
<td>immediate</td>
</tr>
<tr>
<td>timeout(timeout, timeUnit, other)</td>
<td>computation</td>
</tr>
<tr>
<td>timer</td>
<td>computation</td>
</tr>
<tr>
<td>timestamp</td>
<td>immediate</td>
</tr>
<tr>
<td>window(timespan)</td>
<td>computation</td>
</tr>
<tr>
<td>window(timespan, count)</td>
<td>computation</td>
</tr>
<tr>
<td>window(timespan, timeshift)</td>
<td>computation</td>
</tr>
</tbody>
</table>
<h5 id="使用调度器">使用调度器</h5><p>除了将这些调度器传递给RxJava的Observable操作符，你也可以用它们调度你自己的任务。下面的示例展示了Scheduler.Worker的用法：</p>
<figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line">worker = Schedulers.newThread().createWorker();</span><br><span class="line">worker.schedule(<span class="keyword">new</span> Action0() &#123;</span><br><span class="line"> </span><br><span class="line">    <span class="annotation">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">call</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        yourWork();</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">&#125;);</span><br><span class="line"><span class="comment">// some time later...</span></span><br><span class="line">worker.unsubscribe();</span><br></pre></td></tr></table></figure>
<h6 id="递归调度器">递归调度器</h6><p>要调度递归的方法调用，你可以使用schedule，然后再用schedule(this)，示例：</p>
<figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line">worker = Schedulers.newThread().createWorker();</span><br><span class="line">worker.schedule(<span class="keyword">new</span> Action0() &#123;</span><br><span class="line"> </span><br><span class="line">    <span class="annotation">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">call</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        yourWork();</span><br><span class="line">        <span class="comment">// recurse until unsubscribed (schedule will do nothing if unsubscribed)</span></span><br><span class="line">        worker.schedule(<span class="keyword">this</span>);</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">&#125;);</span><br><span class="line"><span class="comment">// some time later...</span></span><br><span class="line">worker.unsubscribe();</span><br></pre></td></tr></table></figure>
<h6 id="检查或设置取消订阅状态">检查或设置取消订阅状态</h6><p>Worker类的对象实现了Subscription接口，使用它的isUnsubscribed和unsubscribe方法，所以你可以在订阅取消时停止任务，或者从正在调度的任务内部取消订阅，示例：</p>
<figure class="highlight sml"><table><tr><td class="code"><pre><span class="line"><span class="type">Worker</span> worker = <span class="type">Schedulers</span>.newThread<span class="literal">()</span>.createWorker<span class="literal">()</span>;</span><br><span class="line"><span class="type">Subscription</span> mySubscription = worker.schedule(new <span class="type">Action0</span><span class="literal">()</span> &#123;</span><br><span class="line"> </span><br><span class="line">    @<span class="type">Override</span></span><br><span class="line">    public void call<span class="literal">()</span> &#123;</span><br><span class="line">        <span class="keyword">while</span>(!worker.isUnsubscribed<span class="literal">()</span>) &#123;</span><br><span class="line">            status = yourWork<span class="literal">()</span>;</span><br><span class="line">            <span class="keyword">if</span>(<span class="type">QUIT</span> == status) &#123; worker.unsubscribe<span class="literal">()</span>; &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>Worker同时是Subscription，因此你可以（通常也应该）调用它的unsubscribe方法通知可以挂起任务和释放资源了。</p>
<h6 id="延时和周期调度器">延时和周期调度器</h6><p>你可以使用schedule(action,delayTime,timeUnit)在指定的调度器上延时执行你的任务，下面例子中的任务将在500毫秒之后开始执行：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="tag">someScheduler</span><span class="class">.schedule</span>(<span class="tag">someAction</span>, 500, <span class="tag">TimeUnit</span><span class="class">.MILLISECONDS</span>);</span><br></pre></td></tr></table></figure>
<p>使用另一个版本的schedule，schedulePeriodically(action,initialDelay,period,timeUnit)方法让你可以安排一个定期执行的任务，下面例子的任务将在500毫秒之后执行，然后每250毫秒执行一次：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">someScheduler.schedulePeriodically(someAction, <span class="number">500</span>, <span class="number">250</span>, TimeUnit.MILLISECONDS);</span><br></pre></td></tr></table></figure>
<h6 id="测试调度器">测试调度器</h6><p>TestScheduler让你可以对调度器的时钟表现进行手动微调。这对依赖精确时间安排的任务的测试很有用处。这个调度器有三个额外的方法：</p>
<ul>
<li>advanceTimeTo(time,unit) 向前波动调度器的时钟到一个指定的时间点</li>
<li>advanceTimeBy(time,unit) 将调度器的时钟向前拨动一个指定的时间段</li>
<li>triggerActions( ) 开始执行任何计划中的但是未启动的任务，如果它们的计划时间等于或者早于调度器时钟的当前时间</li>
</ul>
<h5 id="SubscribeOn_and_ObserveOn">SubscribeOn and ObserveOn</h5><p>RxJava提供了subscribeOn()方法来用于每个Observable对象。subscribeOn()方法用Scheduler来作为参数并在这个Scheduler上执行Observable调用。<br>observeOn()方法将会在指定的调度器上返回结果：如例子中的Android UI线程。<br>onBackpressureBuffer()方法将告诉Observable发射的数据如果比观察者消费的数据要更快的话，它必须把它们存储在缓存中并提供一个合适的时间给它们。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line">...</span><br><span class="line"><span class="class">.onBackpressureBuffer</span>()</span><br><span class="line"><span class="class">.subscribeOn</span>(<span class="tag">Schedulers</span><span class="class">.io</span>())</span><br><span class="line"><span class="class">.observeOn</span>(<span class="tag">AndroidSchedulers</span><span class="class">.mainThread</span>())</span><br><span class="line"><span class="class">.subscribe</span>(<span class="tag">new</span> <span class="tag">Observer</span>&lt;<span class="tag">Object</span>&gt;()<span class="rules">&#123;&#125;</span>);</span><br></pre></td></tr></table></figure>
]]></content>
    <summary type="html">
    <![CDATA[<p>如果你想给Observable操作符链添加多线程功能，你可以指定操作符（或者特定的Observable）在特定的调度器(Scheduler)上执行。</p>
<p>某些ReactiveX的Observable操作符有一些变体，它们可以接受一个Scheduler参数。这个参数]]>
    </summary>
    
      <category term="RxJava" scheme="http://gcblog.cn/tags/RxJava/"/>
    
      <category term="RxJava" scheme="http://gcblog.cn/categories/RxJava/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[RxJava详解]]></title>
    <link href="http://gcblog.cn/2016/01/03/rxjava/"/>
    <id>http://gcblog.cn/2016/01/03/rxjava/</id>
    <published>2016-01-03T02:03:41.000Z</published>
    <updated>2016-01-04T05:17:53.000Z</updated>
    <content type="html"><![CDATA[<blockquote>
<p>转载自：<a href="http://gank.io/post/560e15be2dca930e00da1083" target="_blank" rel="external">给 Android 开发者的 RxJava 详解</a></p>
</blockquote>
<h4 id="概念：扩展的观察者模式">概念：扩展的观察者模式</h4><p>RxJava 有四个基本概念：Observable (可观察者，即被观察者)、 Observer (观察者)、 subscribe (订阅)、事件。Observable 和 Observer 通过 subscribe() 方法实现订阅关系，从而 Observable 可以在需要的时候发出事件来通知 Observer。</p>
<p>与传统观察者模式不同， RxJava 的事件回调方法除了普通事件 onNext() （相当于 onClick() / onEvent()）之外，还定义了两个特殊的事件：onCompleted() 和 onError()。</p>
<ul>
<li>onCompleted(): 事件队列完结。RxJava 不仅把每个事件单独处理，还会把它们看做一个队列。RxJava 规定，当不会再有新的 onNext() 发出时，需要触发 onCompleted() 方法作为标志。</li>
<li>onError(): 事件队列异常。在事件处理过程中出异常时，onError() 会被触发，同时队列自动终止，不允许再有事件发出。</li>
<li>在一个正确运行的事件序列中, onCompleted() 和 onError() 有且只有一个，并且是事件序列中的最后一个。需要注意的是，onCompleted() 和 onError() 二者也是互斥的，即在队列中调用了其中一个，就不应该再调用另一个。</li>
</ul>
<p>RxJava 的观察者模式大致如下图：</p>
<p><img src="http://7xn2zf.com1.z0.glb.clouddn.com/rxjavarxjava_3.png" alt=""></p>
<h4 id="基本实现">基本实现</h4><p>基于以上的概念， RxJava 的基本实现主要有三点：</p>
<h5 id="创建_Observer">创建 Observer</h5><p>Observer 即观察者，它决定事件触发的时候将有怎样的行为。 RxJava 中的 Observer 接口的实现方式：</p>
<figure class="highlight lasso"><table><tr><td class="code"><pre><span class="line">Observer&lt;<span class="built_in">String</span>&gt; observer = <span class="literal">new</span> Observer&lt;<span class="built_in">String</span>&gt;() &#123;</span><br><span class="line">    @Override</span><br><span class="line">    <span class="keyword">public</span> <span class="literal">void</span> onNext(<span class="built_in">String</span> s) &#123;</span><br><span class="line">        <span class="keyword">Log</span><span class="built_in">.</span>d(<span class="built_in">tag</span>, <span class="string">"Item: "</span> + s);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    <span class="keyword">public</span> <span class="literal">void</span> onCompleted() &#123;</span><br><span class="line">        <span class="keyword">Log</span><span class="built_in">.</span>d(<span class="built_in">tag</span>, <span class="string">"Completed!"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    <span class="keyword">public</span> <span class="literal">void</span> onErr<span class="subst">or</span>(Throwable e) &#123;</span><br><span class="line">        <span class="keyword">Log</span><span class="built_in">.</span>d(<span class="built_in">tag</span>, <span class="string">"Error!"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>除了 Observer 接口之外，RxJava 还内置了一个实现了 Observer 的抽象类：Subscriber。 Subscriber 对 Observer 接口进行了一些扩展，但他们的基本使用方式是完全一样的：</p>
<figure class="highlight lasso"><table><tr><td class="code"><pre><span class="line">Subscriber&lt;<span class="built_in">String</span>&gt; subscriber = <span class="literal">new</span> Subscriber&lt;<span class="built_in">String</span>&gt;() &#123;</span><br><span class="line">    @Override</span><br><span class="line">    <span class="keyword">public</span> <span class="literal">void</span> onNext(<span class="built_in">String</span> s) &#123;</span><br><span class="line">        <span class="keyword">Log</span><span class="built_in">.</span>d(<span class="built_in">tag</span>, <span class="string">"Item: "</span> + s);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    <span class="keyword">public</span> <span class="literal">void</span> onCompleted() &#123;</span><br><span class="line">        <span class="keyword">Log</span><span class="built_in">.</span>d(<span class="built_in">tag</span>, <span class="string">"Completed!"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    <span class="keyword">public</span> <span class="literal">void</span> onErr<span class="subst">or</span>(Throwable e) &#123;</span><br><span class="line">        <span class="keyword">Log</span><span class="built_in">.</span>d(<span class="built_in">tag</span>, <span class="string">"Error!"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>不仅基本使用方式一样，实质上，在 RxJava 的 subscribe 过程中，Observer 也总是会先被转换成一个 Subscriber 再使用。所以如果你只想使用基本功能，选择 Observer 和 Subscriber 是完全一样的。它们的区别对于使用者来说主要有两点：</p>
<ol>
<li>onStart(): 这是 Subscriber 增加的方法。它会在 subscribe 刚开始，而事件还未发送之前被调用，可以用于做一些准备工作，例如数据的清零或重置。这是一个可选方法，默认情况下它的实现为空。需要注意的是，如果对准备工作的线程有要求（例如弹出一个显示进度的对话框，这必须在主线程执行）， onStart() 就不适用了，因为它总是在 subscribe 所发生的线程被调用，而不能指定线程。要在指定的线程来做准备工作，可以使用 doOnSubscribe() 方法，具体可以在后面的文中看到。</li>
<li>unsubscribe(): 这是 Subscriber 所实现的另一个接口 Subscription 的方法，用于取消订阅。在这个方法被调用后，Subscriber 将不再接收事件。一般在这个方法调用前，可以使用 isUnsubscribed() 先判断一下状态。 unsubscribe() 这个方法很重要，因为在 subscribe() 之后， Observable 会持有 Subscriber 的引用，这个引用如果不能及时被释放，将有内存泄露的风险。所以最好保持一个原则：要在不再使用的时候尽快在合适的地方（例如 onPause() onStop() 等方法中）调用 unsubscribe() 来解除引用关系，以避免内存泄露的发生。</li>
</ol>
<h5 id="创建_Observable">创建 Observable</h5><p>Observable 即被观察者，它决定什么时候触发事件以及触发怎样的事件。 RxJava 使用 create() 方法来创建一个 Observable ，并为它定义事件触发规则：</p>
<figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line">Observable observable = Observable.create(<span class="keyword">new</span> Observable.OnSubscribe&lt;String&gt;() &#123;</span><br><span class="line">    <span class="annotation">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">call</span><span class="params">(Subscriber&lt;? <span class="keyword">super</span> String&gt; subscriber)</span> </span>&#123;</span><br><span class="line">        subscriber.onNext(<span class="string">"Hello"</span>);</span><br><span class="line">        subscriber.onNext(<span class="string">"Hi"</span>);</span><br><span class="line">        subscriber.onNext(<span class="string">"Aloha"</span>);</span><br><span class="line">        subscriber.onCompleted();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>可以看到，这里传入了一个 OnSubscribe 对象作为参数。OnSubscribe 会被存储在返回的 Observable 对象中，它的作用相当于一个计划表，当 Observable 被订阅的时候，OnSubscribe 的 call() 方法会自动被调用，事件序列就会依照设定依次触发（对于上面的代码，就是观察者Subscriber 将会被调用三次 onNext() 和一次 onCompleted()）。这样，由被观察者调用了观察者的回调方法，就实现了由被观察者向观察者的事件传递，即观察者模式。</p>
<blockquote>
<p>   这个例子很简单：事件的内容是字符串，而不是一些复杂的对象；事件的内容是已经定好了的，而不像有的观察者模式一样是待确定的（例如网络请求的结果在请求返回之前是未知的）；所有事件在一瞬间被全部发送出去，而不是夹杂一些确定或不确定的时间间隔或者经过某种触发器来触发的。总之，这个例子看起来毫无实用价值。但这是为了便于说明，实质上只要你想，各种各样的事件发送规则你都可以自己来写。至于具体怎么做，后面都会讲到，但现在不行。只有把基础原理先说明白了，上层的运用才能更容易说清楚。</p>
</blockquote>
<p>create() 方法是 RxJava 最基本的创造事件序列的方法。基于这个方法， RxJava 还提供了一些方法用来快捷创建事件队列，例如：</p>
<ul>
<li>just(T…): 将传入的参数依次发送出来。</li>
</ul>
<figure class="highlight pf"><table><tr><td class="code"><pre><span class="line">Observable observable = Observable.just(<span class="string">"Hello"</span>, <span class="string">"Hi"</span>, <span class="string">"Aloha"</span>);</span><br><span class="line">// 将会依次调用：</span><br><span class="line">// <span class="keyword">on</span>Next(<span class="string">"Hello"</span>);</span><br><span class="line">// <span class="keyword">on</span>Next(<span class="string">"Hi"</span>);</span><br><span class="line">// <span class="keyword">on</span>Next(<span class="string">"Aloha"</span>);</span><br><span class="line">// <span class="keyword">on</span>Completed();</span><br></pre></td></tr></table></figure>
<ul>
<li>from(T[]) / from(Iterable&lt;? extends T&gt;) : 将传入的数组或 Iterable 拆分成具体对象后，依次发送出来。</li>
</ul>
<figure class="highlight pf"><table><tr><td class="code"><pre><span class="line">String[] words = &#123;<span class="string">"Hello"</span>, <span class="string">"Hi"</span>, <span class="string">"Aloha"</span>&#125;;</span><br><span class="line">Observable observable = Observable.<span class="keyword">from</span>(words);</span><br><span class="line">// 将会依次调用：</span><br><span class="line">// <span class="keyword">on</span>Next(<span class="string">"Hello"</span>);</span><br><span class="line">// <span class="keyword">on</span>Next(<span class="string">"Hi"</span>);</span><br><span class="line">// <span class="keyword">on</span>Next(<span class="string">"Aloha"</span>);</span><br><span class="line">// <span class="keyword">on</span>Completed();</span><br></pre></td></tr></table></figure>
<p>上面 just(T…) 的例子和 from(T[]) 的例子，都和之前的 create(OnSubscribe) 的例子是等价的。</p>
<h5 id="Subscribe_(订阅)">Subscribe (订阅)</h5><p>创建了 Observable 和 Observer 之后，再用 subscribe() 方法将它们联结起来，整条链子就可以工作了。代码形式很简单：</p>
<figure class="highlight gcode"><table><tr><td class="code"><pre><span class="line">observable.subscribe<span class="comment">(observer)</span>;</span><br><span class="line"><span class="comment">// 或者：</span></span><br><span class="line">observable.subscribe<span class="comment">(subscriber)</span>;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>   有人可能会注意到， subscribe() 这个方法有点怪：它看起来是『observalbe 订阅了 observer / subscriber』而不是『observer / subscriber 订阅了 observalbe』，这看起来就像『杂志订阅了读者』一样颠倒了对象关系。这让人读起来有点别扭，不过如果把 API 设计成 observer.subscribe(observable) / subscriber.subscribe(observable) ，虽然更加符合思维逻辑，但对流式 API 的设计就造成影响了，比较起来明显是得不偿失的。</p>
</blockquote>
<p>Observable.subscribe(Subscriber) 的内部实现是这样的（仅核心代码）：</p>
<figure class="highlight armasm"><table><tr><td class="code"><pre><span class="line">// 注意：这不是 <span class="keyword">subscribe() </span>的源码，而是将源码中与性能、兼容性、扩展性有关的代码剔除后的核心代码。</span><br><span class="line">// 如果需要看源码，可以去 RxJava 的 GitHub 仓库下载。</span><br><span class="line"><span class="label">public</span> <span class="keyword">Subscription </span><span class="keyword">subscribe(Subscriber </span><span class="keyword">subscriber) </span>&#123;</span><br><span class="line">    <span class="keyword">subscriber.onStart();</span><br><span class="line"></span>    onSubscribe.call(<span class="keyword">subscriber);</span><br><span class="line"></span>    return <span class="keyword">subscriber;</span><br><span class="line"></span>&#125;</span><br></pre></td></tr></table></figure>
<p>可以看到，subscriber() 做了3件事：</p>
<ol>
<li>调用 Subscriber.onStart() 。这个方法在前面已经介绍过，是一个可选的准备方法。</li>
<li>调用 Observable 中的 OnSubscribe.call(Subscriber) 。在这里，事件发送的逻辑开始运行。从这也可以看出，在 RxJava 中， Observable 并不是在创建的时候就立即开始发送事件，而是在它被订阅的时候，即当 subscribe() 方法执行的时候。</li>
<li>将传入的 Subscriber 作为 Subscription 返回。这是为了方便 unsubscribe().</li>
</ol>
<p>整个过程中对象间的关系如下图：</p>
<p><img src="http://7xn2zf.com1.z0.glb.clouddn.com/rxjavarxjava_4.png" alt=""></p>
<p>或者可以看动图：</p>
<p><img src="http://7xn2zf.com1.z0.glb.clouddn.com/rxjavarxjava_4_d.gif" alt=""></p>
<p>除了 subscribe(Observer) 和 subscribe(Subscriber) ，subscribe() 还支持不完整定义的回调，RxJava 会自动根据定义创建出 Subscriber 。形式如下：</p>
<figure class="highlight pf"><table><tr><td class="code"><pre><span class="line">Action1<span class="variable">&lt;String&gt;</span> <span class="keyword">on</span>NextAction = new Action1<span class="variable">&lt;String&gt;</span>() &#123;</span><br><span class="line">    // <span class="keyword">on</span>Next()</span><br><span class="line">    @Override</span><br><span class="line">    public void call(String s) &#123;</span><br><span class="line">        Log.d(<span class="keyword">tag</span>, s);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line">Action1<span class="variable">&lt;Throwable&gt;</span> <span class="keyword">on</span>ErrorAction = new Action1<span class="variable">&lt;Throwable&gt;</span>() &#123;</span><br><span class="line">    // <span class="keyword">on</span>Error()</span><br><span class="line">    @Override</span><br><span class="line">    public void call(Throwable throwable) &#123;</span><br><span class="line">        // Error handling</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line">Action0 <span class="keyword">on</span>CompletedAction = new Action0() &#123;</span><br><span class="line">    // <span class="keyword">on</span>Completed()</span><br><span class="line">    @Override</span><br><span class="line">    public void call() &#123;</span><br><span class="line">        Log.d(<span class="keyword">tag</span>, <span class="string">"completed"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">// 自动创建 Subscriber ，并使用 <span class="keyword">on</span>NextAction 来定义 <span class="keyword">on</span>Next()</span><br><span class="line">observable.subscribe(<span class="keyword">on</span>NextAction);</span><br><span class="line">// 自动创建 Subscriber ，并使用 <span class="keyword">on</span>NextAction 和 <span class="keyword">on</span>ErrorAction 来定义 <span class="keyword">on</span>Next() 和 <span class="keyword">on</span>Error()</span><br><span class="line">observable.subscribe(<span class="keyword">on</span>NextAction, <span class="keyword">on</span>ErrorAction);</span><br><span class="line">// 自动创建 Subscriber ，并使用 <span class="keyword">on</span>NextAction、 <span class="keyword">on</span>ErrorAction 和 <span class="keyword">on</span>CompletedAction 来定义 <span class="keyword">on</span>Next()、 <span class="keyword">on</span>Error() 和 <span class="keyword">on</span>Completed()</span><br><span class="line">observable.subscribe(<span class="keyword">on</span>NextAction, <span class="keyword">on</span>ErrorAction, <span class="keyword">on</span>CompletedAction);</span><br></pre></td></tr></table></figure>
<p>简单解释一下这段代码中出现的 Action1 和 Action0。 Action0 是 RxJava 的一个接口，它只有一个方法 call()，这个方法是无参无返回值的；由于 onCompleted() 方法也是无参无返回值的，因此 Action0 可以被当成一个包装对象，将 onCompleted() 的内容打包起来将自己作为一个参数传入 subscribe() 以实现不完整定义的回调。这样其实也可以看做将 onCompleted() 方法作为参数传进了 subscribe()，相当于其他某些语言中的『闭包』。 Action1 也是一个接口，它同样只有一个方法 call(T param)，这个方法也无返回值，但有一个参数；与 Action0 同理，由于 onNext(T obj) 和 onError(Throwable error) 也是单参数无返回值的，因此 Action1 可以将 onNext(obj) 和 onError(error) 打包起来传入 subscribe() 以实现不完整定义的回调。事实上，虽然 Action0 和 Action1 在 API 中使用最广泛，但 RxJava 是提供了多个 ActionX 形式的接口 (例如 Action2, Action3) 的，它们可以被用以包装不同的无返回值的方法。</p>
<blockquote>
<p>   注：正如前面所提到的，Observer 和 Subscriber 具有相同的角色，而且 Observer 在 subscribe() 过程中最终会被转换成 Subscriber 对象，因此，从这里开始，后面的描述我将用 Subscriber 来代替 Observer ，这样更加严谨。</p>
</blockquote>
<h5 id="场景示例">场景示例</h5><p>下面举两个例子：</p>
<blockquote>
<p>   为了把原理用更清晰的方式表述出来，本文中挑选的都是功能尽可能简单的例子，以至于有些示例代码看起来会有『画蛇添足』『明明不用 RxJava 可以更简便地解决问题』的感觉。当你看到这种情况，不要觉得是因为 RxJava 太啰嗦，而是因为在过早的时候举出真实场景的例子并不利于原理的解析，因此我刻意挑选了简单的情景。</p>
</blockquote>
<ol>
<li>打印字符串数组</li>
</ol>
<p>将字符串数组 names 中的所有字符串依次打印出来：</p>
<figure class="highlight vbscript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">String</span>[] names = ...;</span><br><span class="line">Observable.from(names)</span><br><span class="line">    .subscribe(<span class="keyword">new</span> Action1&lt;<span class="built_in">String</span>&gt;() &#123;</span><br><span class="line">        @Override</span><br><span class="line">        <span class="keyword">public</span> void <span class="keyword">call</span>(<span class="built_in">String</span> name) &#123;</span><br><span class="line">            <span class="built_in">Log</span>.d(tag, name);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br></pre></td></tr></table></figure>
<ol>
<li>由 id 取得图片并显示</li>
</ol>
<p>由指定的一个 drawable 文件 id drawableRes 取得图片，并显示在 ImageView 中，并在出现异常的时候打印 Toast 报错：</p>
<figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> drawableRes = ...;</span><br><span class="line">ImageView imageView = ...;</span><br><span class="line">Observable.create(<span class="keyword">new</span> OnSubscribe&lt;Drawable&gt;() &#123;</span><br><span class="line">    <span class="annotation">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">call</span><span class="params">(Subscriber&lt;? <span class="keyword">super</span> Drawable&gt; subscriber)</span> </span>&#123;</span><br><span class="line">        Drawable drawable = getTheme().getDrawable(drawableRes));</span><br><span class="line">        subscriber.onNext(drawable);</span><br><span class="line">        subscriber.onCompleted();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;).subscribe(<span class="keyword">new</span> Observer&lt;Drawable&gt;() &#123;</span><br><span class="line">    <span class="annotation">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">onNext</span><span class="params">(Drawable drawable)</span> </span>&#123;</span><br><span class="line">        imageView.setImageDrawable(drawable);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="annotation">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">onCompleted</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="annotation">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">onError</span><span class="params">(Throwable e)</span> </span>&#123;</span><br><span class="line">        Toast.makeText(activity, <span class="string">"Error!"</span>, Toast.LENGTH_SHORT).show();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>正如上面两个例子这样，创建出 Observable 和 Subscriber ，再用 subscribe() 将它们串起来，一次 RxJava 的基本使用就完成了。非常简单。</p>
<p>在 RxJava 的默认规则中，事件的发出和消费都是在同一个线程的。也就是说，如果只用上面的方法，实现出来的只是一个同步的观察者模式。观察者模式本身的目的就是『后台处理，前台回调』的异步机制，因此异步对于 RxJava 是至关重要的。而要实现异步，则需要用到 RxJava 的另一个概念： Scheduler 。</p>
<h4 id="线程控制_——_Scheduler_(一)">线程控制 —— Scheduler (一)</h4><p>在不指定线程的情况下， RxJava 遵循的是线程不变的原则，即：在哪个线程调用 subscribe()，就在哪个线程生产事件；在哪个线程生产事件，就在哪个线程消费事件。如果需要切换线程，就需要用到 Scheduler （调度器）。</p>
<h5 id="Scheduler_的_API_(一)">Scheduler 的 API (一)</h5><p>在RxJava 中，Scheduler ——调度器，相当于线程控制器，RxJava 通过它来指定每一段代码应该运行在什么样的线程。RxJava 已经内置了几个 Scheduler ，它们已经适合大多数的使用场景：</p>
<ul>
<li>Schedulers.immediate(): 直接在当前线程运行，相当于不指定线程。这是默认的 Scheduler。</li>
<li>Schedulers.newThread(): 总是启用新线程，并在新线程执行操作。</li>
<li>Schedulers.io(): I/O 操作（读写文件、读写数据库、网络信息交互等）所使用的 Scheduler。行为模式和 newThread() 差不多，区别在于 io() 的内部实现是是用一个无数量上限的线程池，可以重用空闲的线程，因此多数情况下 io() 比 newThread() 更有效率。不要把计算工作放在 io() 中，可以避免创建不必要的线程。</li>
<li>Schedulers.computation(): 计算所使用的 Scheduler。这个计算指的是 CPU 密集型计算，即不会被 I/O 等操作限制性能的操作，例如图形的计算。这个 Scheduler 使用的固定的线程池，大小为 CPU 核数。不要把 I/O 操作放在 computation() 中，否则 I/O 操作的等待时间会浪费 CPU。</li>
<li>另外， Android 还有一个专用的 AndroidSchedulers.mainThread()，它指定的操作将在 Android 主线程运行。</li>
</ul>
<p>有了这几个 Scheduler ，就可以使用 subscribeOn() 和 observeOn() 两个方法来对线程进行控制了。</p>
<ul>
<li>subscribeOn(): 指定 subscribe() 所发生的线程，即 Observable.OnSubscribe 被激活时所处的线程。或者叫做事件产生的线程。</li>
<li>observeOn(): 指定 Subscriber 所运行在的线程。或者叫做事件消费的线程。</li>
</ul>
<p>文字叙述总归难理解，上代码：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">Observable.just(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>)</span><br><span class="line">    .subscribeOn(Schedulers.io()) <span class="comment">// 指定 subscribe() 发生在 IO 线程</span></span><br><span class="line">    .observeOn(AndroidSchedulers.mainThread()) <span class="comment">// 指定 Subscriber 的回调发生在主线程</span></span><br><span class="line">    .subscribe(<span class="keyword">new</span> Action1&lt;Integer&gt;() &#123;</span><br><span class="line">        @<span class="function">Override</span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title">call</span><span class="params">(Integer number)</span> </span>&#123;</span><br><span class="line">            Log.d(tag, <span class="string">"number:"</span> + number);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br></pre></td></tr></table></figure>
<p>上面这段代码中，由于 subscribeOn(Schedulers.io()) 的指定，被创建的事件的内容 1、2、3、4 将会在 IO 线程发出；而由于 observeOn(AndroidScheculers.mainThread()) 的指定，因此 subscriber 数字的打印将发生在主线程 。事实上，这种在 subscribe() 之前写上两句 subscribeOn(Scheduler.io()) 和 observeOn(AndroidSchedulers.mainThread()) 的使用方式非常常见，它适用于多数的 『后台线程取数据，主线程显示』的程序策略。</p>
<p>而前面提到的由图片 id 取得图片并显示的例子，如果也加上这两句：</p>
<figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> drawableRes = ...;</span><br><span class="line">ImageView imageView = ...;</span><br><span class="line">Observable.create(<span class="keyword">new</span> OnSubscribe&lt;Drawable&gt;() &#123;</span><br><span class="line">    <span class="annotation">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">call</span><span class="params">(Subscriber&lt;? <span class="keyword">super</span> Drawable&gt; subscriber)</span> </span>&#123;</span><br><span class="line">        Drawable drawable = getTheme().getDrawable(drawableRes));</span><br><span class="line">        subscriber.onNext(drawable);</span><br><span class="line">        subscriber.onCompleted();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;)</span><br><span class="line">.subscribeOn(Schedulers.io()) <span class="comment">// 指定 subscribe() 发生在 IO 线程</span></span><br><span class="line">.observeOn(AndroidSchedulers.mainThread()) <span class="comment">// 指定 Subscriber 的回调发生在主线程</span></span><br><span class="line">.subscribe(<span class="keyword">new</span> Observer&lt;Drawable&gt;() &#123;</span><br><span class="line">    <span class="annotation">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">onNext</span><span class="params">(Drawable drawable)</span> </span>&#123;</span><br><span class="line">        imageView.setImageDrawable(drawable);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="annotation">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">onCompleted</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="annotation">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">onError</span><span class="params">(Throwable e)</span> </span>&#123;</span><br><span class="line">        Toast.makeText(activity, <span class="string">"Error!"</span>, Toast.LENGTH_SHORT).show();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>那么，加载图片将会发生在 IO 线程，而设置图片则被设定在了主线程。这就意味着，即使加载图片耗费了几十甚至几百毫秒的时间，也不会造成丝毫界面的卡顿。</p>
<h5 id="Scheduler_的原理_(一)">Scheduler 的原理 (一)</h5><p>RxJava 的 Scheduler API 很方便，也很神奇（加了一句话就把线程切换了，怎么做到的？而且 subscribe() 不是最外层直接调用的方法吗，它竟然也能被指定线程？）。然而 Scheduler 的原理需要放在后面讲，因为它的原理是以下一节《变换》的原理作为基础的。</p>
<p>好吧这一节其实我屁也没说，只是为了让你安心，让你知道我不是忘了讲原理，而是把它放在了更合适的地方。</p>
<h4 id="变换">变换</h4><p>终于要到牛逼的地方了，不管你激动不激动，反正我是激动了。</p>
<p>RxJava 提供了对事件序列进行变换的支持，这是它的核心功能之一，也是大多数人说『RxJava 真是太好用了』的最大原因。<strong>所谓变换，就是将事件序列中的对象或整个序列进行加工处理，转换成不同的事件或事件序列。概念说着总是模糊难懂的，来看 API。</strong></p>
<h5 id="API">API</h5><p>首先看一个 map() 的例子：</p>
<figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line">Observable.just(<span class="string">"images/logo.png"</span>) <span class="comment">// 输入类型 String</span></span><br><span class="line">    .map(<span class="keyword">new</span> Func1&lt;String, Bitmap&gt;() &#123;</span><br><span class="line">        <span class="annotation">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="function">Bitmap <span class="title">call</span><span class="params">(String filePath)</span> </span>&#123; <span class="comment">// 参数类型 String</span></span><br><span class="line">            <span class="function"><span class="keyword">return</span> <span class="title">getBitmapFromPath</span><span class="params">(filePath)</span></span>; <span class="comment">// 返回类型 Bitmap</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">    .subscribe(<span class="keyword">new</span> Action1&lt;Bitmap&gt;() &#123;</span><br><span class="line">        <span class="annotation">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">call</span><span class="params">(Bitmap bitmap)</span> </span>&#123; <span class="comment">// 参数类型 Bitmap</span></span><br><span class="line">            showBitmap(bitmap);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br></pre></td></tr></table></figure>
<p>这里出现了一个叫做 Func1 的类。它和 Action1 非常相似，也是 RxJava 的一个接口，用于包装含有一个参数的方法。 Func1 和 Action 的区别在于， Func1 包装的是有返回值的方法。另外，和 ActionX 一样， FuncX 也有多个，用于不同参数个数的方法。FuncX 和 ActionX 的区别在 FuncX 包装的是有返回值的方法。</p>
<p>可以看到，map() 方法将参数中的 String 对象转换成一个 Bitmap 对象后返回，而在经过 map() 方法后，事件的参数类型也由 String 转为了 Bitmap。这种直接变换对象并返回的，是最常见的也最容易理解的变换。不过 RxJava 的变换远不止这样，它不仅可以针对事件对象，还可以针对整个事件队列，这使得 RxJava 变得非常灵活。我列举几个常用的变换：</p>
<ul>
<li>map(): 事件对象的直接变换，具体功能上面已经介绍过。它是 RxJava 最常用的变换。 map() 的示意图： </li>
</ul>
<p><img src="http://7xn2zf.com1.z0.glb.clouddn.com/rxjavarxjava_6.png" alt=""></p>
<ul>
<li>flatMap(): 这是一个很有用但非常难理解的变换，因此我决定花多些篇幅来介绍它。 首先假设这么一种需求：假设有一个数据结构『学生』，现在需要打印出一组学生的名字。实现方式很简单：</li>
</ul>
<figure class="highlight processing"><table><tr><td class="code"><pre><span class="line">Student[] students = ...;</span><br><span class="line">Subscriber&lt;<span class="keyword">String</span>&gt; subscriber = <span class="keyword">new</span> Subscriber&lt;<span class="keyword">String</span>&gt;() &#123;</span><br><span class="line">    @Override</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> onNext(<span class="keyword">String</span> name) &#123;</span><br><span class="line">        Log.d(tag, name);</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line">Observable.from(students)</span><br><span class="line">    .<span class="built_in">map</span>(<span class="keyword">new</span> Func1&lt;Student, <span class="keyword">String</span>&gt;() &#123;</span><br><span class="line">        @Override</span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">String</span> call(Student student) &#123;</span><br><span class="line">            <span class="keyword">return</span> student.getName();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">    .subscribe(subscriber);</span><br></pre></td></tr></table></figure>
<p>很简单。那么再假设：如果要打印出每个学生所需要修的所有课程的名称呢？（需求的区别在于，每个学生只有一个名字，但却有多个课程。）首先可以这样实现：</p>
<figure class="highlight cs"><table><tr><td class="code"><pre><span class="line">Student[] students = ...;</span><br><span class="line">Subscriber&lt;Student&gt; subscriber = <span class="keyword">new</span> Subscriber&lt;Student&gt;() &#123;</span><br><span class="line">    @<span class="function">Override</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onNext</span>(<span class="params">Student student</span>) </span>&#123;</span><br><span class="line">        List&lt;Course&gt; courses = student.getCourses();</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; courses.size(); i++) &#123;</span><br><span class="line">            Course course = courses.<span class="keyword">get</span>(i);</span><br><span class="line">            Log.d(tag, course.getName());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line">Observable.<span class="keyword">from</span>(students)</span><br><span class="line">    .subscribe(subscriber);</span><br></pre></td></tr></table></figure>
<p>依然很简单。那么如果我不想在 Subscriber 中使用 for 循环，而是希望 Subscriber 中直接传入单个的 Course 对象呢（这对于代码复用很重要）？用 map() 显然是不行的，因为 map() 是一对一的转化，而我现在的要求是一对多的转化。那怎么才能把一个 Student 转化成多个 Course 呢？</p>
<p>这个时候，就需要用 flatMap() 了：</p>
<figure class="highlight cs"><table><tr><td class="code"><pre><span class="line">Student[] students = ...;</span><br><span class="line">Subscriber&lt;Course&gt; subscriber = <span class="keyword">new</span> Subscriber&lt;Course&gt;() &#123;</span><br><span class="line">    @<span class="function">Override</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onNext</span>(<span class="params">Course course</span>) </span>&#123;</span><br><span class="line">        Log.d(tag, course.getName());</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br><span class="line">Observable.<span class="keyword">from</span>(students)</span><br><span class="line">    .flatMap(<span class="keyword">new</span> Func1&lt;Student, Observable&lt;Course&gt;&gt;() &#123;</span><br><span class="line">        @<span class="function">Override</span><br><span class="line">        <span class="keyword">public</span> Observable&lt;Course&gt; <span class="title">call</span>(<span class="params">Student student</span>) </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> Observable.<span class="keyword">from</span>(student.getCourses());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">    .subscribe(subscriber);</span><br></pre></td></tr></table></figure>
<p>从上面的代码可以看出， flatMap() 和 map() 有一个相同点：它也是把传入的参数转化之后返回另一个对象。但需要注意，和 map() 不同的是， flatMap() 中返回的是个 Observable 对象，并且这个 Observable 对象并不是被直接发送到了 Subscriber 的回调方法中。 flatMap() 的原理是这样的：1. 使用传入的事件对象创建一个 Observable 对象；2. 并不发送这个 Observable, 而是将它激活，于是它开始发送事件；3. 每一个创建出来的 Observable 发送的事件，都被汇入同一个 Observable ，而这个 Observable 负责将这些事件统一交给 Subscriber 的回调方法。这三个步骤，把事件拆成了两级，通过一组新创建的 Observable 将初始的对象『铺平』之后通过统一路径分发了下去。而这个『铺平』就是 flatMap() 所谓的 flat。</p>
<p>flatMap() 示意图：</p>
<p><img src="http://7xn2zf.com1.z0.glb.clouddn.com/rxjavarxjava_7.png" alt=""></p>
<p>扩展：由于可以在嵌套的 Observable 中添加异步代码， flatMap() 也常用于嵌套的异步操作，例如嵌套的网络请求。示例代码（Retrofit + RxJava）：</p>
<figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line">networkClient.token() <span class="comment">// 返回 Observable&lt;String&gt;，在订阅时请求 token，并在响应后发送 token</span></span><br><span class="line">    .flatMap(<span class="keyword">new</span> Func1&lt;String, Observable&lt;Messages&gt;&gt;() &#123;</span><br><span class="line">        <span class="annotation">@Override</span></span><br><span class="line">        <span class="keyword">public</span> Observable&lt;Messages&gt; call(String token) &#123;</span><br><span class="line">            <span class="comment">// 返回 Observable&lt;Messages&gt;，在订阅时请求消息列表，并在响应后发送请求到的消息列表</span></span><br><span class="line">            <span class="function"><span class="keyword">return</span> networkClient.<span class="title">messages</span><span class="params">()</span></span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">    .subscribe(<span class="keyword">new</span> Action1&lt;Messages&gt;() &#123;</span><br><span class="line">        <span class="annotation">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">call</span><span class="params">(Messages messages)</span> </span>&#123;</span><br><span class="line">            <span class="comment">// 处理显示消息列表</span></span><br><span class="line">            showMessages(messages);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br></pre></td></tr></table></figure>
<p>传统的嵌套请求需要使用嵌套的 Callback 来实现。而通过 flatMap() ，可以把嵌套的请求写在一条链中，从而保持程序逻辑的清晰。</p>
<p><code>throttleFirst()</code>: 在每次事件触发后的一定时间间隔内丢弃新的事件。常用作去抖动过滤，例如按钮的点击监听器：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">RxView.clickEvents(button) <span class="comment">// RxBinding 代码，后面的文章有解释</span></span><br><span class="line">    .throttleFirst(<span class="number">500</span>, TimeUnit.MILLISECONDS) <span class="comment">// 设置防抖间隔为 500ms</span></span><br><span class="line">    .subscribe(subscriber);</span><br></pre></td></tr></table></figure>
<p>此外， RxJava 还提供很多便捷的方法来实现事件序列的变换，这里就不一一举例了。</p>
<h5 id="变换的原理：lift()">变换的原理：lift()</h5><p>这些变换虽然功能各有不同，但实质上都是针对事件序列的处理和再发送。而在 RxJava 的内部，它们是基于同一个基础的变换方法： lift(Operator)。首先看一下 lift() 的内部实现（仅核心代码）：</p>
<figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 注意：这不是 lift() 的源码，而是将源码中与性能、兼容性、扩展性有关的代码剔除后的核心代码。</span></span><br><span class="line"><span class="comment">// 如果需要看源码，可以去 RxJava 的 GitHub 仓库下载。</span></span><br><span class="line"><span class="keyword">public</span> &lt;R&gt; Observable&lt;R&gt; lift(Operator&lt;? <span class="keyword">extends</span> R, ? <span class="keyword">super</span> T&gt; operator) &#123;</span><br><span class="line">    <span class="function"><span class="keyword">return</span> Observable.<span class="title">create</span><span class="params">(<span class="keyword">new</span> OnSubscribe&lt;R&gt;()</span> </span>&#123;</span><br><span class="line">        <span class="annotation">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">call</span><span class="params">(Subscriber subscriber)</span> </span>&#123;</span><br><span class="line">            Subscriber newSubscriber = operator.call(subscriber);</span><br><span class="line">            newSubscriber.onStart();</span><br><span class="line">            onSubscribe.call(newSubscriber);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这段代码很有意思：它生成了一个新的 Observable 并返回，而且创建新 Observable 所用的参数 OnSubscribe 的回调方法 call() 中的实现竟然看起来和前面讲过的 Observable.subscribe() 一样！然而它们并不一样哟~不一样的地方关键就在于第二行 onSubscribe.call(subscriber) 中的 onSubscribe 所指代的对象不同（高能预警：接下来的几句话可能会导致身体的严重不适）——</p>
<ul>
<li>subscribe() 中这句话的 onSubscribe 指的是 Observable 中的 onSubscribe 对象，这个没有问题，但是 lift() 之后的情况就复杂了点。</li>
<li>当含有 lift() 时： <ol>
<li>lift() 创建了一个 Observable 后，加上之前的原始 Observable，已经有两个 Observable 了； </li>
<li>而同样地，新 Observable 里的新 OnSubscribe 加上之前的原始 Observable 中的原始 OnSubscribe，也就有了两个 OnSubscribe； </li>
<li>当用户调用经过 lift() 后的 Observable 的 subscribe() 的时候，使用的是 lift() 所返回的新的 Observable ，于是它所触发的 onSubscribe.call(subscriber)，也是用的新 Observable 中的新 OnSubscribe，即在 lift() 中生成的那个 OnSubscribe； </li>
<li>而这个新 OnSubscribe 的 call() 方法中的 onSubscribe ，就是指的原始 Observable 中的原始 OnSubscribe ，在这个 call() 方法里，新 OnSubscribe 利用 operator.call(subscriber) 生成了一个新的 Subscriber（Operator 就是在这里，通过自己的 call() 方法将新 Subscriber 和原始 Subscriber 进行关联，并插入自己的『变换』代码以实现变换），然后利用这个新 Subscriber 向原始 Observable 进行订阅。<br>这样就实现了 lift() 过程，有点<strong>像一种代理机制，通过事件拦截和处理实现事件序列的变换。</strong></li>
</ol>
</li>
</ul>
<p>精简掉细节的话，也可以这么说：在 Observable 执行了 lift(Operator) 方法之后，会返回一个新的 Observable，这个新的 Observable 会像一个代理一样，负责接收原始的 Observable 发出的事件，并在处理后发送给 Subscriber。</p>
<p>如果你更喜欢具象思维，可以看图：</p>
<p><img src="http://7xn2zf.com1.z0.glb.clouddn.com/rxjavarxjava_8.png" alt=""></p>
<p>或者可以看动图：</p>
<p><img src="http://7xn2zf.com1.z0.glb.clouddn.com/rxjavarxjava_8_d.gif" alt=""></p>
<p>两次和多次的 lift() 同理，如下图：</p>
<p><img src="http://7xn2zf.com1.z0.glb.clouddn.com/rxjavarxjava_9.png" alt=""></p>
<p>举一个具体的 Operator 的实现。下面这是一个将事件中的 Integer 对象转换成 String 的例子，仅供参考：</p>
<figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line">observable.lift(<span class="keyword">new</span> Observable.Operator&lt;String, Integer&gt;() &#123;</span><br><span class="line">    <span class="annotation">@Override</span></span><br><span class="line">    <span class="keyword">public</span> Subscriber&lt;? <span class="keyword">super</span> Integer&gt; call(<span class="keyword">final</span> Subscriber&lt;? <span class="keyword">super</span> String&gt; subscriber) &#123;</span><br><span class="line">        <span class="comment">// 将事件序列中的 Integer 对象转换为 String 对象</span></span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> Subscriber&lt;Integer&gt;() &#123;</span><br><span class="line">            <span class="annotation">@Override</span></span><br><span class="line">            <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">onNext</span><span class="params">(Integer integer)</span> </span>&#123;</span><br><span class="line">                subscriber.onNext(<span class="string">""</span> + integer);</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="annotation">@Override</span></span><br><span class="line">            <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">onCompleted</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                subscriber.onCompleted();</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="annotation">@Override</span></span><br><span class="line">            <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">onError</span><span class="params">(Throwable e)</span> </span>&#123;</span><br><span class="line">                subscriber.onError(e);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<blockquote>
<p>讲述 lift() 的原理只是为了让你更好地了解 RxJava ，从而可以更好地使用它。然而不管你是否理解了 lift() 的原理，RxJava 都不建议开发者自定义 Operator 来直接使用 lift()，而是建议尽量使用已有的 lift() 包装方法（如 map() flatMap() 等）进行组合来实现需求，因为直接使用 lift() 非常容易发生一些难以发现的错误。</p>
</blockquote>
<h5 id="compose:_对_Observable_整体的变换">compose: 对 Observable 整体的变换</h5><p>除了 lift() 之外， Observable 还有一个变换方法叫做 compose(Transformer)。它和 lift() 的区别在于， lift() <strong>是针对事件项和事件序列的，而 compose() 是针对 Observable 自身进行变换。</strong>举个例子，假设在程序中有多个 Observable ，并且他们都需要应用一组相同的 lift() 变换。你可以这么写：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="tag">observable1</span></span><br><span class="line">    <span class="class">.lift1</span>()</span><br><span class="line">    <span class="class">.lift2</span>()</span><br><span class="line">    <span class="class">.lift3</span>()</span><br><span class="line">    <span class="class">.lift4</span>()</span><br><span class="line">    <span class="class">.subscribe</span>(<span class="tag">subscriber1</span>);</span><br><span class="line"><span class="tag">observable2</span></span><br><span class="line">    <span class="class">.lift1</span>()</span><br><span class="line">    <span class="class">.lift2</span>()</span><br><span class="line">    <span class="class">.lift3</span>()</span><br><span class="line">    <span class="class">.lift4</span>()</span><br><span class="line">    <span class="class">.subscribe</span>(<span class="tag">subscriber2</span>);</span><br><span class="line"><span class="tag">observable3</span></span><br><span class="line">    <span class="class">.lift1</span>()</span><br><span class="line">    <span class="class">.lift2</span>()</span><br><span class="line">    <span class="class">.lift3</span>()</span><br><span class="line">    <span class="class">.lift4</span>()</span><br><span class="line">    <span class="class">.subscribe</span>(<span class="tag">subscriber3</span>);</span><br><span class="line"><span class="tag">observable4</span></span><br><span class="line">    <span class="class">.lift1</span>()</span><br><span class="line">    <span class="class">.lift2</span>()</span><br><span class="line">    <span class="class">.lift3</span>()</span><br><span class="line">    <span class="class">.lift4</span>()</span><br><span class="line">    <span class="class">.subscribe</span>(<span class="tag">subscriber1</span>);</span><br></pre></td></tr></table></figure>
<p>你觉得这样太不软件工程了，于是你改成了这样：</p>
<figure class="highlight dns"><table><tr><td class="code"><pre><span class="line">private Observable liftAll(Observable observable) &#123;</span><br><span class="line">    return observable</span><br><span class="line">        .lift1()</span><br><span class="line">        .lift2()</span><br><span class="line">        .lift3()</span><br><span class="line">        .lift4()<span class="comment">;</span></span><br><span class="line">&#125;</span><br><span class="line">...</span><br><span class="line">liftAll(observable1).subscribe(subscriber1)<span class="comment">;</span></span><br><span class="line">liftAll(observable2).subscribe(subscriber2)<span class="comment">;</span></span><br><span class="line">liftAll(observable3).subscribe(subscriber3)<span class="comment">;</span></span><br><span class="line">liftAll(observable4).subscribe(subscriber4)<span class="comment">;</span></span><br></pre></td></tr></table></figure>
<p>可读性、可维护性都提高了。可是 Observable 被一个方法包起来，这种方式对于 Observale 的灵活性似乎还是增添了那么点限制。怎么办？这个时候，就应该用 compose() 来解决了：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">LiftAllTransformer</span> <span class="keyword">implements</span> <span class="title">Observable</span>.<span class="title">Transformer</span>&lt;<span class="title">Integer</span>, <span class="title">String</span>&gt; </span>&#123;</span><br><span class="line">    <span class="annotation">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Observable&lt;String&gt; <span class="title">call</span><span class="params">(Observable&lt;Integer&gt; observable)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> observable</span><br><span class="line">            .lift1()</span><br><span class="line">            .lift2()</span><br><span class="line">            .lift3()</span><br><span class="line">            .lift4();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">...</span><br><span class="line">Transformer liftAll = <span class="keyword">new</span> LiftAllTransformer();</span><br><span class="line">observable1.compose(liftAll).subscribe(subscriber1);</span><br><span class="line">observable2.compose(liftAll).subscribe(subscriber2);</span><br><span class="line">observable3.compose(liftAll).subscribe(subscriber3);</span><br><span class="line">observable4.compose(liftAll).subscribe(subscriber4);</span><br></pre></td></tr></table></figure>
<p>像上面这样，使用 compose() 方法，Observable 可以利用传入的 Transformer 对象的 call 方法直接对自身进行处理，也就不必被包在方法的里面了。</p>
<p>compose() 的原理比较简单，不附图喽。</p>
<h4 id="线程控制：Scheduler_(二)">线程控制：Scheduler (二)</h4><p>除了灵活的变换，RxJava 另一个牛逼的地方，就是线程的自由控制。</p>
<h5 id="Scheduler_的_API_(二)">Scheduler 的 API (二)</h5><p>前面讲到了，可以利用 subscribeOn() 结合 observeOn() 来实现线程控制，让事件的产生和消费发生在不同的线程。可是在了解了 map() flatMap() 等变换方法后，有些好事的（其实就是当初刚接触 RxJava 时的我）就问了：能不能多切换几次线程？</p>
<p>答案是：能。因为 observeOn() 指定的是 Subscriber 的线程，而这个 Subscriber 并不是（严格说应该为『不一定是』，但这里不妨理解为『不是』）subscribe() 参数中的 Subscriber ，而是 observeOn() 执行时的当前 Observable 所对应的 Subscriber ，即它的直接下级 Subscriber 。换句话说，observeOn() 指定的是它之后的操作所在的线程。因此如果有多次切换线程的需求，只要在每个想要切换线程的位置调用一次 observeOn() 即可。上代码：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">Observable.just(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>) <span class="comment">// IO 线程，由 subscribeOn() 指定</span></span><br><span class="line">    .subscribeOn(Schedulers.io())</span><br><span class="line">    .observeOn(Schedulers.newThread())</span><br><span class="line">    .<span class="built_in">map</span>(mapOperator) <span class="comment">// 新线程，由 observeOn() 指定</span></span><br><span class="line">    .observeOn(Schedulers.io())</span><br><span class="line">    .<span class="built_in">map</span>(mapOperator2) <span class="comment">// IO 线程，由 observeOn() 指定</span></span><br><span class="line">    .observeOn(AndroidSchedulers.mainThread) </span><br><span class="line">    .subscribe(subscriber);  <span class="comment">// Android 主线程，由 observeOn() 指定</span></span><br></pre></td></tr></table></figure>
<p>如上，通过 observeOn() 的多次调用，程序实现了线程的多次切换。</p>
<p>不过，不同于 observeOn() ， subscribeOn() 的位置放在哪里都可以，但它是只能调用一次的。</p>
<p>又有好事的（其实还是当初的我）问了：如果我非要调用多次 subscribeOn() 呢？会有什么效果？</p>
<p>这个问题先放着，我们还是从 RxJava 线程控制的原理说起吧。</p>
<h5 id="Scheduler_的原理（二）">Scheduler 的原理（二）</h5><p>其实， subscribeOn() 和 observeOn() 的内部实现，也是用的 lift()。具体看图（不同颜色的箭头表示不同的线程）：</p>
<p>subscribeOn() 原理图：</p>
<p><img src="http://7xn2zf.com1.z0.glb.clouddn.com/rxjavarxjava_10.png" alt=""></p>
<p>observeOn() 原理图：</p>
<p><img src="http://7xn2zf.com1.z0.glb.clouddn.com/rxjavarxjava_11.png" alt=""></p>
<p>从图中可以看出，subscribeOn() 和 observeOn() 都做了线程切换的工作（图中的 “schedule…” 部位）。不同的是， subscribeOn() 的线程切换发生在 OnSubscribe 中，即在它通知上一级 OnSubscribe 时，这时事件还没有开始发送，因此 subscribeOn() 的线程控制可以从事件发出的开端就造成影响；而 observeOn() 的线程切换则发生在它内建的 Subscriber 中，即发生在它即将给下一级 Subscriber 发送事件时，因此 observeOn() 控制的是它后面的线程。</p>
<p>最后，我用一张图来解释当多个 subscribeOn() 和 observeOn() 混合使用时，线程调度是怎么发生的（由于图中对象较多，相对于上面的图对结构做了一些简化调整）：</p>
<p><img src="http://7xn2zf.com1.z0.glb.clouddn.com/rxjavarxjava_12.png" alt=""></p>
<p>图中共有 5 处含有对事件的操作。由图中可以看出，①和②两处受第一个 subscribeOn() 影响，运行在红色线程；③和④处受第一个 observeOn() 的影响，运行在绿色线程；⑤处受第二个 onserveOn() 影响，运行在紫色线程；而第二个 subscribeOn() ，由于在通知过程中线程就被第一个 subscribeOn() 截断，因此对整个流程并没有任何影响。这里也就回答了前面的问题：当使用了多个 subscribeOn() 的时候，只有第一个 subscribeOn() 起作用。</p>
<h5 id="延伸：doOnSubscribe()">延伸：doOnSubscribe()</h5><p>然而，虽然超过一个的 subscribeOn() 对事件处理的流程没有影响，但在流程之前却是可以利用的。</p>
<p>在前面讲 Subscriber 的时候，提到过 Subscriber 的 onStart() 可以用作流程开始前的初始化。然而 onStart() 由于在 subscribe() 发生时就被调用了，因此不能指定线程，而是只能执行在 subscribe() 被调用时的线程。这就导致如果 onStart() 中含有对线程有要求的代码（例如在界面上显示一个 ProgressBar，这必须在主线程执行），将会有线程非法的风险，因为有时你无法预测 subscribe() 将会在什么线程执行。</p>
<p>而与 Subscriber.onStart() 相对应的，有一个方法 Observable.doOnSubscribe() 。它和 Subscriber.onStart() 同样是在 subscribe() 调用后而且在事件发送前执行，但区别在于它可以指定线程。默认情况下， doOnSubscribe() 执行在 subscribe() 发生的线程；而如果在 doOnSubscribe() 之后有 subscribeOn() 的话，它将执行在离它最近的 subscribeOn() 所指定的线程。</p>
<p>示例代码：</p>
<figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line">Observable.create(onSubscribe)</span><br><span class="line">    .subscribeOn(Schedulers.io())</span><br><span class="line">    .doOnSubscribe(<span class="keyword">new</span> Action0() &#123;</span><br><span class="line">        <span class="annotation">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">call</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            progressBar.setVisibility(View.VISIBLE); <span class="comment">// 需要在主线程执行</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">    .subscribeOn(AndroidSchedulers.mainThread()) <span class="comment">// 指定主线程</span></span><br><span class="line">    .observeOn(AndroidSchedulers.mainThread())</span><br><span class="line">    .subscribe(subscriber);</span><br></pre></td></tr></table></figure>
<p>如上，在 doOnSubscribe()的后面跟一个 subscribeOn() ，就能指定准备工作的线程了。</p>
<h4 id="RxJava_的适用场景和使用方式">RxJava 的适用场景和使用方式</h4><h5 id="与_Retrofit_的结合">与 Retrofit 的结合</h5><blockquote>
<p>Retrofit 是 Square 的一个著名的网络请求库。没有用过 Retrofit 的可以选择跳过这一小节也没关系，我举的每种场景都只是个例子，而且例子之间并无前后关联，只是个抛砖引玉的作用，所以你跳过这里看别的场景也可以的。</p>
</blockquote>
<p>Retrofit 除了提供了传统的 Callback 形式的 API，还有 RxJava 版本的 Observable 形式 API。下面我用对比的方式来介绍 Retrofit 的 RxJava 版 API 和传统版本的区别。</p>
<p>以获取一个 User 对象的接口作为例子。使用Retrofit 的传统 API，你可以用这样的方式来定义请求：</p>
<figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="variable">@GET</span>(<span class="string">"/user"</span>)</span><br><span class="line">public void <span class="function">getUser</span>(<span class="variable">@Query</span>(<span class="string">"userId"</span>) String userId, Callback&lt;User&gt; callback);</span><br></pre></td></tr></table></figure>
<p>在程序的构建过程中， Retrofit 会把自动把方法实现并生成代码，然后开发者就可以利用下面的方法来获取特定用户并处理响应：</p>
<figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line">getUser(userId, <span class="keyword">new</span> Callback&lt;User&gt;() &#123;</span><br><span class="line">    <span class="annotation">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">success</span><span class="params">(User user)</span> </span>&#123;</span><br><span class="line">        userView.setUser(user);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="annotation">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">failure</span><span class="params">(RetrofitError <span class="keyword">error</span>)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// Error handling</span></span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>而使用 RxJava 形式的 API，定义同样的请求是这样的：</p>
<figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="variable">@GET</span>(<span class="string">"/user"</span>)</span><br><span class="line">public Observable&lt;User&gt; <span class="function">getUser</span>(<span class="variable">@Query</span>(<span class="string">"userId"</span>) String userId);</span><br></pre></td></tr></table></figure>
<p>使用的时候是这样的：</p>
<figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line">getUser(userId)</span><br><span class="line">    .observeOn(AndroidSchedulers.mainThread())</span><br><span class="line">    .subscribe(<span class="keyword">new</span> Observer&lt;User&gt;() &#123;</span><br><span class="line">        <span class="annotation">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">onNext</span><span class="params">(User user)</span> </span>&#123;</span><br><span class="line">            userView.setUser(user);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="annotation">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">onCompleted</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="annotation">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">onError</span><span class="params">(Throwable <span class="keyword">error</span>)</span> </span>&#123;</span><br><span class="line">            <span class="comment">// Error handling</span></span><br><span class="line">            ...</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br></pre></td></tr></table></figure>
<p>看到区别了吗？</p>
<p>当 RxJava 形式的时候，Retrofit 把请求封装进 Observable ，在请求结束后调用 onNext() 或在请求失败后调用 onError()。</p>
<p>对比来看， Callback 形式和 Observable 形式长得不太一样，但本质都差不多，而且在细节上 Observable 形式似乎还比 Callback 形式要差点。那 Retrofit 为什么还要提供 RxJava 的支持呢？</p>
<p>因为它好用啊！从这个例子看不出来是因为这只是最简单的情况。而一旦情景复杂起来， Callback 形式马上就会开始让人头疼。比如：</p>
<p>假设这么一种情况：你的程序取到的 User 并不应该直接显示，而是需要先与数据库中的数据进行比对和修正后再显示。使用 Callback 方式大概可以这么写：</p>
<figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line">getUser(userId, <span class="keyword">new</span> Callback&lt;User&gt;() &#123;</span><br><span class="line">    <span class="annotation">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">success</span><span class="params">(User user)</span> </span>&#123;</span><br><span class="line">        processUser(user); <span class="comment">// 尝试修正 User 数据</span></span><br><span class="line">        userView.setUser(user);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="annotation">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">failure</span><span class="params">(RetrofitError <span class="keyword">error</span>)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// Error handling</span></span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>有问题吗？</p>
<p>很简便，但不要这样做。为什么？因为这样做会影响性能。数据库的操作很重，一次读写操作花费 10~20ms 是很常见的，这样的耗时很容易造成界面的卡顿。所以通常情况下，如果可以的话一定要避免在主线程中处理数据库。所以为了提升性能，这段代码可以优化一下：</p>
<figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line">getUser(userId, <span class="keyword">new</span> Callback&lt;User&gt;() &#123;</span><br><span class="line">    <span class="annotation">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">success</span><span class="params">(User user)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">new</span> Thread() &#123;</span><br><span class="line">            <span class="annotation">@Override</span></span><br><span class="line">            <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                processUser(user); <span class="comment">// 尝试修正 User 数据</span></span><br><span class="line">                runOnUiThread(<span class="keyword">new</span> Runnable() &#123; <span class="comment">// 切回 UI 线程</span></span><br><span class="line">                    <span class="annotation">@Override</span></span><br><span class="line">                    <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                        userView.setUser(user);</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;);</span><br><span class="line">            &#125;).start();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="annotation">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">failure</span><span class="params">(RetrofitError <span class="keyword">error</span>)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// Error handling</span></span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>性能问题解决，但……这代码实在是太乱了，迷之缩进啊！杂乱的代码往往不仅仅是美观问题，因为代码越乱往往就越难读懂，而如果项目中充斥着杂乱的代码，无疑会降低代码的可读性，造成团队开发效率的降低和出错率的升高。</p>
<p>这时候，如果用 RxJava 的形式，就好办多了。 RxJava 形式的代码是这样的：</p>
<figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line">getUser(userId)</span><br><span class="line">    .doOnNext(<span class="keyword">new</span> Action1&lt;User&gt;() &#123;</span><br><span class="line">        <span class="annotation">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">call</span><span class="params">(User user)</span> </span>&#123;</span><br><span class="line">            processUser(user);</span><br><span class="line">        &#125;)</span><br><span class="line">    .observeOn(AndroidSchedulers.mainThread())</span><br><span class="line">    .subscribe(<span class="keyword">new</span> Observer&lt;User&gt;() &#123;</span><br><span class="line">        <span class="annotation">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">onNext</span><span class="params">(User user)</span> </span>&#123;</span><br><span class="line">            userView.setUser(user);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="annotation">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">onCompleted</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="annotation">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">onError</span><span class="params">(Throwable <span class="keyword">error</span>)</span> </span>&#123;</span><br><span class="line">            <span class="comment">// Error handling</span></span><br><span class="line">            ...</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br></pre></td></tr></table></figure>
<p>后台代码和前台代码全都写在一条链中，明显清晰了很多。</p>
<p>再举一个例子：假设 /user 接口并不能直接访问，而需要填入一个在线获取的 token ，代码应该怎么写？</p>
<p>Callback 方式，可以使用嵌套的 Callback：</p>
<figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@GET</span>(<span class="string">"/token"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">getToken</span><span class="params">(Callback&lt;String&gt; callback)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="annotation">@GET</span>(<span class="string">"/user"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> getUser(<span class="annotation">@Query</span>(<span class="string">"token"</span>) String token, <span class="annotation">@Query</span>(<span class="string">"userId"</span>) String userId, Callback&lt;User&gt; callback);</span><br><span class="line"></span><br><span class="line">...</span><br><span class="line"></span><br><span class="line">getToken(<span class="keyword">new</span> Callback&lt;String&gt;() &#123;</span><br><span class="line">    <span class="annotation">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">success</span><span class="params">(String token)</span> </span>&#123;</span><br><span class="line">        getUser(userId, <span class="keyword">new</span> Callback&lt;User&gt;() &#123;</span><br><span class="line">            <span class="annotation">@Override</span></span><br><span class="line">            <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">success</span><span class="params">(User user)</span> </span>&#123;</span><br><span class="line">                userView.setUser(user);</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="annotation">@Override</span></span><br><span class="line">            <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">failure</span><span class="params">(RetrofitError <span class="keyword">error</span>)</span> </span>&#123;</span><br><span class="line">                <span class="comment">// Error handling</span></span><br><span class="line">                ...</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="annotation">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">failure</span><span class="params">(RetrofitError <span class="keyword">error</span>)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// Error handling</span></span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>倒是没有什么性能问题，可是迷之缩进毁一生，你懂我也懂，做过大项目的人应该更懂。</p>
<p>而使用 RxJava 的话，代码是这样的：</p>
<figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@GET</span>(<span class="string">"/token"</span>)</span><br><span class="line"><span class="keyword">public</span> Observable&lt;String&gt; getToken();</span><br><span class="line"></span><br><span class="line"><span class="annotation">@GET</span>(<span class="string">"/user"</span>)</span><br><span class="line"><span class="keyword">public</span> Observable&lt;User&gt; getUser(<span class="annotation">@Query</span>(<span class="string">"token"</span>) String token, <span class="annotation">@Query</span>(<span class="string">"userId"</span>) String userId);</span><br><span class="line"></span><br><span class="line">...</span><br><span class="line"></span><br><span class="line">getToken()</span><br><span class="line">    .flatMap(<span class="keyword">new</span> Func1&lt;String, Observable&lt;User&gt;&gt;() &#123;</span><br><span class="line">        <span class="annotation">@Override</span></span><br><span class="line">        <span class="keyword">public</span> Observable&lt;User&gt; onNext(String token) &#123;</span><br><span class="line">            <span class="function"><span class="keyword">return</span> <span class="title">getUser</span><span class="params">(token, userId)</span></span>;</span><br><span class="line">        &#125;)</span><br><span class="line">    .observeOn(AndroidSchedulers.mainThread())</span><br><span class="line">    .subscribe(<span class="keyword">new</span> Observer&lt;User&gt;() &#123;</span><br><span class="line">        <span class="annotation">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">onNext</span><span class="params">(User user)</span> </span>&#123;</span><br><span class="line">            userView.setUser(user);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="annotation">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">onCompleted</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="annotation">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">onError</span><span class="params">(Throwable <span class="keyword">error</span>)</span> </span>&#123;</span><br><span class="line">            <span class="comment">// Error handling</span></span><br><span class="line">            ...</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br></pre></td></tr></table></figure>
<p>用一个 flatMap() 就搞定了逻辑，依然是一条链。看着就很爽，是吧？</p>
<p>好，Retrofit 部分就到这里。</p>
<h5 id="RxBinding">RxBinding</h5><p><a href="https://github.com/JakeWharton/RxBinding" target="_blank" rel="external">RxBinding</a> 是 Jake Wharton 的一个开源库，它提供了一套在 Android 平台上的基于 RxJava 的 Binding API。所谓 Binding，就是类似设置 OnClickListener 、设置 TextWatcher 这样的注册绑定对象的 API。</p>
<p>举个设置点击监听的例子。使用 RxBinding ，可以把事件监听用这样的方法来设置：</p>
<figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line">Button button = ...;</span><br><span class="line">RxView.clickEvents(button) <span class="comment">// 以 Observable 形式来反馈点击事件</span></span><br><span class="line">    .subscribe(<span class="keyword">new</span> Action1&lt;ViewClickEvent&gt;() &#123;</span><br><span class="line">        <span class="annotation">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">call</span><span class="params">(ViewClickEvent event)</span> </span>&#123;</span><br><span class="line">            <span class="comment">// Click handling</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br></pre></td></tr></table></figure>
<p>看起来除了形式变了没什么区别，实质上也是这样。甚至如果你看一下它的源码，你会发现它连实现都没什么惊喜：它的内部是直接用一个包裹着的 setOnClickListener() 来实现的。然而，仅仅这一个形式的改变，却恰好就是 RxBinding 的目的：扩展性。通过 RxBinding 把点击监听转换成 Observable 之后，就有了对它进行扩展的可能。扩展的方式有很多，根据需求而定。一个例子是前面提到过的 throttleFirst() ，用于去抖动，也就是消除手抖导致的快速连环点击：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="tag">RxView</span><span class="class">.clickEvents</span>(<span class="tag">button</span>)</span><br><span class="line">    <span class="class">.throttleFirst</span>(500, <span class="tag">TimeUnit</span><span class="class">.MILLISECONDS</span>)</span><br><span class="line">    <span class="class">.subscribe</span>(<span class="tag">clickAction</span>);</span><br></pre></td></tr></table></figure>
<p>如果想对 RxBinding 有更多了解，可以去它的 GitHub 项目 下面看看。</p>
<h5 id="各种异步操作">各种异步操作</h5><p>前面举的 Retrofit 和 RxBinding 的例子，是两个可以提供现成的 Observable 的库。而如果你有某些异步操作无法用这些库来自动生成 Observable，也完全可以自己写。例如数据库的读写、大图片的载入、文件压缩/解压等各种需要放在后台工作的耗时操作，都可以用 RxJava 来实现，有了之前几章的例子，这里应该不用再举例了。</p>
<h4 id="RxBus">RxBus</h4><p>RxBus 名字看起来像一个库，但它并不是一个库，而是一种模式，它的思想是使用 RxJava 来实现了 EventBus ，而让你不再需要使用 Otto 或者 GreenRobot 的 EventBus。至于什么是 RxBus，可以看这篇文章。顺便说一句，Flipboard 已经用 RxBus 替换掉了 Otto ，目前为止没有不良反应。</p>
<h4 id="最后">最后</h4><p>对于 Android 开发者来说， RxJava 是一个很难上手的库，因为它对于 Android 开发者来说有太多陌生的概念了。可是它真的很牛逼。因此，我写了这篇《给 Android 开发者的 RxJava 详解》，希望能给始终搞不明白什么是 RxJava 的人一些入门的指引，或者能让正在使用 RxJava 但仍然心存疑惑的人看到一些更深入的解析。无论如何，只要能给各位同为 Android 工程师的你们提供一些帮助，这篇文章的目的就达到了。</p>
]]></content>
    <summary type="html">
    <![CDATA[<blockquote>
<p>转载自：<a href="http://gank.io/post/560e15be2dca930e00da1083" target="_blank" rel="external">给 Android 开发者的 RxJava 详解</a></p>
<]]>
    </summary>
    
      <category term="RxJava" scheme="http://gcblog.cn/tags/RxJava/"/>
    
      <category term="RxJava" scheme="http://gcblog.cn/categories/RxJava/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[Vim命令]]></title>
    <link href="http://gcblog.cn/2016/01/02/vim/"/>
    <id>http://gcblog.cn/2016/01/02/vim/</id>
    <published>2016-01-02T12:23:56.000Z</published>
    <updated>2016-01-02T14:28:09.000Z</updated>
    <content type="html"><![CDATA[<blockquote>
<p>转载自：<a href="http://my.oschina.net/u/1455799/blog/214448?fromerr=oMXPHQ26" target="_blank" rel="external">http://my.oschina.net/u/1455799/blog/214448?fromerr=oMXPHQ26</a></p>
</blockquote>
<h3 id="第一级_–_存活">第一级 – 存活</h3><figure class="highlight asciidoc"><table><tr><td class="code"><pre><span class="line">i → Insert 模式，按 ESC 回到 Normal 模式.</span><br><span class="line"></span><br><span class="line">x → 删当前光标所在的一个字符。</span><br><span class="line"></span><br><span class="line"><span class="attribute">:wq → 存盘 + 退出 (:w</span> 存盘, :q 退出)   （陈皓注：:w 后可以跟文件名）</span><br><span class="line"></span><br><span class="line">dd → 删除当前行，并把删除的行存到剪贴板里</span><br><span class="line"></span><br><span class="line">p → 粘贴剪贴板</span><br><span class="line"></span><br><span class="line">推荐:</span><br><span class="line"></span><br><span class="line">hjkl (强例推荐使用其移动光标，但不必需) →你也可以使用光标键 (←↓↑→). 注: j 就像下箭头。</span><br><span class="line"></span><br><span class="line"><span class="attribute">:help &lt;command&gt; → 显示相关命令的帮助。你也可以就输入 :help</span> 而不跟命令。（陈皓注：退出帮助需要输入:q）</span><br></pre></td></tr></table></figure>
<h3 id="第二级_–_感觉良好">第二级 – 感觉良好</h3><h4 id="各种插入模式">各种插入模式</h4><figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line"><span class="tag">a</span> → 在光标后插入</span><br><span class="line"></span><br><span class="line">o → 在当前行后插入一个新行</span><br><span class="line"></span><br><span class="line">O → 在当前行前插入一个新行</span><br><span class="line"></span><br><span class="line">cw → 替换从光标所在位置后到一个单词结尾的字符</span><br></pre></td></tr></table></figure>
<h4 id="简单的移动光标">简单的移动光标</h4><figure class="highlight fortran"><table><tr><td class="code"><pre><span class="line"><span class="number">0</span> → 数字零，到行头</span><br><span class="line"></span><br><span class="line">^ → 到本行第一个不是<span class="keyword">blank</span>字符的位置（所谓<span class="keyword">blank</span>字符就是空格，tab，换行，回车等）</span><br><span class="line"></span><br><span class="line">$ → 到本行行尾</span><br><span class="line"></span><br><span class="line">g_ → 到本行最后一个不是<span class="keyword">blank</span>字符的位置。</span><br><span class="line"></span><br><span class="line">/pattern → 搜索 pattern 的字符串（陈皓注：如果搜索出多个匹配，可按n键到下一个）</span><br></pre></td></tr></table></figure>
<h4 id="拷贝/粘贴">拷贝/粘贴</h4><figure class="highlight tp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">P</span> → 粘贴（p/<span class="keyword">P</span>都可以，p是表示在当前位置之后，<span class="keyword">P</span>表示在当前位置之前）</span><br><span class="line"></span><br><span class="line">yy → 拷贝当前行当行于 ddP</span><br></pre></td></tr></table></figure>
<h4 id="Undo/Redo">Undo/Redo</h4><figure class="highlight mel"><table><tr><td class="code"><pre><span class="line">u → <span class="keyword">undo</span></span><br><span class="line"></span><br><span class="line">&lt;C-r&gt; → <span class="keyword">redo</span></span><br></pre></td></tr></table></figure>
<h4 id="打开/保存/退出/改变文件(Buffer)">打开/保存/退出/改变文件(Buffer)</h4><figure class="highlight asciidoc"><table><tr><td class="code"><pre><span class="line">:e &lt;path/to/file&gt; → 打开一个文件</span><br><span class="line"></span><br><span class="line">:w → 存盘</span><br><span class="line"></span><br><span class="line">:saveas &lt;path/to/file&gt; → 另存为 &lt;path/to/file&gt;</span><br><span class="line"></span><br><span class="line"><span class="attribute">:x， ZZ 或 :wq</span> → 保存并退出 (:x 表示仅在需要时保存，ZZ不需要输入冒号并回车)</span><br><span class="line"></span><br><span class="line"><span class="attribute">:q! → 退出不保存 :qa!</span> 强行退出所有的正在编辑的文件，就算别的文件有更改。</span><br><span class="line"></span><br><span class="line"><span class="attribute">:bn 和 :bp</span> → 你可以同时打开很多文件，使用这两个命令来切换下一个或上一个文件。（陈皓注：我喜欢使用:n到下一个文件）</span><br></pre></td></tr></table></figure>
<h3 id="第三级_–_更好，更强，更快">第三级 – 更好，更强，更快</h3><figure class="highlight livecodeserver"><table><tr><td class="code"><pre><span class="line">. → (小数点) 可以重复上一次的命令</span><br><span class="line"></span><br><span class="line">N&lt;<span class="command"><span class="keyword">command</span>&gt; → 重复某个命令<span class="title">N</span>次</span></span><br></pre></td></tr></table></figure>
<p>下面是一个示例，找开一个文件你可以试试下面的命令：</p>
<blockquote>
<ul>
<li><p>2dd → 删除2行</p>
</li>
<li><p>3p → 粘贴文本3次</p>
</li>
<li><p>100idesu [ESC] → 会写下 “desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu desu “</p>
</li>
<li><p>→ 重复上一个命令—— 100 “desu “.</p>
</li>
<li><p>→ 重复 3 次 “desu” (注意：不是 300，你看，VIM多聪明啊).</p>
</li>
</ul>
</blockquote>
<h4 id="更强">更强</h4><p>你要让你的光标移动更有效率，你一定要了解下面的这些命令，千万别跳过。</p>
<figure class="highlight stata"><table><tr><td class="code"><pre><span class="line">NG → 到第 <span class="keyword">N</span> 行 （陈皓注：注意命令中的<span class="keyword">G</span>是大写的，另我一般使用 : <span class="keyword">N</span> 到第<span class="keyword">N</span>行，如 :137 到第137行）</span><br><span class="line"></span><br><span class="line">gg → 到第一行。（陈皓注：相当于1G，或 :1）</span><br><span class="line"></span><br><span class="line"><span class="keyword">G</span> → 到最后一行。</span><br></pre></td></tr></table></figure>
<p>按单词移动：</p>
<blockquote>
<p>如果你认为单词是由默认方式，那么就用小写的e和w。默认上来说，一个单词由字母，数字和下划线组成（陈皓注：程序变量）</p>
<p>如果你认为单词是由blank字符分隔符，那么你需要使用大写的E和W。（陈皓注：程序语句）</p>
</blockquote>
<p><img src="http://static.oschina.net/uploads/img/201403/30213311_pgWj.jpg" alt=""></p>
<ol>
<li><p>w → 到下一个单词的开头。</p>
</li>
<li><p>e → 到下一个单词的结尾。</p>
</li>
</ol>
<p>下面，让我来说说最强的光标移动：</p>
<blockquote>
<ul>
<li><p>% : 匹配括号移动，包括 (, {, [. （陈皓注：你需要把光标先移到括号上）</p>
</li>
<li><p>和 #:  匹配光标当前所在的单词，移动光标到下一个（或上一个）匹配单词（*是下一个，#是上一个）</p>
</li>
</ul>
</blockquote>
<h4 id="更快">更快</h4><p>你一定要记住光标的移动，因为很多命令都可以和这些移动光标的命令连动。很多命令都可以如下来干：</p>
<p><start position=""><command><end position=""></end></start></p>
<p>例如 0y$ 命令意味着：</p>
<ul>
<li><p>0 → 先到行头</p>
</li>
<li><p>y → 从这里开始拷贝</p>
</li>
<li><p>$ → 拷贝到本行最后一个字符</p>
</li>
</ul>
<p>你可可以输入 ye，从当前位置拷贝到本单词的最后一个字符。</p>
<p>你也可以输入 y2/foo 来拷贝2个 “foo” 之间的字符串。</p>
<p>还有很多时间并不一定你就一定要按y才会拷贝，下面的命令也会被拷贝：</p>
<ul>
<li><p>d (删除 )</p>
</li>
<li><p>v (可视化的选择)</p>
</li>
<li><p>gU (变大写)</p>
</li>
<li><p>gu (变小写)</p>
</li>
<li><p>等等</p>
</li>
</ul>
<h3 id="第四级_–_Vim_超能力">第四级 – Vim 超能力</h3><p>你只需要掌握前面的命令，你就可以很舒服的使用VIM了。但是，现在，我们向你介绍的是VIM杀手级的功能。下面这些功能是我只用vim的原因。</p>
<p>在当前行上移动光标: 0 ^ $ f F t T , ;</p>
<blockquote>
<ul>
<li><p>0 → 到行头</p>
</li>
<li><p>^ → 到本行的第一个非blank字符</p>
</li>
<li><p>$ → 到行尾</p>
</li>
<li><p>g_ → 到本行最后一个不是blank字符的位置。</p>
</li>
<li><p>fa → 到下一个为a的字符处，你也可以fs到下一个为s的字符。</p>
</li>
<li><p>t, → 到逗号前的第一个字符。逗号可以变成其它字符。</p>
</li>
<li><p>3fa → 在当前行查找第三个出现的a。</p>
</li>
<li><p>F 和 T → 和 f 和 t 一样，只不过是相反方向。</p>
</li>
</ul>
</blockquote>
<p><img src="http://static.oschina.net/uploads/img/201403/30213311_S4ef.jpg" alt=""></p>
<p>还有一个很有用的命令是 dt” → 删除所有的内容，直到遇到双引号—— “。</p>
<h4 id="区域选择_&lt;action&gt;a&lt;object&gt;_或_&lt;action&gt;i&lt;object&gt;">区域选择 <code>&lt;action&gt;a&lt;object&gt; 或 &lt;action&gt;i&lt;object&gt;</code></h4><p>在visual 模式下，这些命令很强大，其命令格式为</p>
<p><code>&lt;action&gt;a&lt;object&gt; 和 &lt;action&gt;i&lt;object&gt;</code></p>
<ul>
<li><p>action可以是任何的命令，如 d (删除), y (拷贝), v (可以视模式选择)。</p>
</li>
<li><p>object 可能是： w 一个单词， W 一个以空格为分隔的单词， s 一个句字， p 一个段落。也可以是一个特别的字符：”、 ‘、 )、 }、 ]。</p>
</li>
</ul>
<p>假设你有一个字符串 (map (+) (“foo”)).而光标键在第一个 o 的位置。</p>
<blockquote>
<ul>
<li><p>vi” → 会选择 foo.</p>
</li>
<li><p>va” → 会选择 “foo”.</p>
</li>
<li><p>vi) → 会选择 “foo”.</p>
</li>
<li><p>va) → 会选择(“foo”).</p>
</li>
<li><p>v2i) → 会选择 map (+) (“foo”)</p>
</li>
<li><p>v2a) → 会选择 (map (+) (“foo”))</p>
</li>
</ul>
</blockquote>
<p><img src="http://static.oschina.net/uploads/img/201403/30213319_fpAj.png" alt=""></p>
<h4 id="块操作:_&lt;C-v&gt;">块操作: <code>&lt;C-v&gt;</code></h4><p>块操作，典型的操作： <code>0 &lt;C-v&gt; &lt;C-d&gt; I-- [ESC]</code></p>
<ul>
<li>^ → 到行头</li>
<li><code>&lt;C-v&gt;</code> → 开始块操作</li>
<li><code>&lt;C-d&gt;</code> → 向下移动 (你也可以使用hjkl来移动光标，或是使用%，或是别的)</li>
<li>I– [ESC] → I是插入，插入“–”，按ESC键来为每一行生效。</li>
</ul>
<p><img src="http://static.oschina.net/uploads/img/201403/30213319_eOK2.gif" alt=""></p>
<p>在Windows下的vim，你需要使用 <code>&lt;C-q&gt;</code> 而不是 <code>&lt;C-v&gt;</code> ，<code>&lt;C-v&gt;</code> 是拷贝剪贴板。</p>
<h4 id="自动提示：_&lt;C-n&gt;_和_&lt;C-p&gt;">自动提示： <code>&lt;C-n&gt; 和 &lt;C-p&gt;</code></h4><p>在 Insert 模式下，你可以输入一个词的开头，然后按 <code>&lt;C-p&gt;或是&lt;C-n&gt;</code>，自动补齐功能就出现了……</p>
<p><img src="http://static.oschina.net/uploads/img/201403/30213320_pPoI.gif" alt=""></p>
<h4 id="宏录制：_qa_操作序列_q,_@a_,_@@">宏录制： qa 操作序列 q, @a , @@</h4><ul>
<li><p>qa 把你的操作记录在寄存器 a。</p>
</li>
<li><p>于是 @a  会replay被录制的宏。</p>
</li>
<li><p>@@ 是一个快捷键用来replay最新录制的宏。</p>
</li>
</ul>
<blockquote>
<p><strong><em>示例</em></strong></p>
<p>在一个只有一行且这一行只有“1”的文本中，键入如下命令：</p>
<ul>
<li><p>qaYp<c-a>q→</c-a></p>
<ul>
<li><p>qa 开始录制</p>
</li>
<li><p>Yp 复制行.</p>
</li>
<li><p><code>&lt;C-a&gt;</code> 增加1.</p>
</li>
<li><p>q 停止录制.</p>
</li>
</ul>
</li>
<li><p>@a  → 在1下面写下 2</p>
</li>
<li><p>@@ → 在2 正面写下3</p>
</li>
<li><p>现在做 100@@ 会创建新的100行，并把数据增加到 103.</p>
</li>
</ul>
</blockquote>
<p><img src="http://static.oschina.net/uploads/img/201403/30213321_axRo.gif" alt=""></p>
<h4 id="可视化选择：_v,V,&lt;C-v&gt;">可视化选择： v,V,<code>&lt;C-v&gt;</code></h4><p>前面，我们看到了 <code>&lt;C-v&gt;</code>的示例 （在Windows下应该是<code>&lt;C-q&gt;</code>），我们可以使用 v 和 V。一但被选好了，你可以做下面的事：</p>
<ul>
<li><p>J → 把所有的行连接起来（变成一行）</p>
</li>
<li><p>&lt; 或 &gt; → 左右缩进</p>
</li>
<li><p>= → 自动给缩进 </p>
</li>
</ul>
<p><img src="http://static.oschina.net/uploads/img/201403/30213324_dgd1.gif" alt=""></p>
<p>在所有被选择的行后加上点东西：</p>
<ul>
<li><p><code>&lt;C-v&gt;</code></p>
</li>
<li><p>选中相关的行 (可使用 j 或 <code>&lt;C-d&gt;</code> 或是 /pattern 或是 % 等……)</p>
</li>
<li><p>$ 到行最后</p>
</li>
<li><p>A, 输入字符串，按 ESC。</p>
</li>
</ul>
<p><img src="http://static.oschina.net/uploads/img/201403/30213326_mQtz.gif" alt=""></p>
<h4 id="分屏:_:split_和_vsplit-">分屏: :split 和 vsplit.</h4><p>下面是主要的命令，你可以使用VIM的帮助 :help split. 你可以参考本站以前的一篇文章VIM分屏。</p>
<blockquote>
<ul>
<li><p>:split → 创建分屏 (:vsplit创建垂直分屏)</p>
</li>
<li><p><code>&lt;C-w&gt;&lt;dir&gt;</code> : dir就是方向，可以是 hjkl 或是 ←↓↑→ 中的一个，其用来切换分屏。</p>
</li>
<li><p><code>&lt;C-w&gt;_</code> (或 <code>&lt;C-w&gt;|</code>) : 最大化尺寸 (<code>&lt;C-w&gt;|</code> 垂直分屏)</p>
</li>
<li><p><code>&lt;C-w&gt;+</code> (或 <code>&lt;C-w&gt;-</code>) : 增加尺寸</p>
</li>
</ul>
</blockquote>
<p><img src="http://static.oschina.net/uploads/img/201403/30213328_ANuy.gif" alt=""></p>
]]></content>
    <summary type="html">
    <![CDATA[<blockquote>
<p>转载自：<a href="http://my.oschina.net/u/1455799/blog/214448?fromerr=oMXPHQ26" target="_blank" rel="external">http://my.oschina.]]>
    </summary>
    
      <category term="Vim" scheme="http://gcblog.cn/tags/Vim/"/>
    
      <category term="Vim" scheme="http://gcblog.cn/categories/Vim/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[BCD码]]></title>
    <link href="http://gcblog.cn/2015/12/28/BCD/"/>
    <id>http://gcblog.cn/2015/12/28/BCD/</id>
    <published>2015-12-28T15:05:58.000Z</published>
    <updated>2015-12-28T15:17:30.000Z</updated>
    <content type="html"><![CDATA[<h4 id="简介">简介</h4><blockquote>
<p>BCD码（Binary-Coded Decimal‎）亦称二进码十进数或二-十进制代码。用4位二进制数来表示1位十进制数中的0~9这10个数码。是一种二进制的数字编码形式，用二进制编码的十进制代码。BCD码这种编码形式利用了四个位元来储存一个十进制的数码，使二进制和十进制之间的转换得以快捷的进行。这种编码技巧最常用于会计系统的设计里，因为会计制度经常需要对很长的数字串作准确的计算。相对于一般的浮点式记数法，采用BCD码，既可保存数值的精确度，又可免去使电脑作浮点运算时所耗费的时间。此外，对于其他需要高精确度的计算，BCD编码亦很常用。</p>
</blockquote>
<p>BCD码可分为有权码和无权码两类：有权BCD码有8421码、2421码、5421码，其中8421码是最常用的；无权BCD码有余3码，余3循环码等。</p>
<p>最常用的BCD编码，就是使用”0”至”9”这十个数值的二进码来表示。这种编码方式，在称之为“8421码”（日常所说的BCD码大都是指8421BCD码形式）。</p>
<p>附注：压缩BCD码与非压缩BCD码的区别—— 压缩BCD码的每一位用4位二进制表示，一个字节表示两位十进制数。例如10010110B表示十进制数96D；非压缩BCD码用1个字节表示一位十进制数，高四位总是0000，低4位的0000~1001表示0~9.例如00001000B表示十进制数8.</p>
<h4 id="BCD码的运算法则">BCD码的运算法则</h4><p>BCD码的运算规则：BCD码是十进制数，而运算器对数据做加减运算时，都是按二进制运算规则进行处理的。这样，当将 BCD码传送给运算器进行运算时，其结果需要修正。修正的规则是：当两个BCD码相加，如果和等于或小于 1001(即十进制数9)，不需要修正；如果相加之和在 1010 到1111(即十六进制数 0AH～0FH)之间，则需加 6 进行修正；如果相加时，本位产生了进位，也需加 6 进行修正。这样做的原因是，机器按二进制相加，所以 4 位二进制数相加时，是按“逢十六进一”的原则进行运算的，而实质上是 2 个十进制数相加，应该按“逢十进一”的原则相加，16 与10相差 6，所以当和超过 9或有进位时，都要加 6 进行修正。下面举例说明。</p>
<ol>
<li><p>计算 5+8；</p>
<p>将 5 和 8 以 8421 BCD输入机器，则运算如下：</p>
<p>0 1 0 1</p>
<p>+) 1 0 0 0</p>
<p>1 1 0 1 结果大于 9</p>
<p>+) 0 1 1 0 加 6 修正</p>
<p>1 0 0 1 1 即13 的 BCD码</p>
<p>结果是 0011，即十进制数3，还产生了进位。5+8=13，结论正确。</p>
</li>
<li><p>计算 8+8</p>
<p> 将8以8421 BCD输入机器，则运算如下：</p>
<p> 1 0 0 0</p>
<p> +）1 0 0 0</p>
<p> 1 0 0 0 0 结果大于9</p>
<p> +）0 1 1 0 加6修正</p>
<p> 1 0 1 1 0 16的BCD码</p>
<p> 结果是0110，即十进制的6，而且产生进位。8+8=16，结论正确。</p>
</li>
</ol>
<h4 id="Java_BCD方法">Java BCD方法</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span><br><span class="line"> * Created by chrisgong on 15/12/28.</span><br><span class="line"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> BCDUtils &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span><br><span class="line">     * @功能: BCD码转为10进制串(阿拉伯数据)</span><br><span class="line">     * @参数: BCD码</span><br><span class="line">     * @结果: 10进制串</span><br><span class="line">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> String <span class="title">bcd2Str</span><span class="params">(byte[] bytes)</span> </span>&#123;</span><br><span class="line">        StringBuffer temp = <span class="keyword">new</span> StringBuffer(bytes.length * <span class="number">2</span>);</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; bytes.length; i++) &#123;</span><br><span class="line">            temp.append((byte) ((bytes[i] &amp; <span class="number">0xf0</span>) &gt;&gt;&gt; <span class="number">4</span>));</span><br><span class="line">            temp.append((byte) (bytes[i] &amp; <span class="number">0x0f</span>));</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> temp.toString().substring(<span class="number">0</span>, <span class="number">1</span>).equalsIgnoreCase(<span class="string">"0"</span>) ? temp</span><br><span class="line">                .toString().substring(<span class="number">1</span>) : temp.toString();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span><br><span class="line">     * @功能: 10进制串转为BCD码</span><br><span class="line">     * @参数: 10进制串</span><br><span class="line">     * @结果: BCD码</span><br><span class="line">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> byte[] str2Bcd(String asc) &#123;</span><br><span class="line">        <span class="keyword">int</span> len = asc.length();</span><br><span class="line">        <span class="keyword">int</span> mod = len % <span class="number">2</span>;</span><br><span class="line">        <span class="keyword">if</span> (mod != <span class="number">0</span>) &#123;</span><br><span class="line">            asc = <span class="string">"0"</span> + asc;</span><br><span class="line">            len = asc.length();</span><br><span class="line">        &#125;</span><br><span class="line">        byte abt[] = <span class="keyword">new</span> byte[len];</span><br><span class="line">        <span class="keyword">if</span> (len &gt;= <span class="number">2</span>) &#123;</span><br><span class="line">            len = len / <span class="number">2</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        byte bbt[] = <span class="keyword">new</span> byte[len];</span><br><span class="line">        abt = asc.getBytes();</span><br><span class="line">        <span class="keyword">int</span> j, k;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> p = <span class="number">0</span>; p &lt; asc.length() / <span class="number">2</span>; p++) &#123;</span><br><span class="line">            <span class="keyword">if</span> ((abt[<span class="number">2</span> * p] &gt;= <span class="string">'0'</span>) &amp;&amp; (abt[<span class="number">2</span> * p] &lt;= <span class="string">'9'</span>)) &#123;</span><br><span class="line">                j = abt[<span class="number">2</span> * p] - <span class="string">'0'</span>;</span><br><span class="line">            &#125; <span class="keyword">else</span> <span class="keyword">if</span> ((abt[<span class="number">2</span> * p] &gt;= <span class="string">'a'</span>) &amp;&amp; (abt[<span class="number">2</span> * p] &lt;= <span class="string">'z'</span>)) &#123;</span><br><span class="line">                j = abt[<span class="number">2</span> * p] - <span class="string">'a'</span> + <span class="number">0x0a</span>;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                j = abt[<span class="number">2</span> * p] - <span class="string">'A'</span> + <span class="number">0x0a</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> ((abt[<span class="number">2</span> * p + <span class="number">1</span>] &gt;= <span class="string">'0'</span>) &amp;&amp; (abt[<span class="number">2</span> * p + <span class="number">1</span>] &lt;= <span class="string">'9'</span>)) &#123;</span><br><span class="line">                k = abt[<span class="number">2</span> * p + <span class="number">1</span>] - <span class="string">'0'</span>;</span><br><span class="line">            &#125; <span class="keyword">else</span> <span class="keyword">if</span> ((abt[<span class="number">2</span> * p + <span class="number">1</span>] &gt;= <span class="string">'a'</span>) &amp;&amp; (abt[<span class="number">2</span> * p + <span class="number">1</span>] &lt;= <span class="string">'z'</span>)) &#123;</span><br><span class="line">                k = abt[<span class="number">2</span> * p + <span class="number">1</span>] - <span class="string">'a'</span> + <span class="number">0x0a</span>;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                k = abt[<span class="number">2</span> * p + <span class="number">1</span>] - <span class="string">'A'</span> + <span class="number">0x0a</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">int</span> a = (j &lt;&lt; <span class="number">4</span>) + k;</span><br><span class="line">            byte b = (byte) a;</span><br><span class="line">            bbt[p] = b;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> bbt;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    <summary type="html">
    <![CDATA[<h4 id="简介">简介</h4><blockquote>
<p>BCD码（Binary-Coded Decimal‎）亦称二进码十进数或二-十进制代码。用4位二进制数来表示1位十进制数中的0~9这10个数码。是一种二进制的数字编码形式，用二进制编码的十进制代码。BCD码这种]]>
    </summary>
    
      <category term="Algorithm" scheme="http://gcblog.cn/tags/Algorithm/"/>
    
      <category term="Algorithm" scheme="http://gcblog.cn/categories/Algorithm/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[Dex Method Size 计算脚本]]></title>
    <link href="http://gcblog.cn/2015/12/27/dex-method/"/>
    <id>http://gcblog.cn/2015/12/27/dex-method/</id>
    <published>2015-12-27T14:26:49.000Z</published>
    <updated>2015-12-27T14:43:17.000Z</updated>
    <content type="html"><![CDATA[<p>The script：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="shebang">#!/bin/bash</span><br><span class="line"></span></span><br><span class="line">SDK_DIR=<span class="variable">$1</span> <span class="comment"># SDK dexdump路径</span></span><br><span class="line">APK_DIR=<span class="variable">$2</span>  <span class="comment"># 需要计算的APK地址</span></span><br><span class="line"></span><br><span class="line"><span class="variable">$1</span>/./dexdump <span class="operator">-f</span> <span class="variable">$2</span> | grep method_ids_size</span><br></pre></td></tr></table></figure>
<p>The results:</p>
<figure class="highlight groovy"><table><tr><td class="code"><pre><span class="line">➜  temp  .<span class="regexp">/dex_method.sh /</span>Users<span class="regexp">/chrisgong/</span>Library<span class="regexp">/Android/</span>sdk<span class="regexp">/build-tools/</span><span class="number">23.0</span><span class="number">.2</span> <span class="regexp">~/mac/</span>git<span class="regexp">/cim-android/</span>build<span class="regexp">/outputs/</span>apk/cim-android-debug-v1<span class="number">.3</span><span class="number">.2</span>.apk</span><br><span class="line"></span><br><span class="line"><span class="string">method_ids_size     :</span> <span class="number">40899</span></span><br></pre></td></tr></table></figure>]]></content>
    <summary type="html">
    <![CDATA[<p>The script：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="shebang">#!/bin/bash</spa]]>
    </summary>
    
      <category term="Android" scheme="http://gcblog.cn/tags/Android/"/>
    
      <category term="Android" scheme="http://gcblog.cn/categories/Android/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[Logger - Android日志输出库]]></title>
    <link href="http://gcblog.cn/2015/12/19/logger/"/>
    <id>http://gcblog.cn/2015/12/19/logger/</id>
    <published>2015-12-19T04:40:51.000Z</published>
    <updated>2015-12-19T04:55:15.000Z</updated>
    <content type="html"><![CDATA[<h3 id="Logger">Logger</h3><blockquote>
<p>原文地址： <a href="https://github.com/orhanobut/logger" target="_blank" rel="external">https://github.com/orhanobut/logger</a></p>
</blockquote>
<h4 id="简介">简介</h4><p>Simple, pretty and powerful logger for android</p>
<p>Logger provides :</p>
<ul>
<li>Thread information</li>
<li>Class information</li>
<li>Method information</li>
<li>Pretty-print for json content</li>
<li>Pretty-print for new line “\n”</li>
<li>Clean output</li>
<li>Jump to source</li>
</ul>
<h4 id="依赖">依赖</h4><p>gradle 导入</p>
<figure class="highlight gradle"><table><tr><td class="code"><pre><span class="line"><span class="keyword">repositories</span> &#123;</span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">  maven &#123; url <span class="string">"https://jitpack.io"</span> &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">dependencies</span> &#123;</span><br><span class="line">  <span class="keyword">compile</span> <span class="string">'com.github.orhanobut:logger:1.12'</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="使用">使用</h4><h5 id="初始化">初始化</h5><p><em>在程序初始化时候实现以下代码（Application，或者LAUNCHER Activity中）</em></p>
<figure class="highlight haskell"><table><tr><td class="code"><pre><span class="line"><span class="type">Logger</span></span><br><span class="line">  .init(<span class="type">YOUR_TAG</span>)                 // <span class="default"><span class="keyword">default</span> <span class="type">PRETTYLOGGER</span> or use just init<span class="container">()</span></span></span><br><span class="line">  .methodCount(<span class="number">3</span>)                 // <span class="default"><span class="keyword">default</span> 2</span></span><br><span class="line">  .hideThreadInfo()               // <span class="default"><span class="keyword">default</span> shown</span></span><br><span class="line">  .logLevel(<span class="type">LogLevel</span>.<span class="type">NONE</span>)        // <span class="default"><span class="keyword">default</span> <span class="type">LogLevel</span>.<span class="type">FULL</span></span></span><br><span class="line">  .methodOffset(<span class="number">2</span>)                // <span class="default"><span class="keyword">default</span> 0</span></span><br><span class="line">  .logTool(new <span class="type">AndroidLogTool</span>()); // custom log tool, optional</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5 id="支持格式">支持格式</h5><figure class="highlight stata"><table><tr><td class="code"><pre><span class="line">Logger.<span class="literal">d</span>(<span class="string">"hello"</span>);</span><br><span class="line">Logger.<span class="literal">e</span>(<span class="string">"hello"</span>);</span><br><span class="line">Logger.<span class="literal">w</span>(<span class="string">"hello"</span>);</span><br><span class="line">Logger.v(<span class="string">"hello"</span>);</span><br><span class="line">Logger.wtf(<span class="string">"hello"</span>);</span><br><span class="line">Logger.json(JSON_CONTENT);</span><br><span class="line">Logger.xml(XML_CONTENT);</span><br></pre></td></tr></table></figure>
<h5 id="举个栗子">举个栗子</h5><figure class="highlight stata"><table><tr><td class="code"><pre><span class="line">Logger.<span class="literal">d</span>(<span class="string">"hello"</span>);</span><br><span class="line">Logger.<span class="literal">e</span>(exception, <span class="string">"message"</span>);</span><br><span class="line">Logger.json(JSON_CONTENT);</span><br></pre></td></tr></table></figure>
<p><img src="https://raw.githubusercontent.com/orhanobut/logger/master/images/logger-log.png" alt=""></p>
]]></content>
    <summary type="html">
    <![CDATA[<h3 id="Logger">Logger</h3><blockquote>
<p>原文地址： <a href="https://github.com/orhanobut/logger" target="_blank" rel="external">https://github]]>
    </summary>
    
      <category term="Android" scheme="http://gcblog.cn/tags/Android/"/>
    
      <category term="Android" scheme="http://gcblog.cn/categories/Android/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[gradle学习笔记 构建脚本概要]]></title>
    <link href="http://gcblog.cn/2015/12/06/gradle-study-3/"/>
    <id>http://gcblog.cn/2015/12/06/gradle-study-3/</id>
    <published>2015-12-06T02:37:31.000Z</published>
    <updated>2015-12-19T04:57:39.000Z</updated>
    <content type="html"><![CDATA[<h3 id="构建块">构建块</h3><p>一个gradle构建包含三个基本构建块：project、task、property。每个构建包含至少一个project，进而又包含一个或多个task。</p>
<h4 id="project">project</h4><h4 id="task">task</h4><h5 id="扩展属性">扩展属性</h5><figure class="highlight gradle"><table><tr><td class="code"><pre><span class="line"><span class="keyword">project</span>.ext.myProp = <span class="string">'myValue'</span></span><br><span class="line"></span><br><span class="line">ext &#123;</span><br><span class="line">    someOtherProp = <span class="number">123</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">assert myProp == <span class="string">'myValue'</span></span><br><span class="line"><span class="keyword">println</span> <span class="keyword">project</span>.someOtherProp</span><br><span class="line">ext.someOtherProp = <span class="number">567</span></span><br></pre></td></tr></table></figure>
<h5 id="Gradle属性">Gradle属性</h5><p>通过再gradle.properties文件中声明直接添加到项目中，这个文件中声明的属性对所有的项目可用。</p>
<p>gradle.properties</p>
<figure class="highlight ini"><table><tr><td class="code"><pre><span class="line"><span class="setting">exampleProp = <span class="value">myValue</span></span></span><br><span class="line"><span class="setting">someOtherProp = <span class="value"><span class="number">455</span></span></span></span><br></pre></td></tr></table></figure>
<p>builde.gradle</p>
<figure class="highlight gradle"><table><tr><td class="code"><pre><span class="line">assert <span class="keyword">project</span>.exampleProp == <span class="string">'myValue'</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">task</span> printGradleProperty &lt;&lt; &#123;</span><br><span class="line">    <span class="keyword">println</span> <span class="string">"Second property: $someOtherProp"</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight groovy"><table><tr><td class="code"><pre><span class="line">➜  gradle-properties <span class="string">git:</span>(master) ✗ gradle pGP</span><br><span class="line">:printGradleProperty</span><br><span class="line">Second <span class="string">property:</span> <span class="number">455</span></span><br><span class="line"></span><br><span class="line">BUILD SUCCESSFUL</span><br><span class="line"></span><br><span class="line">Total <span class="string">time:</span> <span class="number">1.24</span> secs</span><br><span class="line"></span><br><span class="line">This build could be faster, please consider using the Gradle <span class="string">Daemon:</span> <span class="string">https:</span><span class="comment">//docs.gradle.org/2.9/userguide/gradle_daemon.html</span></span><br></pre></td></tr></table></figure>
<h5 id="声明属性的其他方式">声明属性的其他方式</h5><p>前面两种方式，大多用来声明自定义变量及值。Gradle也提供了很多其他方式为构建提供属性：</p>
<ul>
<li>项目属性 -P</li>
<li>系统属性 -D</li>
<li>环境属性 按此模式提供 ORG_GRADLE_PROJECT_propertyName=someValue</li>
</ul>
]]></content>
    <summary type="html">
    <![CDATA[<h3 id="构建块">构建块</h3><p>一个gradle构建包含三个基本构建块：project、task、property。每个构建包含至少一个project，进而又包含一个或多个task。</p>
<h4 id="project">project</h4><h4 id=]]>
    </summary>
    
      <category term="gradle" scheme="http://gcblog.cn/tags/gradle/"/>
    
      <category term="gradle" scheme="http://gcblog.cn/categories/gradle/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[gradle学习笔记 - 命令行、守护进程、包装器]]></title>
    <link href="http://gcblog.cn/2015/12/06/gradle-study-2/"/>
    <id>http://gcblog.cn/2015/12/06/gradle-study-2/</id>
    <published>2015-12-06T00:09:34.000Z</published>
    <updated>2015-12-10T14:56:36.000Z</updated>
    <content type="html"><![CDATA[<h3 id="Gradle命令行使用">Gradle命令行使用</h3><p>列出所有可用的tasks</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line">gradle -<span class="tag">q</span> tasks</span><br></pre></td></tr></table></figure>
<p>需要更多task信息，使用–all选项</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line">gradle -<span class="tag">q</span> tasks -all</span><br></pre></td></tr></table></figure>
<p>gradle能够以驼峰式的缩写再命令行上运行任务(任务名字的缩写必须是唯一的)</p>
<figure class="highlight gradle"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">task</span> startSession &lt;&lt; &#123;</span><br><span class="line">    chant()</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> chant() &#123;</span><br><span class="line">    <span class="keyword">ant</span>.echo(message: <span class="string">'Repeat after me...'</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="number">3</span>.<span class="keyword">times</span> &#123;</span><br><span class="line">    <span class="keyword">task</span> <span class="string">"yayGradle$it"</span> &lt;&lt; &#123;</span><br><span class="line">        <span class="keyword">println</span> <span class="string">'Gradle rocks'</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">yayGradle0.dependsOn startSession</span><br><span class="line">yayGradle2.dependsOn yayGradle1, yayGradle0</span><br><span class="line"><span class="keyword">task</span> groupTerapy(dependsOn: yayGradle2)</span><br></pre></td></tr></table></figure>
<figure class="highlight elixir"><table><tr><td class="code"><pre><span class="line">➜  gradle  gradle gT</span><br><span class="line"><span class="symbol">:startSession</span></span><br><span class="line">[<span class="symbol">ant:</span>echo] <span class="constant">Repeat </span>after me...</span><br><span class="line"><span class="symbol">:yayGradle0</span></span><br><span class="line"><span class="constant">Gradle </span>rocks</span><br><span class="line"><span class="symbol">:yayGradle1</span></span><br><span class="line"><span class="constant">Gradle </span>rocks</span><br><span class="line"><span class="symbol">:yayGradle2</span></span><br><span class="line"><span class="constant">Gradle </span>rocks</span><br><span class="line"><span class="symbol">:groupTerapy</span></span><br><span class="line"></span><br><span class="line"><span class="constant">BUILD SUCCESSFUL</span></span><br><span class="line"></span><br><span class="line"><span class="constant">Total </span><span class="symbol">time:</span> <span class="number">1.474</span> secs</span><br><span class="line"></span><br><span class="line"><span class="constant">This </span>build could be faster, please consider using the <span class="constant">Gradle Daemon:</span> <span class="symbol">https:</span>/<span class="regexp">/docs.gradle.org/</span><span class="number">2.9</span>/userguide/gradle_daemon.html</span><br></pre></td></tr></table></figure>
<p>执行时排除某个一个任务(-x)</p>
<figure class="highlight livecodeserver"><table><tr><td class="code"><pre><span class="line">➜  gradle  gradle <span class="variable">gT</span> -x yayGradle0</span><br><span class="line">:yayGradle1</span><br><span class="line">Gradle rocks</span><br><span class="line">:yayGradle2</span><br><span class="line">Gradle rocks</span><br><span class="line">:groupTerapy</span><br><span class="line"></span><br><span class="line">BUILD SUCCESSFUL</span><br><span class="line"></span><br><span class="line">Total <span class="built_in">time</span>: <span class="number">1.28</span> <span class="built_in">secs</span></span><br><span class="line"></span><br><span class="line">This build could be faster, please consider <span class="keyword">using</span> <span class="operator">the</span> Gradle Daemon: <span class="keyword">https</span>://docs.gradle.org/<span class="number">2.9</span>/userguide/gradle_daemon.html</span><br></pre></td></tr></table></figure>
<h3 id="命令行选项">命令行选项</h3><ul>
<li>-?,h,–help 打印出所有可用的命令行选项</li>
<li>-b,–build-file Gradle构建脚本的默认命名月设定是buidle.gradle。使用这个命令行选项可以执行一个特定名字的构建脚本（ex：gradle -b test.gradle）</li>
<li>–offline 通常构建中声明的依赖必须在离线仓库中存在才可用，如果这些依赖在本地缓存中没有，那么运行在一个没有网络的环境中的构建都会失败。使用这个选项可以让你以离线模式运行构建，仅仅在本地缓存中检查依赖是否存在</li>
</ul>
<p>参数选项</p>
<ul>
<li>-D,–system-prop gradle是以一个JVM进程运行的。和所有java进程一样，你可以提供一个系统参数，就像-Dmyprop=myvalue一样</li>
<li>-P,–project-prop 项目参数是构建脚本中可用的变量，你可以使用这个选项直接向构建脚本中传入参数（ex：-Pmyprop=myvalue）</li>
</ul>
<p>日志选项</p>
<ul>
<li>-i,–info 构建发生了什么的详细日志</li>
<li>-s,–statcktrace 在有异常抛出时会打印出简单的堆栈跟踪信息</li>
<li>-q,–quiet 减少构建出错时打印出来的错误日志信息</li>
</ul>
<p>帮助任务</p>
<ul>
<li>tasks 显示项目中所有可以运行的task，包括他们的描述信息</li>
<li>properties 显示项目中所有可用的属性。</li>
</ul>
<h3 id="守护进程">守护进程</h3><ul>
<li>–daemon 运行任务时加上即会开启守护进程</li>
<li>–no-daemon 不运行在守护进程内</li>
<li>–stop 停止守护进程</li>
</ul>
<figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line">➜  gradle  gradle gT --stop</span><br><span class="line">Stopping <span class="function"><span class="title">daemon</span><span class="params">(s)</span></span>.</span><br><span class="line">Gradle daemon stopped.</span><br><span class="line">➜  gradle  gradle gT --daemon</span><br><span class="line">Starting <span class="tag">a</span> new Gradle Daemon <span class="keyword">for</span> this build (subsequent builds will be faster).</span><br><span class="line">:startSession</span><br><span class="line">[ant:echo] Repeat after me...</span><br><span class="line">:yayGradle0</span><br><span class="line">Gradle rocks</span><br><span class="line">:yayGradle1</span><br><span class="line">Gradle rocks</span><br><span class="line">:yayGradle2</span><br><span class="line">Gradle rocks</span><br><span class="line">:groupTerapy</span><br><span class="line"></span><br><span class="line">BUILD SUCCESSFUL</span><br><span class="line"></span><br><span class="line">Total <span class="tag">time</span>: <span class="number">2.152</span> secs</span><br></pre></td></tr></table></figure>
<h3 id="定义仓库">定义仓库</h3><figure class="highlight mathematica"><table><tr><td class="code"><pre><span class="line">respositories<span class="list">&#123;</span><br><span class="line">	mavenCentral()</span><br><span class="line">&#125;</span></span><br></pre></td></tr></table></figure>
<h3 id="定义依赖">定义依赖</h3><figure class="highlight groovy"><table><tr><td class="code"><pre><span class="line">dependencies&#123;</span><br><span class="line">	complie <span class="string">group:</span> <span class="string">'org.apache.commons'</span>, <span class="string">name:</span> <span class="string">'commons-lang3'</span>, <span class="string">version:</span> <span class="string">'3.1'</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="Gradle包装器">Gradle包装器</h3><p>能够让机器在没有安装Gradle运行环境的情况下通过中央仓库下载一个指定的Gradle版本运行构建。（独立于系统，系统配置和Gradle版本的可靠和可重复的构建）</p>
<h4 id="配置包装器">配置包装器</h4><ol>
<li>创建包装器任务</li>
</ol>
<figure class="highlight elm"><table><tr><td class="code"><pre><span class="line"><span class="title">task</span> wrapper(<span class="typedef"><span class="keyword">type</span>: <span class="type">Wrapper</span>) <span class="container">&#123;</span><br><span class="line">    gradleVersion = '1.2'</span><br><span class="line">&#125;</span></span></span><br></pre></td></tr></table></figure>
<ol>
<li>执行任务</li>
</ol>
<figure class="highlight groovy"><table><tr><td class="code"><pre><span class="line">➜  todo-webapp-wrapper <span class="string">git:</span>(master) ✗ gradle wrapper</span><br><span class="line">:wrapper</span><br><span class="line"></span><br><span class="line">BUILD SUCCESSFUL</span><br><span class="line"></span><br><span class="line">Total <span class="string">time:</span> <span class="number">2.043</span> secs</span><br><span class="line"></span><br><span class="line">This build could be faster, please consider using the Gradle <span class="string">Daemon:</span> <span class="string">https:</span><span class="comment">//docs.gradle.org/2.9/userguide/gradle_daemon.html</span></span><br></pre></td></tr></table></figure>
<h4 id="定制包装器">定制包装器</h4><figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">task wrapper(<span class="class"><span class="keyword">type</span>:</span> <span class="type">Wrapper</span>) &#123;</span><br><span class="line">    gradleVersion = <span class="symbol">'2</span><span class="number">.8</span>'</span><br><span class="line">    <span class="comment">//获取gradle包装器url</span></span><br><span class="line">    distributionUrl = <span class="symbol">'https</span>:<span class="comment">//services.gradle.org/distributions/gradle-2.8-bin.zip'</span></span><br><span class="line">    <span class="comment">//包装器解压后存放相对路径</span></span><br><span class="line">    distributionPath = <span class="symbol">'gradle</span>-dists'</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    <summary type="html">
    <![CDATA[<h3 id="Gradle命令行使用">Gradle命令行使用</h3><p>列出所有可用的tasks</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line]]>
    </summary>
    
      <category term="gradle" scheme="http://gcblog.cn/tags/gradle/"/>
    
      <category term="gradle" scheme="http://gcblog.cn/categories/gradle/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[gradle学习笔记 - Task]]></title>
    <link href="http://gcblog.cn/2015/12/02/gradle-study-1/"/>
    <id>http://gcblog.cn/2015/12/02/gradle-study-1/</id>
    <published>2015-12-02T13:23:36.000Z</published>
    <updated>2015-12-02T14:19:39.000Z</updated>
    <content type="html"><![CDATA[<h3 id="构建脚本">构建脚本</h3><p>task：</p>
<figure class="highlight gradle"><table><tr><td class="code"><pre><span class="line"><span class="keyword">task</span> hello &#123;</span><br><span class="line">    <span class="keyword">doLast</span> &#123;</span><br><span class="line">        <span class="keyword">println</span> <span class="string">'Hello world!'</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>闭包写法：</p>
<figure class="highlight gradle"><table><tr><td class="code"><pre><span class="line"><span class="keyword">task</span> hello &lt;&lt; &#123;</span><br><span class="line">    <span class="keyword">println</span> <span class="string">'Hello world!'</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<hr>
<p>构建脚本：</p>
<ol>
<li>使用Groovy语言</li>
</ol>
<figure class="highlight gradle"><table><tr><td class="code"><pre><span class="line"><span class="keyword">task</span> upper &lt;&lt; &#123;</span><br><span class="line">    String someString = <span class="string">'mY_nAmE'</span></span><br><span class="line">    <span class="keyword">println</span> <span class="string">"Original: "</span> + someString </span><br><span class="line">    <span class="keyword">println</span> <span class="string">"Upper case: "</span> + someString.toUpperCase()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight gradle"><table><tr><td class="code"><pre><span class="line"><span class="keyword">task</span> <span class="keyword">count</span> &lt;&lt; &#123;</span><br><span class="line">    <span class="number">4</span>.<span class="keyword">times</span> &#123; <span class="keyword">print</span> <span class="string">"$it "</span> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="任务依赖">任务依赖</h3><figure class="highlight gradle"><table><tr><td class="code"><pre><span class="line"><span class="keyword">task</span> hello &lt;&lt; &#123;</span><br><span class="line">    <span class="keyword">println</span> <span class="string">'Hello world!'</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">task</span> intro(dependsOn: hello) &lt;&lt; &#123;</span><br><span class="line">    <span class="keyword">println</span> <span class="string">"I'm Gradle"</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">➜  gradle  gradle -q intro</span><br><span class="line">Hello world!</span><br><span class="line">I<span class="string">'m Gradle</span></span><br></pre></td></tr></table></figure>
<p>Lazy dependsOn - the other task does not exist (yet)</p>
<figure class="highlight gradle"><table><tr><td class="code"><pre><span class="line"><span class="keyword">task</span> taskX(dependsOn: <span class="string">'taskY'</span>) &lt;&lt; &#123;</span><br><span class="line">    <span class="keyword">println</span> <span class="string">'taskX'</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">task</span> taskY &lt;&lt; &#123;</span><br><span class="line">    <span class="keyword">println</span> <span class="string">'taskY'</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="动态任务">动态任务</h3><figure class="highlight perl"><table><tr><td class="code"><pre><span class="line"><span class="number">4</span>.<span class="keyword">times</span> &#123; counter -&gt;</span><br><span class="line">    task <span class="string">"task<span class="variable">$counter</span>"</span> &lt;&lt; &#123;</span><br><span class="line">        println <span class="string">"I'm task number <span class="variable">$counter</span>"</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">➜  gradle  gradle -<span class="keyword">q</span> task1</span><br><span class="line">I<span class="string">'m task number 1</span><br><span class="line">➜  gradle  gradle -q task2</span><br><span class="line">I'</span><span class="keyword">m</span> task number <span class="number">2</span></span><br><span class="line">➜  gradle  gradle -<span class="keyword">q</span> task3</span><br><span class="line">I<span class="string">'m task number 3</span><br><span class="line">➜  gradle  gradle -q task4</span><br><span class="line"></span><br><span class="line">FAILURE: Build failed with an exception.</span></span><br></pre></td></tr></table></figure>
<h3 id="使用已存在任务">使用已存在任务</h3><figure class="highlight perl"><table><tr><td class="code"><pre><span class="line"><span class="number">4</span>.<span class="keyword">times</span> &#123; counter -&gt;</span><br><span class="line">    task <span class="string">"task<span class="variable">$counter</span>"</span> &lt;&lt; &#123;</span><br><span class="line">        println <span class="string">"I'm task number <span class="variable">$counter</span>"</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">task<span class="number">0</span>.dependsOn task2, task3</span><br><span class="line"></span><br><span class="line">➜  gradle  gradle -<span class="keyword">q</span> task<span class="number">0</span></span><br><span class="line">I<span class="string">'m task number 2</span><br><span class="line">I'</span><span class="keyword">m</span> task number <span class="number">3</span></span><br><span class="line">I<span class="string">'m task number 0</span></span><br></pre></td></tr></table></figure>
<p>Accessing a task via API - adding behaviour</p>
<figure class="highlight gradle"><table><tr><td class="code"><pre><span class="line"><span class="keyword">task</span> hello &lt;&lt; &#123;</span><br><span class="line">    <span class="keyword">println</span> <span class="string">'Hello Earth'</span></span><br><span class="line">&#125;</span><br><span class="line">hello.<span class="keyword">doFirst</span> &#123;</span><br><span class="line">    <span class="keyword">println</span> <span class="string">'Hello Venus'</span></span><br><span class="line">&#125;</span><br><span class="line">hello.<span class="keyword">doLast</span> &#123;</span><br><span class="line">    <span class="keyword">println</span> <span class="string">'Hello Mars'</span></span><br><span class="line">&#125;</span><br><span class="line">hello &lt;&lt; &#123;</span><br><span class="line">    <span class="keyword">println</span> <span class="string">'Hello Jupiter'</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">➜  gradle  gradle -q hello</span><br><span class="line">Hello Venus</span><br><span class="line">Hello Earth</span><br><span class="line">Hello Mars</span><br><span class="line">Hello Jupiter</span><br></pre></td></tr></table></figure>
<p><em>The calls doFirst and doLast can be executed multiple times. They add an action to the beginning or the end of the task’s actions list. When the task executes, the actions in the action list are executed in order. The &lt;&lt; operator is simply an alias for doLast.</em></p>
<h3 id="缩写">缩写</h3><p>使用属性来访问方法</p>
<figure class="highlight gradle"><table><tr><td class="code"><pre><span class="line"><span class="keyword">task</span> hello &lt;&lt; &#123;</span><br><span class="line">    <span class="keyword">println</span> <span class="string">'Hello world!'</span></span><br><span class="line">&#125;</span><br><span class="line">hello.<span class="keyword">doLast</span> &#123;</span><br><span class="line">    <span class="keyword">println</span> <span class="string">"Greetings from the $hello.name task."</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">➜  gradle  gradle -q hello</span><br><span class="line">Hello world!</span><br><span class="line">Greetings <span class="keyword">from</span> the hello <span class="keyword">task</span>.</span><br></pre></td></tr></table></figure>
<h3 id="扩展属性">扩展属性</h3><figure class="highlight gradle"><table><tr><td class="code"><pre><span class="line"><span class="keyword">task</span> myTask &#123;</span><br><span class="line">    ext.myProperty = <span class="string">"myValue"</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">task</span> printTaskProperties &lt;&lt; &#123;</span><br><span class="line">    <span class="keyword">println</span> myTask.myProperty</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">➜  gradle  gradle -q printTaskProperties</span><br><span class="line">myValue</span><br></pre></td></tr></table></figure>
<h3 id="使用Ant任务">使用Ant任务</h3><p>遍历文件夹并打印文件名和属性</p>
<figure class="highlight livecodeserver"><table><tr><td class="code"><pre><span class="line">task loadfile &lt;&lt; &#123;</span><br><span class="line">    def <span class="built_in">files</span> = <span class="built_in">file</span>(<span class="string">'t'</span>).listFiles().<span class="built_in">sort</span>()</span><br><span class="line">    <span class="built_in">files</span>.<span class="keyword">each</span> &#123; File <span class="built_in">file</span> -&gt;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">file</span>.isFile()) &#123;</span><br><span class="line">            ant.loadfile(srcFile: <span class="built_in">file</span>, property: <span class="built_in">file</span>.name)</span><br><span class="line">            println <span class="string">" *** $file.name ***"</span></span><br><span class="line">            println <span class="string">"$&#123;ant.properties[file.name]&#125;"</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">➜  gradle  mkdir t</span><br><span class="line">➜  gradle  touch <span class="keyword">text</span></span><br><span class="line">➜  gradle  vim t/<span class="keyword">text</span></span><br><span class="line">...</span><br><span class="line">➜  gradle  gradle -q loadfile</span><br><span class="line"> *** <span class="keyword">text</span> ***</span><br><span class="line">Using AntBuilder <span class="built_in">to</span> execute ant.loadfile target</span><br></pre></td></tr></table></figure>
<h3 id="使用方法">使用方法</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">task <span class="operator"><span class="keyword">checksum</span> &lt;&lt; &#123;</span><br><span class="line">    fileList(<span class="string">'../antLoadfileResources'</span>).<span class="keyword">each</span> &#123;<span class="keyword">File</span> <span class="keyword">file</span> -&gt;</span><br><span class="line">        ant.<span class="keyword">checksum</span>(<span class="keyword">file</span>: <span class="keyword">file</span>, property: <span class="string">"cs_$file.name"</span>)</span><br><span class="line">        println <span class="string">"$file.name Checksum: $&#123;ant.properties["</span>cs_$<span class="keyword">file</span>.<span class="keyword">name</span><span class="string">"]&#125;"</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">task loadfile &lt;&lt; &#123;</span><br><span class="line">    fileList(<span class="string">'../antLoadfileResources'</span>).<span class="keyword">each</span> &#123;<span class="keyword">File</span> <span class="keyword">file</span> -&gt;</span><br><span class="line">        ant.loadfile(srcFile: <span class="keyword">file</span>, property: <span class="keyword">file</span>.<span class="keyword">name</span>)</span><br><span class="line">        println <span class="string">"I'm fond of $file.name"</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">File</span>[] fileList(<span class="keyword">String</span> dir) &#123;</span><br><span class="line">    <span class="keyword">file</span>(dir).listFiles(&#123;<span class="keyword">file</span> -&gt; <span class="keyword">file</span>.isFile() &#125; <span class="keyword">as</span> FileFilter).<span class="keyword">sort</span>()</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">➜  gradle  gradle -q loadfile</span><br><span class="line"><span class="keyword">I</span><span class="string">'m fond of ab</span><br><span class="line">I'</span><span class="keyword">m</span> fond <span class="keyword">of</span> <span class="built_in">text</span></span><br><span class="line"></span><br><span class="line">➜  gradle  gradle -q <span class="keyword">checksum</span></span><br><span class="line">ab <span class="keyword">Checksum</span>: <span class="number">03862</span>a037618f9566ac4d6cbdb5dd075</span><br><span class="line"><span class="built_in">text</span> <span class="keyword">Checksum</span>: <span class="number">865943</span>c62f297e63f6a381350c9dc0e1</span></span><br></pre></td></tr></table></figure>
<h3 id="默认任务">默认任务</h3><figure class="highlight gradle"><table><tr><td class="code"><pre><span class="line">defaultTasks <span class="string">'clean'</span>, <span class="string">'run'</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">task</span> clean &lt;&lt; &#123;</span><br><span class="line">    <span class="keyword">println</span> <span class="string">'Default Cleaning!'</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">task</span> run &lt;&lt; &#123;</span><br><span class="line">    <span class="keyword">println</span> <span class="string">'Default Running!'</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">task</span> other &lt;&lt; &#123;</span><br><span class="line">    <span class="keyword">println</span> <span class="string">"I'm not a default task!"</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">➜  gradle  gradle -q</span><br><span class="line"><span class="keyword">Default</span> Cleaning!</span><br><span class="line"><span class="keyword">Default</span> Running!</span><br></pre></td></tr></table></figure>
<h3 id="配置DAG">配置DAG</h3><figure class="highlight livecodeserver"><table><tr><td class="code"><pre><span class="line">task distribution &lt;&lt; &#123;</span><br><span class="line">    println <span class="string">"We build the zip with version=$version"</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">task release(dependsOn: <span class="string">'distribution'</span>) &lt;&lt; &#123;</span><br><span class="line">    println <span class="string">'We release now'</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">gradle.taskGraph.whenReady &#123;taskGraph -&gt;</span><br><span class="line">    <span class="keyword">if</span> (taskGraph.hasTask(release)) &#123;</span><br><span class="line">        <span class="built_in">version</span> = <span class="string">'1.0'</span></span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="built_in">version</span> = <span class="string">'1.0-SNAPSHOT'</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">➜  gradle  gradle -q distribution</span><br><span class="line">We build <span class="operator">the</span> zip <span class="operator">with</span> <span class="built_in">version</span>=<span class="number">1.0</span>-SNAPSHOT</span><br><span class="line">➜  gradle  gradle -q release</span><br><span class="line">We build <span class="operator">the</span> zip <span class="operator">with</span> <span class="built_in">version</span>=<span class="number">1.0</span></span><br><span class="line">We release now</span><br></pre></td></tr></table></figure>
]]></content>
    <summary type="html">
    <![CDATA[<h3 id="构建脚本">构建脚本</h3><p>task：</p>
<figure class="highlight gradle"><table><tr><td class="code"><pre><span class="line"><span class="keywor]]>
    </summary>
    
      <category term="gradle" scheme="http://gcblog.cn/tags/gradle/"/>
    
      <category term="gradle" scheme="http://gcblog.cn/categories/gradle/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[gradle配置开发流程]]></title>
    <link href="http://gcblog.cn/2015/12/02/first-gradle/"/>
    <id>http://gcblog.cn/2015/12/02/first-gradle/</id>
    <published>2015-12-02T08:59:50.000Z</published>
    <updated>2015-12-02T09:18:20.000Z</updated>
    <content type="html"><![CDATA[<p>最近开始学习gradle的使用。今天跑起了第一个task。记录一下过程。</p>
<h3 id="Step_1">Step 1</h3><p>下载地址:</p>
<p><a href="https://services.gradle.org/distributions/gradle-2.9-all.zip" target="_blank" rel="external"><strong>Complete distribution</strong></a> (binaries, sources and offline documentation)</p>
<p><a href="https://services.gradle.org/distributions/gradle-2.9-bin.zip" target="_blank" rel="external"><strong>Binary only distribution</strong></a> (no documentation or source code)</p>
<p><a href="https://services.gradle.org/distributions/gradle-2.9-src.zip" target="_blank" rel="external"><strong>Gradle source code</strong></a> (just the Gradle source code; not a usable Gradle installation)</p>
<p><a href="http://gongchaodemacbook-pro.local:62328/Dash/pghvcpxd/userguide/userguide.html" target="_blank" rel="external">文档地址</a></p>
<h3 id="Step_2">Step 2</h3><p>下载完解压缩，配置gralde命令路径</p>
<figure class="highlight xquery"><table><tr><td class="code"><pre><span class="line">vim ~/.bash_profile</span><br><span class="line">export PATH=$&#123;PATH&#125;:~/mac/app/gradle-<span class="number">2.9</span>/bin:<span class="variable">$PATH</span></span><br></pre></td></tr></table></figure>
<h3 id="Step_3">Step 3</h3><p>用gradle init来初始化当前目录生成gradle运行环境</p>
<figure class="highlight stata"><table><tr><td class="code"><pre><span class="line">➜  <span class="keyword">mac</span>  <span class="keyword">mkdir</span> gradle</span><br><span class="line">➜  <span class="keyword">mac</span>  <span class="keyword">cd</span> gradle</span><br><span class="line">➜  gradle  gradle init</span><br><span class="line">:wrapper</span><br><span class="line">:init</span><br><span class="line"></span><br><span class="line">BUILD SUCCESSFUL</span><br><span class="line"></span><br><span class="line"><span class="keyword">Total</span> time: 1.668 secs</span><br><span class="line"></span><br><span class="line">This build could be faster, please consider using the Gradle Daemon: https:<span class="comment">//docs.gradle.org/2.9/userguide/gradle_daemon.html</span></span><br></pre></td></tr></table></figure>
<p>至此gradle环境配置完成</p>
<h3 id="Step_4">Step 4</h3><p>开始编写gradle文件</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="tag">gradle</span>  <span class="tag">vim</span> <span class="tag">build</span><span class="class">.gradle</span></span><br></pre></td></tr></table></figure>
<p>build.gradle:</p>
<figure class="highlight gradle"><table><tr><td class="code"><pre><span class="line">...</span><br><span class="line"><span class="keyword">task</span> hello &#123;</span><br><span class="line">    <span class="keyword">doLast</span> &#123;</span><br><span class="line">        <span class="keyword">println</span> <span class="string">'Hello world!'</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line">➜  gradle  gradle -<span class="tag">q</span> hello</span><br><span class="line">Hello world!</span><br></pre></td></tr></table></figure>
<p><strong>第一个Gradle Task finish！</strong></p>
]]></content>
    <summary type="html">
    <![CDATA[<p>最近开始学习gradle的使用。今天跑起了第一个task。记录一下过程。</p>
<h3 id="Step_1">Step 1</h3><p>下载地址:</p>
<p><a href="https://services.gradle.org/distributions/gr]]>
    </summary>
    
      <category term="gradle" scheme="http://gcblog.cn/tags/gradle/"/>
    
      <category term="gradle" scheme="http://gcblog.cn/categories/gradle/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[Android简洁架构设计]]></title>
    <link href="http://gcblog.cn/2015/11/29/Android-Framwork/"/>
    <id>http://gcblog.cn/2015/11/29/Android-Framwork/</id>
    <published>2015-11-29T13:25:00.000Z</published>
    <updated>2015-11-29T13:48:03.000Z</updated>
    <content type="html"><![CDATA[<blockquote>
<p>英文出处：fernandocejas.com。</p>
</blockquote>
<h3 id="开始">开始</h3><p>我们知道编写高质量软件是既困难又复杂的：不仅是满足需求方面，还要健壮、可维护、可测试，并且足够灵活以适应增长和变化。这就是“代码整洁之道”的来源，并可以成为开发任何软件应用程序的良好方法。思想很简单：代码整洁之道代表构建系统的一组实践：</p>
<ol>
<li>独立于框架</li>
<li>可测试性</li>
<li>独立于UI</li>
<li>独立于数据库</li>
<li>独立于任何外部代理</li>
</ol>
<p><img src="http://jbcdn2.b0.upaiyun.com/2015/07/c169972aba197d0b1e79922aa554bfb7.png" alt=""></p>
<p>只限于使用图示中的4个圆圈并不是必须的，因为这只是语义描述，你还要考虑依赖规则（the Dependency Rule）：源码依赖只应该指向内圈，内圈不应该知道外圈的任何东西。</p>
<p>下面的相关词汇可以帮助熟悉理解这个方法：</p>
<p>实体（Entities）：应用的逻辑对象。<br>用例（Use Cases）：用例编排数据流从实体的流入和流出。也叫交互器（Interactor）。<br>接口适配器（Interface Adapters）：这些适配器将数据转换为用例和实体最合适的格式。表示器（Presenter）和控制器（Controller）就位于这里。<br>框架和驱动器（Frameworks and Drivers）：这是所有细节集中的地方，UI、工具、框架等。<br>为了更好更深入地理解，看下<a href="http://blog.8thlight.com/uncle-bob/2012/08/13/the-clean-architecture.html" target="_blank" rel="external">这篇文章</a></p>
<h3 id="Android架构">Android架构</h3><p>架构的目标是通过将业务规则与外部世界隔离以分离关注点，这样，才可以在不依赖外部元素的情况下测试业务规则。为了达到这个目标，我的建议是将项目拆分为3个不同的层次，每层都有自己的目标，独立地工作。需要提及的是每层都使用自己的数据模型，这样才可以取得依赖（你可以看到，在代码中需要数据映射器（data mapper）来完成数据转换，不想在整个应用之上交叉使用自己的模型总要付出点代价）。下面是一个帮助你理解的模式：</p>
<p><img src="http://jbcdn2.b0.upaiyun.com/2015/07/c1f3d18f5be40e2e949bc7b1416ac5b1.png" alt=""></p>
<p>注意：我没有使用任何外部库（除了解析json数据的gson，还有测试用的junit、 mockito、 robolectri和espresso）。原因是这么做会让例子会更清楚。无论如何，一定不要犹豫使用那些让生活更美好的东西，比如添加ORM来存储磁盘数据，或者任何依赖注入框架，或者任何你熟悉的工具或库。（记住，重新发明轮子不是一个好的实践）。</p>
<h4 id="表示层">表示层</h4><p>这里实现了与视图和动画有关的逻辑。它只使用一个模型-视图-表示器（Model View Presenter， 下文称<a href="http://en.wikipedia.org/wiki/Model%E2%80%93view%E2%80%93presenter" target="_blank" rel="external">MVP</a>），但是你可以使用其他任何模式，如MVC或MVVM。在这里我不会深入介绍，这里的片段和活动只有视图，除了UI逻辑之外没有任何其他逻辑，这也是所有渲染发生的地方。这层中的表示器与交互器（用例）共同在Android UI线程之外开启新线程执行这些工作，并通过回调来处理数据，数据将在视图中渲染。</p>
<p><img src="http://jbcdn2.b0.upaiyun.com/2015/07/f0a7ef97b9d356c4eb8d20b196239b29.png" alt=""><br>如果你想要一个使用MVP和MVVM更酷的<a href="https://github.com/pedrovgs/EffectiveAndroidUI/" target="_blank" rel="external">高效Android UI</a>例子，去看看我朋友Pedro Gómez所做的工作。</p>
<h4 id="领域层">领域层</h4><p>这里的业务规则：所有逻辑都在这层。至于Android项目，你也将会看到所有的交互器（用例）实现。这层是不依赖Android的纯Java模块。所有的外部组件使用接口连接到业务对象。</p>
<p><img src="http://jbcdn2.b0.upaiyun.com/2015/07/1255acd8de2f6705b7963620823fc1d6.png" alt=""></p>
<h4 id="数据层">数据层</h4><p>应用需要的全部数据都来自这层，数据通过一个使用<a href="http://martinfowler.com/eaaCatalog/repository.html" target="_blank" rel="external">仓储模式（Repository Pattern）</a>实现的UserRepository存取，其策略是通过一个工厂，根据特定的条件来选择不同的数据源。比如，当通过id获取用户时，如果用户已存在于缓存中，就会选取磁盘缓存数据源，不然就会查询云端获取数据，之后保存到磁盘缓存。这里的思想是，数据的来源对客户端是透明的，它不关系数据到底来自内存、磁盘还是云端，唯一的事实是数据将会到达，然后被获取。</p>
<p><img src="http://jbcdn2.b0.upaiyun.com/2015/07/c4dbed79c1a3c5a730c86f87eda5637e.png" alt=""></p>
<p>注意：我使用文件系统和Android首选项实现了一个简单的磁盘缓存，仅用于学习目的。再次提醒，如果已经存在某些库可以更好地实现这些功能，你不应该重新发明轮子。</p>
<h3 id="错误处理">错误处理</h3><p>这个话题很容易引发讨论，如果你能分享自己的解决方案会更棒。我的策略是采用回调，因此，一旦数据仓库有事件发生，回调包含2个方法onResponse()和onError()。最后一个将异常封装到称作“ErrorBundle“的包装类中：这种方法会带来很多难处，因为只有一个回调链，错误会一直到达表示层被渲染。代码可读性略显不佳。另外，我也可以实现一个事件总线系统，一旦出错就抛出事件，但是这个方案就像使用GOTO一样；我认为，有时候如果你订阅了多个事件，不仔细控制的话很容易迷失。</p>
<h3 id="测试">测试</h3><p>关于测试，我倾向于根据不同层选择多重方案：</p>
<ul>
<li>表示层：使用Android instrumentation或espresso进行集成和功能测试。</li>
<li>领域层：JUnit和mockito进行单元测试。</li>
<li>数据层：Robolectric（由于这层有Android依赖）和junit、mockito进行集成和单元测试。</li>
</ul>
<h3 id="代码展示">代码展示</h3><p><a href="https://github.com/android10/Android-CleanArchitecture" target="_blank" rel="external">github链接</a></p>
<p>目录结构:</p>
<ul>
<li>presentation：表示层的Android模块。</li>
<li>domain：无Android依赖的java模块。</li>
<li>data：所有数据存放的Android模块。</li>
<li>data-test：数据层的测试。由于使用Robolectric有些限制，我不得不在新的Java模块中使用。</li>
</ul>
<h3 id="结论">结论</h3><p>就像Bob大叔说过的，“架构在于目的而非框架”，我完全赞同。当然，有很多不同的做事方式（不同的实现），我确定你（像我一样）每天都面对很多挑战，但是使用这些技巧，你可以保证应用将会：</p>
<ul>
<li>易于维护。</li>
<li>易于测试。</li>
<li>非常内聚。</li>
<li>解耦。</li>
</ul>
<h3 id="链接和资源">链接和资源</h3><ol>
<li><a href="https://github.com/android10/Android-CleanArchitecture" target="_blank" rel="external">源代码</a></li>
<li><a href="http://blog.8thlight.com/uncle-bob/2012/08/13/the-clean-architecture.html" target="_blank" rel="external">Bob大叔的整洁架构</a></li>
<li><a href="http://www.infoq.com/news/2013/07/architecture_intent_frameworks" target="_blank" rel="external">架构在于目的而非框架</a></li>
<li><a href="http://en.wikipedia.org/wiki/Model%E2%80%93view%E2%80%93presenter" target="_blank" rel="external">模型 视图 表示器</a></li>
<li><a href="http://martinfowler.com/eaaCatalog/repository.html" target="_blank" rel="external">Martin Fowler仓储模式</a></li>
<li><a href="http://www.slideshare.net/PedroVicenteGmezSnch/" target="_blank" rel="external">Android设计模式演示稿</a></li>
</ol>
]]></content>
    <summary type="html">
    <![CDATA[<blockquote>
<p>英文出处：fernandocejas.com。</p>
</blockquote>
<h3 id="开始">开始</h3><p>我们知道编写高质量软件是既困难又复杂的：不仅是满足需求方面，还要健壮、可维护、可测试，并且足够灵活以适应增长和变化。这就]]>
    </summary>
    
      <category term="Android" scheme="http://gcblog.cn/tags/Android/"/>
    
      <category term="Android" scheme="http://gcblog.cn/categories/Android/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[Shell批量修改文件编码]]></title>
    <link href="http://gcblog.cn/2015/11/27/Shell-change-coding/"/>
    <id>http://gcblog.cn/2015/11/27/Shell-change-coding/</id>
    <published>2015-11-27T03:55:13.000Z</published>
    <updated>2015-11-27T06:07:34.000Z</updated>
    <content type="html"><![CDATA[<blockquote>
<p>第一个Shell脚本，很成功，值得记录一下。</p>
</blockquote>
<figure class="highlight stata"><table><tr><td class="code"><pre><span class="line">inputDir=`<span class="keyword">ls</span> ~/<span class="keyword">mac</span>/temp/SDK\ v1.2.0.F源文件/`</span><br><span class="line">outDir=~/<span class="keyword">mac</span>/temp/<span class="keyword">out</span>/</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> <span class="keyword">file</span> <span class="keyword">in</span> <span class="label">$inputDir</span></span><br><span class="line"><span class="keyword">do</span></span><br><span class="line"> new_file=<span class="string">"$outDir$file"</span></span><br><span class="line"> iconv -f GBK -t UTF-8 <span class="label">$&#123;file&#125;</span> &gt; <span class="label">$&#123;new_file&#125;</span> </span><br><span class="line">done</span><br></pre></td></tr></table></figure>
<blockquote>
<p>改进版</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="shebang">#!/bin/bash</span><br><span class="line"></span></span><br><span class="line">DIR=<span class="variable">$1</span> <span class="comment"># 转换编码文件目录</span></span><br><span class="line">FT=<span class="variable">$2</span>  <span class="comment"># 需要转换的文件类型（扩展名）</span></span><br><span class="line">SE=<span class="variable">$3</span>  <span class="comment"># 原始编码</span></span><br><span class="line">DE=<span class="variable">$4</span>  <span class="comment"># 目标编码</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> file <span class="keyword">in</span> `find <span class="variable">$DIR</span> -type f -name <span class="string">"*.<span class="variable">$FT</span>"</span>`; <span class="keyword">do</span></span><br><span class="line">	<span class="built_in">echo</span> <span class="string">"conversion <span class="variable">$file</span> encoding <span class="variable">$SE</span> to <span class="variable">$DE</span>"</span></span><br><span class="line">    iconv <span class="operator">-f</span> <span class="variable">$SE</span> -t <span class="variable">$DE</span> <span class="string">"<span class="variable">$file</span>"</span> &gt; <span class="string">"<span class="variable">$file</span>"</span>.tmp</span><br><span class="line">    mv <span class="operator">-f</span> <span class="string">"<span class="variable">$file</span>"</span>.tmp <span class="string">"<span class="variable">$file</span>"</span></span><br><span class="line"><span class="keyword">done</span></span><br></pre></td></tr></table></figure>]]></content>
    <summary type="html">
    <![CDATA[<blockquote>
<p>第一个Shell脚本，很成功，值得记录一下。</p>
</blockquote>
<figure class="highlight stata"><table><tr><td class="code"><pre><span class="line"]]>
    </summary>
    
      <category term="Shell" scheme="http://gcblog.cn/tags/Shell/"/>
    
      <category term="Shell" scheme="http://gcblog.cn/categories/Shell/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[AndroidAnnotations (十) - Trace]]></title>
    <link href="http://gcblog.cn/2015/11/25/AndroidAnnotations-%E4%B9%9D/"/>
    <id>http://gcblog.cn/2015/11/25/AndroidAnnotations-九/</id>
    <published>2015-11-25T01:52:48.000Z</published>
    <updated>2015-11-25T01:54:02.000Z</updated>
    <content type="html"><![CDATA[<h3 id="Trace">Trace</h3><p>The @Trace annotation allows you to trace the execution of a method by writing log entries.</p>
<p>The method must not be private.</p>
<p>Usage examples:</p>
<figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="variable">@Trace</span></span><br><span class="line">void <span class="function">doWork</span>() &#123;</span><br><span class="line">    <span class="comment">// ... Do Work ...</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="variable">@Trace</span></span><br><span class="line">boolean <span class="function">doMoreWork</span>(String someString) &#123;</span><br><span class="line">    <span class="comment">// ... Do more Work ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>Will generate the following log entries for <code>doWork()</code>:</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line">I/<span class="function"><span class="title">TracedMethodActivity</span><span class="params">(  <span class="number">302</span>)</span></span>: Entering [void <span class="function"><span class="title">doWork</span><span class="params">()</span></span> ]</span><br><span class="line">I/<span class="function"><span class="title">TracedMethodActivity</span><span class="params">(  <span class="number">302</span>)</span></span>: Exiting [void <span class="function"><span class="title">doWork</span><span class="params">()</span></span> ], duration <span class="keyword">in</span> ms: <span class="number">1002</span></span><br></pre></td></tr></table></figure>
<p>And this log entries for <code>doMoreWork()</code>:</p>
<blockquote>
<p>Since AndroidAnnotations 3.1</p>
</blockquote>
<figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line">I/<span class="function"><span class="title">TracedMethodActivity</span><span class="params">(  <span class="number">302</span>)</span></span>: Entering [boolean <span class="function"><span class="title">doMoreWork</span><span class="params">(someString = Hello World)</span></span>]</span><br><span class="line">I/<span class="function"><span class="title">TracedMethodActivity</span><span class="params">(  <span class="number">302</span>)</span></span>: Exiting [boolean <span class="function"><span class="title">doMoreWork</span><span class="params">(String)</span></span> returning: true], duration <span class="keyword">in</span> ms: <span class="number">651</span></span><br></pre></td></tr></table></figure>
<p>Customization:</p>
<figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@Trace</span>(tag=<span class="string">"CustomTag"</span>, level=Log.WARN)</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">doWork</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">// ... Do Work ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
    <summary type="html">
    <![CDATA[<h3 id="Trace">Trace</h3><p>The @Trace annotation allows you to trace the execution of a method by writing log entries.</p>
<p>The method mu]]>
    </summary>
    
      <category term="Android" scheme="http://gcblog.cn/tags/Android/"/>
    
      <category term="AndroidAnnotations" scheme="http://gcblog.cn/categories/AndroidAnnotations/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[AndroidAnnotations (九) - Event Handling]]></title>
    <link href="http://gcblog.cn/2015/11/25/AndroidAnnotations-%E5%8D%81/"/>
    <id>http://gcblog.cn/2015/11/25/AndroidAnnotations-十/</id>
    <published>2015-11-25T01:26:39.000Z</published>
    <updated>2015-11-25T01:54:42.000Z</updated>
    <content type="html"><![CDATA[<h3 id="ClickEvents">ClickEvents</h3><h4 id="@Click">@Click</h4><figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="variable">@Click</span>(R.id.myButton)</span><br><span class="line">void <span class="function">myButtonWasClicked</span>() &#123;</span><br><span class="line">    <span class="attr_selector">[...]</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="variable">@Click</span></span><br><span class="line">void <span class="function">anotherButton</span>() &#123;</span><br><span class="line">    <span class="attr_selector">[...]</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="variable">@Click</span></span><br><span class="line">void <span class="function">yetAnotherButton</span>(View clickedView) &#123;</span><br><span class="line">    <span class="attr_selector">[...]</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="variable">@Click</span>(&#123;R.id.myButton, R.id.myOtherButton&#125;)</span><br><span class="line">void <span class="function">handlesTwoButtons</span>() &#123;</span><br><span class="line">    <span class="attr_selector">[...]</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="Other_events">Other events</h3><p>Currently, AndroidAnnotations supports the following <a href="http://developer.android.com/guide/topics/ui/ui-events.html#EventListeners" target="_blank" rel="external">events</a> on views:</p>
<p><a href="http://developer.android.com/reference/android/view/View.OnClickListener.html" target="_blank" rel="external">Clicks</a> with @Click<br><a href="http://developer.android.com/reference/android/view/View.OnLongClickListener.html" target="_blank" rel="external">Long clicks</a> with @LongClick<br><a href="http://developer.android.com/reference/android/view/View.OnTouchListener.html" target="_blank" rel="external">Touches</a> with @Touch</p>
<h3 id="AdapterViewEvents">AdapterViewEvents</h3><p>You can bind methods to handle events on items in an AdapterView:</p>
<ul>
<li><a href="http://developer.android.com/reference/android/widget/AdapterView.OnItemClickListener.html" target="_blank" rel="external">Item clicks</a> with @ItemClick</li>
<li><a href="http://developer.android.com/reference/android/widget/AdapterView.OnItemLongClickListener.html" target="_blank" rel="external">Long item clicks</a> with @ItemLongClick</li>
<li><a href="http://developer.android.com/reference/android/widget/AdapterView.OnItemSelectedListener.html" target="_blank" rel="external">Item selection</a> with @ItemSelect</li>
</ul>
<p>The annotation value should be one or several of R.id.<em> fields. If not set, the method name will be used as the R.id.</em> field name.</p>
<p>Methods annotated with @ItemClick or @ItemLongClick must have one parameter. This parameter can be of any type, it’s the object retrieved when calling adapter.getItem(position).</p>
<p>Methods annotated with @ItemSelect may have one or two parameters. The first parameter must be a boolean, and the second is the object from the adapter, at the selected position.</p>
<figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="variable">@EActivity</span>(R.layout.my_list)</span><br><span class="line">public class MyListActivity extends Activity &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"></span><br><span class="line">    <span class="variable">@ItemClick</span></span><br><span class="line">    public void <span class="function">myListItemClicked</span>(MyItem clickedItem) &#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="variable">@ItemLongClick</span></span><br><span class="line">    public void <span class="function">myListItemLongClicked</span>(MyItem clickedItem) &#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="variable">@ItemSelect</span></span><br><span class="line">    public void <span class="function">myListItemSelected</span>(boolean selected, MyItem selectedItem) &#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>Since AndroidAnnotations 2.4</p>
<p>For <code>@ItemClick</code>, <code>@ItemLongClick</code> and <code>@ItemSelect</code>, if the parameter is of type int, then the position is given instead of the object coming from the adapter.</p>
</blockquote>
<figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="variable">@EActivity</span>(R.layout.my_list)</span><br><span class="line">public class MyListActivity extends Activity &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"></span><br><span class="line">    <span class="variable">@ItemClick</span></span><br><span class="line">    public void <span class="function">myListItemClicked</span>(int position) &#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="variable">@ItemLongClick</span></span><br><span class="line">    public void <span class="function">myListItemLongClicked</span>(int position) &#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="variable">@ItemSelect</span></span><br><span class="line">    public void <span class="function">myListItemSelected</span>(boolean selected, int position) &#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="TextChangeEvents">TextChangeEvents</h3><h4 id="@TextChange">@TextChange</h4><p>This annotation is intended to be used on methods to receive events defined by <code>android.text.TextWatcher.onTextChanged(CharSequence s, int start, int before, int count)</code>when the text is changed on the targeted TextView or subclass of TextView. The annotation value should be one or several R.id.<em> fields that refers to TextView or subclasses of TextView. If not set, the <a href="https://github.com/excilys/androidannotations/wiki/InferringIDFromMethodName" target="_blank" rel="external">method name</a> will be used as the R.id.</em> field name. The method may have multiple parameter:</p>
<ul>
<li>A <code>android.widget.TextView</code> parameter to know which view has received this event</li>
<li>A <code>java.lang.CharSequence</code> parameter to get the modified text.</li>
<li>An int parameter named start to get the start position of the modified text.</li>
<li>An int parameter named before to know the text length before modification.</li>
<li>An int parameter named count to know the number of modified characters.</li>
</ul>
<p>Some usage examples of <code>@BeforeTextChange</code> annotation:</p>
<figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="variable">@TextChange</span>(R.id.helloTextView)</span><br><span class="line">void <span class="function">onTextChangesOnHelloTextView</span>(CharSequence text, TextView hello, int before, int start, int count) &#123;</span><br><span class="line">   <span class="comment">// Something Here</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="variable">@TextChange</span></span><br><span class="line">void <span class="function">helloTextViewTextChanged</span>(TextView hello) &#123;</span><br><span class="line">   <span class="comment">// Something Here</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="variable">@TextChange</span>(&#123;R.id.editText, R.id.helloTextView&#125;)</span><br><span class="line">void <span class="function">onTextChangesOnSomeTextViews</span>(TextView tv, CharSequence text) &#123;</span><br><span class="line">   <span class="comment">// Something Here</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="variable">@TextChange</span>(R.id.helloTextView)</span><br><span class="line">void <span class="function">onTextChangesOnHelloTextView</span>() &#123;</span><br><span class="line">   <span class="comment">// Something Here</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="@BeforeTextChange">@BeforeTextChange</h4><p>This annotation is intended to be used on methods to receive events defined by android.text.TextWatcher.beforeTextChanged(CharSequence s, int start, int count, int after) before the text is changed on the targeted TextView or subclass of TextView. The annotation value should be one or several R.id.<em> fields that refers to TextView or subclasses of TextView. If not set, the <a href="https://github.com/excilys/androidannotations/wiki/InferringIDFromMethodName" target="_blank" rel="external">method name</a> will be used as the R.id.</em> field name. The method may have multiple parameters:</p>
<ul>
<li>A <code>android.widget.TextView</code> parameter to know which view has targeted this event</li>
<li>An <code>java.lang.CharSequence</code> parameter to get the text before modification.</li>
<li>An int parameter named start to get the start position of the modified text.</li>
<li>An int parameter named count to know the number of modified characters.</li>
<li>An int parameter named after to know the text length after the text modification.</li>
</ul>
<p>Some usage examples of <code>@BeforeTextChange</code> annotation:</p>
<figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="variable">@BeforeTextChange</span>(R.id.helloTextView)</span><br><span class="line">void <span class="function">beforeTextChangedOnHelloTextView</span>(TextView hello, CharSequence text, int start, int count, int after) &#123;</span><br><span class="line"><span class="comment">// Something Here</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="variable">@BeforeTextChange</span></span><br><span class="line">void <span class="function">helloTextViewBeforeTextChanged</span>(TextView hello) &#123;</span><br><span class="line"><span class="comment">// Something Here</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="variable">@BeforeTextChange</span>(&#123;R.id.editText, R.id.helloTextView&#125;)</span><br><span class="line">void <span class="function">beforeTextChangedOnSomeTextViews</span>(TextView tv, CharSequence text) &#123;</span><br><span class="line"><span class="comment">// Something Here</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="variable">@BeforeTextChange</span>(R.id.helloTextView)</span><br><span class="line">void <span class="function">beforeTextChangedOnHelloTextView</span>() &#123;</span><br><span class="line"><span class="comment">// Something Here</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="@AfterTextChange">@AfterTextChange</h4><p>This annotation is intended to be used on methods to receive events defined by <code>android.text.TextWatcher.afterTextChanged(Editable s)</code> after the text is changed on the targeted TextView or subclass of TextView. The annotation value should be one or several <code>R.id.*</code> fields that refers to TextView or subclasses of TextView. If not set, the <a href="https://github.com/excilys/androidannotations/wiki/InferringIDFromMethodName" target="_blank" rel="external">method name</a> will be used as the <code>R.id.*</code> field name. The method may have multiple parameter:</p>
<ul>
<li>A <code>android.widget.TextView</code> parameter to know which view has targeted this event</li>
<li><code>An android.text.Editable</code> to make changes on modified text.</li>
</ul>
<p>Some usage examples of <code>@BeforeTextChange</code> annotation :</p>
<figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="variable">@AfterTextChange</span>(R.id.helloTextView)</span><br><span class="line">void <span class="function">afterTextChangedOnHelloTextView</span>(Editable text, TextView hello) &#123;</span><br><span class="line"><span class="comment">// Something Here</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="variable">@AfterTextChange</span></span><br><span class="line">void <span class="function">helloTextViewAfterTextChanged</span>(TextView hello) &#123;</span><br><span class="line"><span class="comment">// Something Here</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="variable">@AfterTextChange</span>(&#123;R.id.editText, R.id.helloTextView&#125;)</span><br><span class="line">void <span class="function">afterTextChangedOnSomeTextViews</span>(TextView tv, Editable text) &#123;</span><br><span class="line"><span class="comment">// Something Here</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="variable">@AfterTextChange</span>(R.id.helloTextView)</span><br><span class="line">void <span class="function">afterTextChangedOnHelloTextView</span>() &#123;</span><br><span class="line"><span class="comment">// Something Here</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="@EditorAction">@EditorAction</h4><p>This annotation is intended to be used on methods to receive events defined by <code>android.widget.TextView.OnEditorActionListener#onEditorAction(android.widget.TextView, int, android.view.KeyEvent)</code> when an action is performed on the editor.</p>
<p>The annotation value should be one or several R.id.<em> fields that refers to TextView or subclasses of <code>TextView</code>. If not set, the <a href="https://github.com/excilys/androidannotations/wiki/InferringIDFromMethodName" target="_blank" rel="external">method name</a> will be used as the R.id.</em> field name.</p>
<p>The method may have multiple parameters:</p>
<ul>
<li>A <code>android.widget.TextView</code> parameter to know which view has targeted this event.</li>
<li>An <code>int</code> parameter to get the <code>actionId</code>.</li>
<li>A <code>android.view.KeyEvent</code> parameter.</li>
</ul>
<p>The return type of the method can be either <code>void</code> or <code>boolean</code>. In case of <code>boolean</code>, the value returned from the annotated method will be returned in the generated listener method (indicating event consumption). If the annotated method is <code>void</code>, always <code>true</code> will be returned in the listener method (so the event is consumed).</p>
<p>Some usage examples of <code>@EditorAction</code> annotation:</p>
<figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@EditorAction</span>(R.id.helloTextView)</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">onEditorActionsOnHelloTextView</span><span class="params">(TextView hello, <span class="keyword">int</span> actionId, KeyEvent keyEvent)</span> </span>&#123;</span><br><span class="line"><span class="comment">// Something Here</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="annotation">@EditorAction</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">helloTextViewEditorAction</span><span class="params">(TextView hello)</span> </span>&#123;</span><br><span class="line"><span class="comment">// Something Here</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="annotation">@EditorAction</span>(&#123;R.id.editText, R.id.helloTextView&#125;)</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">onEditorActionsOnSomeTextViews</span><span class="params">(TextView tv, <span class="keyword">int</span> actionId)</span> </span>&#123;</span><br><span class="line"><span class="comment">// Something Here</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="annotation">@EditorAction</span>(R.id.helloTextView)</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">onEditorActionsOnHelloTextView</span><span class="params">()</span> </span>&#123;</span><br><span class="line"><span class="comment">// Something Here</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="annotation">@EditorAction</span>(R.id.helloTextView)</span><br><span class="line"><span class="function"><span class="keyword">boolean</span> <span class="title">onEditorActionsOnHelloTextView</span><span class="params">()</span> </span>&#123;</span><br><span class="line"><span class="comment">// Something Here</span></span><br><span class="line"><span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="Handling_options_menu"><a href="https://github.com/excilys/androidannotations/wiki/Handling%20Options%20Menu" target="_blank" rel="external">Handling options menu</a></h3><h3 id="KeyEvents"><a href="https://github.com/excilys/androidannotations/wiki/KeyEvents" target="_blank" rel="external">KeyEvents</a></h3><h3 id="SeekBarEvents"><a href="https://github.com/excilys/androidannotations/wiki/SeekBarEvents" target="_blank" rel="external">SeekBarEvents</a></h3>]]></content>
    <summary type="html">
    <![CDATA[<h3 id="ClickEvents">ClickEvents</h3><h4 id="@Click">@Click</h4><figure class="highlight less"><table><tr><td class="code"><pre><span class=]]>
    </summary>
    
      <category term="Android" scheme="http://gcblog.cn/tags/Android/"/>
    
      <category term="AndroidAnnotations" scheme="http://gcblog.cn/categories/AndroidAnnotations/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[AndroidAnnotations (八) - SharedPreferences]]></title>
    <link href="http://gcblog.cn/2015/11/25/AndroidAnnotations-%E5%85%AB/"/>
    <id>http://gcblog.cn/2015/11/25/AndroidAnnotations-八/</id>
    <published>2015-11-25T00:05:16.000Z</published>
    <updated>2015-11-25T00:11:04.000Z</updated>
    <content type="html"><![CDATA[<h3 id="Defining_the_preferences">Defining the preferences</h3><p>First, you should create an interface annotated with <code>@SharedPref</code> to define the SharedPreferences :</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@SharedPref</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">MyPrefs</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// The field name will have default value "John"</span></span><br><span class="line">    <span class="annotation">@DefaultString</span>(<span class="string">"John"</span>)</span><br><span class="line">    <span class="function">String <span class="title">name</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// The field age will have default value 42</span></span><br><span class="line">    <span class="annotation">@DefaultInt</span>(<span class="number">42</span>)</span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">age</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// The field lastUpdated will have default value 0</span></span><br><span class="line">    <span class="function"><span class="keyword">long</span> <span class="title">lastUpdated</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>Based on that specification, AndroidAnnotations builds a SharedPreferences Helper that has the same name plus an underscore. You can get an instance of the generated helper in any enhanced class with the <code>@Pref</code> annotation.</p>
<p><strong>Important</strong>: The type of the field <strong>MUST</strong> be the generated class instead of the source class. It’s the only exception in AA.</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@EActivity</span></span><br><span class="line">public <span class="class"><span class="keyword">class</span> <span class="title">MyActivity</span> <span class="keyword"><span class="keyword">extends</span></span> <span class="title">Activity</span> &#123;</span></span><br><span class="line"></span><br><span class="line">    <span class="annotation">@Pref</span></span><br><span class="line">    <span class="type">MyPrefs_</span> myPrefs;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>You can then start using it:</p>
<figure class="highlight processing"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Simple edit</span></span><br><span class="line">myPrefs.name().put(<span class="string">"John"</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Batch edit</span></span><br><span class="line">myPrefs.edit()</span><br><span class="line">  .name()</span><br><span class="line">  .put(<span class="string">"John"</span>)</span><br><span class="line">  .age()</span><br><span class="line">  .put(<span class="number">42</span>)</span><br><span class="line">  .apply();</span><br><span class="line"></span><br><span class="line"><span class="comment">// Preference clearing:</span></span><br><span class="line">myPrefs.<span class="built_in">clear</span>();</span><br><span class="line"></span><br><span class="line"><span class="comment">// Check if a value exists:</span></span><br><span class="line"><span class="built_in">boolean</span> nameExists = myPrefs.name().exists();</span><br><span class="line"></span><br><span class="line"><span class="comment">// Reading a value</span></span><br><span class="line"><span class="keyword">long</span> lastUpdated = myPrefs.lastUpdated().<span class="built_in">get</span>();</span><br><span class="line"></span><br><span class="line"><span class="comment">// Reading a value and providing a fallback default value</span></span><br><span class="line"><span class="keyword">long</span> now = System.currentTimeMillis();</span><br><span class="line"><span class="keyword">long</span> lastUpdated = myPrefs.lastUpdated().getOr(now);</span><br></pre></td></tr></table></figure>
<h3 id="Default_resource_value">Default resource value</h3><blockquote>
<p>Since AndroidAnnotations 3.0</p>
</blockquote>
<p>It’s now possible to inject a default value from Android resources with <code>@DefaultRes</code>:</p>
<figure class="highlight autoit"><table><tr><td class="code"><pre><span class="line"><span class="constant">@SharedPref</span></span><br><span class="line">public interface MyPrefs &#123;</span><br><span class="line">    <span class="constant">@DefaultRes</span>(R.<span class="built_in">string</span>.defaultPrefName)</span><br><span class="line">    <span class="built_in">String</span> resourceName()<span class="comment">;</span></span><br><span class="line"></span><br><span class="line">    <span class="constant">@DefaultRes</span> // uses <span class="string">'R.string.defaultPrefAge'</span> <span class="keyword">to</span> set <span class="keyword">default</span> value</span><br><span class="line">    <span class="built_in">String</span> defaultPrefAge()<span class="comment">;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="String_resource_as_preference_key">String resource as preference key</h3><blockquote>
<p>Since AndroidAnnotations 3.1</p>
</blockquote>
<p>You can specify a String resource by its id to be used as the preference key instead of the method name. This is useful when you specify your preferences in an xml file, and you use String resource keys there. Example:</p>
<figure class="highlight cs"><table><tr><td class="code"><pre><span class="line">@SharedPref</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title">MyPrefs</span> &#123;</span><br><span class="line">    @DefaultString(<span class="keyword">value</span> = <span class="string">"John"</span>, keyRes = R.<span class="keyword">string</span>.myPrefKey)</span><br><span class="line">    <span class="function">String <span class="title">name</span>(<span class="params"></span>)</span>;</span><br><span class="line"></span><br><span class="line">    @DefaultRes(keyRes = R.<span class="keyword">string</span>.myOtherPrefKey)</span><br><span class="line">    <span class="function">String <span class="title">defaultPrefAge</span>(<span class="params"></span>)</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="Scope">Scope</h3><p>Observe that you can name the shared preference by setting <code>value</code> to one of the following:</p>
<ul>
<li><code>ACTIVITY</code>, for a shared preference named <code>MyActivity_MyPrefs</code>;</li>
<li><code>ACTIVITY_DEFAULT</code>, for a shared preference named <code>MyActivity</code> (also available through <code>activity.getPreferences()</code>);</li>
<li><code>APPLICATION_DEFAULT</code>, for the default <code>SharedPreference</code> or <code>UNIQUE</code>, for a shared preference named <code>MyPrefs</code>.<br>Therefore, if a single shared preference is needed for the interface defined, in order to all activities of a given application to share the same preferences, the following should be used:</li>
</ul>
<figure class="highlight cs"><table><tr><td class="code"><pre><span class="line">@SharedPref(<span class="keyword">value</span>=SharedPref.Scope.UNIQUE)</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title">MyPrefs</span> &#123;</span><br><span class="line">...</span><br></pre></td></tr></table></figure>
<h3 id="Using_it_with_a_PreferenceActivity">Using it with a <code>PreferenceActivity</code></h3><p>The Android <code>PreferenceActivity</code> or <code>PreferenceFragment</code> can edit the values of your shared preferences.</p>
<figure class="highlight groovy"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@SharedPref</span>(SharedPref.Scope.UNIQUE)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">MyPrefs</span> &#123;</span></span><br><span class="line">...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight processing"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">String</span> PREF_NAME = <span class="string">"MyPrefs"</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// in onCreate</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Using your MyPrefs values </span></span><br><span class="line"><span class="keyword">this</span>.getPreferenceManager().setSharedPreferencesName(PREF_NAME);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Opening the layout </span></span><br><span class="line">addPreferencesFromResource(R.xml.prefs);</span><br></pre></td></tr></table></figure>
]]></content>
    <summary type="html">
    <![CDATA[<h3 id="Defining_the_preferences">Defining the preferences</h3><p>First, you should create an interface annotated with <code>@SharedPref</co]]>
    </summary>
    
      <category term="Android" scheme="http://gcblog.cn/tags/Android/"/>
    
      <category term="AndroidAnnotations" scheme="http://gcblog.cn/categories/AndroidAnnotations/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[AndroidAnnotations (七) - Thread]]></title>
    <link href="http://gcblog.cn/2015/11/25/AndroidAnnotations-%E4%B8%83/"/>
    <id>http://gcblog.cn/2015/11/25/AndroidAnnotations-七/</id>
    <published>2015-11-24T23:39:50.000Z</published>
    <updated>2015-11-25T00:03:11.000Z</updated>
    <content type="html"><![CDATA[<h3 id="WorkingWithThreads">WorkingWithThreads</h3><h4 id="@Background">@Background</h4><p>The <code>@Background</code> annotation indicates that a method will run in a thread other than the <a href="http://developer.android.com/guide/topics/fundamentals.html#threads" target="_blank" rel="external">ui thread</a>.</p>
<p>Usage example:</p>
<figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">myMethod</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    someBackgroundWork(<span class="string">"hello"</span>, <span class="number">42</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="annotation">@Background</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">someBackgroundWork</span><span class="params">(String aParam, <span class="keyword">long</span> anotherParam)</span> </span>&#123;</span><br><span class="line">    [...]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>The method is executed on a separate thread, but this doesn’t necessarily mean that a new thread will be started, because we use a shared cached thread pool executor (which can be replaced) to prevent creating too much threads.</p>
<p>This also means that two <code>@Background</code> methods may run in parallel</p>
</blockquote>
<h5 id="Id">Id</h5><blockquote>
<p>Since AndroidAnnotations 3.0</p>
</blockquote>
<p>If you want to cancel a background task, you can use the <code>id</code> field. Every task could then be cancelled by <code>BackgroundExecutor.cancelAll(&quot;id&quot;)</code>;:</p>
<figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">myMethod</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    someCancellableBackground(<span class="string">"hello"</span>, <span class="number">42</span>);</span><br><span class="line">    [...]</span><br><span class="line">    <span class="keyword">boolean</span> mayInterruptIfRunning = <span class="keyword">true</span>;</span><br><span class="line">    BackgroundExecutor.cancelAll(<span class="string">"cancellable_task"</span>, mayInterruptIfRunning);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="annotation">@Background</span>(id=<span class="string">"cancellable_task"</span>)</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">someCancellableBackground</span><span class="params">(String aParam, <span class="keyword">long</span> anotherParam)</span> </span>&#123;</span><br><span class="line">    [...]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5 id="Serial(串行)">Serial(串行)</h5><blockquote>
<p>Since AndroidAnnotations 3.0</p>
</blockquote>
<p>By default, <code>@Background</code> method are processed in parallel(并行). If you want them to be executed sequentially, you can use the <code>serial</code> field. All background tasks with the same <code>serial</code> will be executed sequentially:</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">myMethod</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++)</span><br><span class="line">        someSequentialBackgroundMethod(i);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">@Background(serial = <span class="string">"test"</span>)</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">someSequentialBackgroundMethod</span><span class="params">(<span class="keyword">int</span> i)</span> </span>&#123;</span><br><span class="line">    SystemClock.sleep(<span class="keyword">new</span> Random().nextInt(<span class="number">2000</span>)+<span class="number">1000</span>);</span><br><span class="line">    Log.d(<span class="string">"AA"</span>, <span class="string">"value : "</span> + i);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5 id="Delay">Delay</h5><blockquote>
<p>Since AndroidAnnotations 3.0</p>
</blockquote>
<p>If you need to add a delay before a background method is run, you can use the <code>delay</code> parameter:</p>
<figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@Background</span>(delay=<span class="number">2000</span>)</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">doInBackgroundAfterTwoSeconds</span><span class="params">()</span> </span>&#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5 id="@UiThread">@UiThread</h5><p>The <code>@UiThread</code> annotation indicates that a method will run in the <a href="http://developer.android.com/guide/topics/fundamentals.html#threads" target="_blank" rel="external">ui thread</a>.</p>
<p>Usage example:</p>
<figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">myMethod</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    doInUiThread(<span class="string">"hello"</span>, <span class="number">42</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="annotation">@UiThread</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">doInUiThread</span><span class="params">(String aParam, <span class="keyword">long</span> anotherParam)</span> </span>&#123;</span><br><span class="line">    [...]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>No more <code>AsyncTask&lt;Param, Progress, Result&gt;</code>!!</p>
<h5 id="Propagation">Propagation</h5><p>Prior to 3.0, <code>@Thread</code> annotated method calls was always added in the handler execution queue to ensure that execution was done in Ui thread. Since 3.0, we kept the same behavior for compatibility purpose.</p>
<p>But, if you want to optimize UiThread calls, you may want to change <code>propagation</code> value to <code>REUSE</code>. In this configuration, the code will make a direct call to the method if current thread is already Ui thread. If not, we’re falling back to handler call.</p>
<p>If you need to add a delay before a method is run on the UI Thread, you can use the <code>delay</code> parameter:</p>
<figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@UiThread</span>(propagation = Propagation.REUSE)</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">runInSameThreadIfOnUiThread</span><span class="params">()</span> </span>&#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><strong>Note</strong>: If combined with delay, propagation will be ignored and the call will always be posted in handler execution queue.</p>
<h4 id="@SupposeBackground">@SupposeBackground</h4><blockquote>
<p>Since AndroidAnnotations 3.1</p>
</blockquote>
<p>Annotate your methods to ensure they are called from a background thread with (optionally) restrictions by allowed serials. If it is not called from a supposed background thread, then <code>IllegalStateException</code> will be thrown (by default). The allowed IDs can be passed to the <code>serial</code> parameter, if nothing is passed, any <code>ID</code> is allowed.<br>If you want to override what happens when the method is not called on the supposed thread, pass an instance of <code>BackgroundExecutor.WrongThreadListener</code>to <code>BackgroundExecutor.setWrongThreadListener()</code>. Its onBgExpected method will be invoked if the annotated method is called on the wrong thread, or its <code>onWrongBgSerial</code> will be invoked if the annotated method called on a background thread but with wrong IDs.</p>
<p>Usage example:</p>
<figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="variable">@EBean</span></span><br><span class="line">public class MyBean &#123;</span><br><span class="line"></span><br><span class="line">  <span class="variable">@SupposeBackground</span></span><br><span class="line">  void <span class="function">someMethodThatShouldNotBeCalledFromUiThread</span>() &#123;</span><br><span class="line">  	<span class="comment">//if this method will be called from the UI-thread an exception will be thrown</span></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="variable">@SupposeBackground</span>(serial = &#123;<span class="string">"serial1"</span>, <span class="string">"serial2"</span>&#125;)</span><br><span class="line">  void <span class="function">someMethodThatShouldBeCalledFromSerial1OrSerial2</span>() &#123;</span><br><span class="line">  	<span class="comment">//if this method will be called from another thread then a background thread with a</span></span><br><span class="line"> 	<span class="comment">//serial "serial1" or "serial2", an exception will be thrown</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="@SupposeUiThread">@SupposeUiThread</h4><blockquote>
<p>Since AndroidAnnotations 3.1</p>
</blockquote>
<p>Annotate your methods to ensure they are called from the UI thread. If they are not, then <code>IllegalStateException</code> will be thrown (by default).<br>If you want to override what happens when the method is not called on the UI thread, pass an instance of <code>BackgroundExecutor.WrongThreadListener</code>to <code>BackgroundExecutor.setWrongThreadListener()</code>. Its <code>onUiExpected</code> method will be invoked if the annotated method is called on the wrong thread.</p>
<p>Usage example:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@EBean</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyBean</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">  <span class="annotation">@SupposeUiThread</span></span><br><span class="line">  <span class="function"><span class="keyword">void</span> <span class="title">someMethodThatShouldBeCalledOnlyFromUiThread</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="comment">//if this method will be called from a background thread an exception will be thrown</span></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="BackgroundTasksAndActivityBinding">BackgroundTasksAndActivityBinding</h3><blockquote>
<p>Since AndroidAnnotations 2.5</p>
</blockquote>
<p>Like an AsyncTask, <code>@Background</code> does not handle any lifecycle changes on your activities.</p>
<p>For instance, if you call an <code>@Background</code> method and then the screen is rotated, then the <code>@Background</code> code will be executed, no matter what, on the instance it was called on.</p>
<p>If <code>@Background</code> is placed on a method that belongs to the activity, and in turns call a <code>@UiThread</code> method, there is a risk that the <code>@UiThread</code> method will be called on the wrong activity instance, if a configuration change occurred.</p>
<p>In Android, prior to Loaders, the usual way to handle that was to use an <code>AsyncTask</code>, keep references to those tasks in <code>onRetainNonConfigurationInstance()</code>, and rebind them to the new activity after a config change.</p>
<p>AndroidAnnotations provides <code>@NonConfigurationInstance</code>, which can be combined with <code>@EBean</code>, <code>@Bean</code> and <code>@Background</code> to achieve the same effect.</p>
<p>Here is an example, first of all the activity:</p>
<figure class="highlight nimrod"><table><tr><td class="code"><pre><span class="line">@<span class="type">EActivity</span></span><br><span class="line">public class <span class="type">MyActivity</span> extends <span class="type">Activity</span> &#123;</span><br><span class="line"></span><br><span class="line">  /* <span class="type">Using</span> @<span class="type">NonConfigurationInstance</span> on a @<span class="type">Bean</span> will automatically </span><br><span class="line">   * update the context <span class="keyword">ref</span> on configuration changes,</span><br><span class="line">   * <span class="keyword">if</span> the bean <span class="keyword">is</span> <span class="keyword">not</span> a singleton</span><br><span class="line">   */</span><br><span class="line">  @<span class="type">NonConfigurationInstance</span></span><br><span class="line">  @<span class="type">Bean</span></span><br><span class="line">  <span class="type">MyBackgroundTask</span> task;</span><br><span class="line"></span><br><span class="line">  @<span class="type">Click</span></span><br><span class="line">  <span class="type">void</span> myButtonClicked() &#123;</span><br><span class="line">    task.doSomethingInBackground();</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="type">void</span> showResult(<span class="type">MyResult</span> <span class="literal">result</span>) &#123;</span><br><span class="line">    // <span class="keyword">do</span> something <span class="keyword">with</span> <span class="literal">result</span></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>Then the task itself:</p>
<figure class="highlight nimrod"><table><tr><td class="code"><pre><span class="line">@<span class="type">EBean</span></span><br><span class="line">public class <span class="type">MyBackgroundTask</span> &#123;</span><br><span class="line"></span><br><span class="line">  @<span class="type">RootContext</span></span><br><span class="line">  <span class="type">MyActivity</span> activity;</span><br><span class="line"></span><br><span class="line">  @<span class="type">Background</span></span><br><span class="line">  <span class="type">void</span> doSomethingInBackground() &#123;</span><br><span class="line">    // <span class="keyword">do</span> something</span><br><span class="line">    <span class="type">MyResult</span> <span class="literal">result</span> = <span class="type">XXX</span>;</span><br><span class="line"></span><br><span class="line">    updateUI(<span class="literal">result</span>);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  // <span class="type">Notice</span> that we manipulate the activity <span class="keyword">ref</span> only <span class="keyword">from</span> the <span class="type">UI</span> thread</span><br><span class="line">  @<span class="type">UiThread</span></span><br><span class="line">  <span class="type">void</span> updateUI(<span class="type">MyResult</span> <span class="literal">result</span>) &#123;</span><br><span class="line">    activity.showResult(<span class="literal">result</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>We could probably provide even better solutions, but there are a lot of different use cases, and we need to think about them all. So right now, @Background has a very simple behavior and this is not going to change. We could, however, introduce new annotations with an advanced “thread + lifecycle” behavior.</p>
</blockquote>
]]></content>
    <summary type="html">
    <![CDATA[<h3 id="WorkingWithThreads">WorkingWithThreads</h3><h4 id="@Background">@Background</h4><p>The <code>@Background</code> annotation indicates]]>
    </summary>
    
      <category term="Android" scheme="http://gcblog.cn/tags/Android/"/>
    
      <category term="AndroidAnnotations" scheme="http://gcblog.cn/categories/AndroidAnnotations/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[AndroidAnnotations (六) - Resources]]></title>
    <link href="http://gcblog.cn/2015/11/24/AndroidAnnotations-%E5%85%AD/"/>
    <id>http://gcblog.cn/2015/11/24/AndroidAnnotations-六/</id>
    <published>2015-11-24T14:43:55.000Z</published>
    <updated>2015-11-24T14:51:50.000Z</updated>
    <content type="html"><![CDATA[<blockquote>
<p>对应Android本身各种资源标签，没什么探究的了，实在记不住用得时候查一下就好了。</p>
</blockquote>
<p>All @XXXRes annotations indicate that an activity field should be injected with the corresponding Android resource from your res folder. The resource id can be set in the annotation parameter, ie @StringRes(R.string.hello).</p>
<h3 id="@StringRes">@StringRes</h3><hr>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@EActivity</span></span><br><span class="line">public <span class="class"><span class="keyword">class</span> <span class="title">MyActivity</span> <span class="keyword"><span class="keyword">extends</span></span> <span class="title">Activity</span> &#123;</span></span><br><span class="line"></span><br><span class="line">  <span class="annotation">@StringRes</span>(<span class="type">R</span>.string.hello)</span><br><span class="line">  <span class="type">String</span> myHelloString;</span><br><span class="line"></span><br><span class="line">  <span class="annotation">@StringRes</span></span><br><span class="line">  <span class="type">String</span> hello;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="@ColorRes">@ColorRes</h3><hr>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@EActivity</span></span><br><span class="line">public <span class="class"><span class="keyword">class</span> <span class="title">MyActivity</span> <span class="keyword"><span class="keyword">extends</span></span> <span class="title">Activity</span> &#123;</span></span><br><span class="line"></span><br><span class="line">  <span class="annotation">@ColorRes</span>(<span class="type">R</span>.color.backgroundColor)</span><br><span class="line">  int someColor;</span><br><span class="line"></span><br><span class="line">  <span class="annotation">@ColorRes</span></span><br><span class="line">  int backgroundColor;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="@AnimationRes">@AnimationRes</h3><hr>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@EActivity</span></span><br><span class="line">public <span class="class"><span class="keyword">class</span> <span class="title">MyActivity</span> <span class="keyword"><span class="keyword">extends</span></span> <span class="title">Activity</span> &#123;</span></span><br><span class="line"></span><br><span class="line">  <span class="annotation">@AnimationRes</span>(<span class="type">R</span>.anim.fadein)</span><br><span class="line">  <span class="type">XmlResourceParser</span> xmlResAnim;</span><br><span class="line"></span><br><span class="line">  <span class="annotation">@AnimationRes</span></span><br><span class="line">  <span class="type">Animation</span> fadein;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="@DimensionRes">@DimensionRes</h3><hr>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@EActivity</span></span><br><span class="line">public <span class="class"><span class="keyword">class</span> <span class="title">MyActivity</span> <span class="keyword"><span class="keyword">extends</span></span> <span class="title">Activity</span> &#123;</span></span><br><span class="line"></span><br><span class="line">  <span class="annotation">@DimensionRes</span>(<span class="type">R</span>.dimen.fontsize)</span><br><span class="line">  float fontSizeDimension;</span><br><span class="line"></span><br><span class="line">  <span class="annotation">@DimensionRes</span></span><br><span class="line">  float fontsize;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="@DimensionPixelOffsetRes">@DimensionPixelOffsetRes</h3><hr>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@EActivity</span></span><br><span class="line">public <span class="class"><span class="keyword">class</span> <span class="title">MyActivity</span> <span class="keyword"><span class="keyword">extends</span></span> <span class="title">Activity</span> &#123;</span></span><br><span class="line"></span><br><span class="line">  <span class="annotation">@DimensionPixelOffsetRes</span>(<span class="type">R</span>.string.fontsize)</span><br><span class="line">  int fontSizeDimension;</span><br><span class="line"></span><br><span class="line">  <span class="annotation">@DimensionPixelOffsetRes</span></span><br><span class="line">  int fontsize;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="@DimensionPixelSizeRes">@DimensionPixelSizeRes</h3><hr>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@EActivity</span></span><br><span class="line">public <span class="class"><span class="keyword">class</span> <span class="title">MyActivity</span> <span class="keyword"><span class="keyword">extends</span></span> <span class="title">Activity</span> &#123;</span></span><br><span class="line"></span><br><span class="line">  <span class="annotation">@DimensionPixelSizeRes</span>(<span class="type">R</span>.string.fontsize)</span><br><span class="line">  int fontSizeDimension;</span><br><span class="line"></span><br><span class="line">  <span class="annotation">@DimensionPixelSizeRes</span></span><br><span class="line">  int fontsize;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="Other_@XXXRes">Other @XXXRes</h3><hr>
<p>Here is the list of other supported resource annotations:</p>
<ul>
<li><code>@BooleanRes</code></li>
<li><code>@ColorStateListRes</code></li>
<li><code>@DrawableRes</code></li>
<li><code>@IntArrayRes</code></li>
<li><code>@IntegerRes</code></li>
<li><code>@LayoutRes</code></li>
<li><code>@MovieRes</code></li>
<li><code>@TextRes</code></li>
<li><code>@TextArrayRes</code></li>
<li><code>@StringArrayRes</code></li>
</ul>
]]></content>
    <summary type="html">
    <![CDATA[<blockquote>
<p>对应Android本身各种资源标签，没什么探究的了，实在记不住用得时候查一下就好了。</p>
</blockquote>
<p>All @XXXRes annotations indicate that an activity field shou]]>
    </summary>
    
      <category term="Android" scheme="http://gcblog.cn/tags/Android/"/>
    
      <category term="AndroidAnnotations" scheme="http://gcblog.cn/categories/AndroidAnnotations/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[AndroidAnnotations (五) - CustomClasses/CustomViews]]></title>
    <link href="http://gcblog.cn/2015/11/24/AndroidAnnotations-%E4%BA%94/"/>
    <id>http://gcblog.cn/2015/11/24/AndroidAnnotations-五/</id>
    <published>2015-11-24T08:59:00.000Z</published>
    <updated>2015-11-24T23:39:35.000Z</updated>
    <content type="html"><![CDATA[<h3 id="Custom_Classes">Custom Classes</h3><h4 id="定义注解classes">定义注解classes</h4><figure class="highlight groovy"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@EBean</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyClass</span> &#123;</span></span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>@EBean类只能有一个构造函数，并且这个构造函数必须没有参数,<br>AndroidAnnotations 2.7以后它可以有一个Context参数。</p>
</blockquote>
<h4 id="使用注解classes">使用注解classes</h4><figure class="highlight groovy"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@EBean</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyOtherClass</span> &#123;</span></span><br><span class="line"></span><br><span class="line">	<span class="annotation">@Bean</span></span><br><span class="line">	MyClass myClass;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@EActivity</span></span><br><span class="line">public <span class="class"><span class="keyword">class</span> <span class="title">MyActivity</span> <span class="keyword"><span class="keyword">extends</span></span> <span class="title">Activity</span> &#123;</span></span><br><span class="line"></span><br><span class="line">	<span class="annotation">@Bean</span></span><br><span class="line">	<span class="type">MyOtherClass</span> myOtherClass;</span><br><span class="line">	</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>当你用@Bean时就会得到一个新实例，bean是一个单例除外</p>
</blockquote>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@EActivity</span></span><br><span class="line">public <span class="class"><span class="keyword">class</span> <span class="title">MyChildActivity</span> <span class="keyword"><span class="keyword">extends</span></span> <span class="title">MyActivity</span> &#123;</span></span><br><span class="line">	<span class="annotation">@Override</span></span><br><span class="line">	<span class="keyword">protected</span> void onCreate(<span class="type">Bundle</span> savedInstanceState) &#123;</span><br><span class="line">		<span class="keyword">super</span>.onCreate(savedInstanceState);</span><br><span class="line">		myOtherClass.myClass.toString();</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="使用实现关系classes">使用实现关系classes</h4><blockquote>
<p>Since AndroidAnnotations 2.5</p>
</blockquote>
<p>如果你想在代码中使用超类或接口，您可以再@Bean内指定实现类。</p>
<figure class="highlight actionscript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyInterface</span> </span>&#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line">public <span class="class"><span class="keyword">class</span> <span class="title">MyImplementation</span> <span class="keyword"><span class="keyword">extends</span></span> <span class="title">MyInterface</span> &#123;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@EActivity</span></span><br><span class="line">public <span class="class"><span class="keyword">class</span> <span class="title">OtherActivity</span> <span class="keyword"><span class="keyword">extends</span></span> <span class="title">Activity</span> &#123;</span></span><br><span class="line">    <span class="annotation">@Bean</span>(<span class="type">MyImplementation</span>.<span class="keyword">class</span>)</span><br><span class="line">    <span class="type">MyInterface</span> myInterface;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="支持的annotations">支持的annotations</h4><p>可以使用所有AA 支持的的annotations</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@EBean</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyClass</span> </span>&#123;</span><br><span class="line">	<span class="annotation">@SystemService</span></span><br><span class="line">	NotificationManager notificationManager;</span><br><span class="line"></span><br><span class="line">	<span class="annotation">@UiThread</span></span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">updateUI</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="视图相关annotations">视图相关annotations</h4><p>可以使用@View，@Click…等</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@EBean</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyClass</span> </span>&#123;</span><br><span class="line">	<span class="annotation">@ViewById</span></span><br><span class="line">	TextView myTextView;</span><br><span class="line"></span><br><span class="line">	<span class="annotation">@Click</span>(R.id.myButton)</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">handleButtonClick</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="RootContext">RootContext</h4><p>可以使用@RootContext在@EBean class里注解Android组件，在对应的组件内实例此Bean时，不属于此组件的RootContext将为null。</p>
<figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="variable">@EBean</span></span><br><span class="line">public class MyClass &#123;</span><br><span class="line">	<span class="variable">@RootContext</span></span><br><span class="line">	Context context;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// Only injected if the root context is an activity</span></span><br><span class="line">	<span class="variable">@RootContext</span></span><br><span class="line">	Activity activity;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// Only injected if the root context is a service</span></span><br><span class="line">	<span class="variable">@RootContext</span></span><br><span class="line">	Service service;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// Only injected if the root context is an instance of MyActivity</span></span><br><span class="line">	<span class="variable">@RootContext</span></span><br><span class="line">	MyActivity myActivity;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">@EActivity(R.layout.layout_other)</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> OtherActivity extends Activity &#123;</span><br><span class="line"></span><br><span class="line">    @Bean</span><br><span class="line">    MyClass myClass;</span><br><span class="line"></span><br><span class="line">    @<span class="function">Override</span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">onCreate</span><span class="params">(Bundle savredInstanceState)</span> </span>&#123;</span><br><span class="line">        super.onCreate(savredInstanceState);</span><br><span class="line">        </span><br><span class="line">        Log.e(<span class="string">"cim"</span>,<span class="string">""</span> + (myClass.activity == null));</span><br><span class="line"></span><br><span class="line">        Log.e(<span class="string">"cim"</span>,<span class="string">""</span> + (myClass.service == null));</span><br><span class="line"></span><br><span class="line">        Log.e(<span class="string">"cim"</span>,<span class="string">""</span> + (myClass.context == null));</span><br><span class="line"></span><br><span class="line">        Log.e(<span class="string">"cim"</span>,<span class="string">""</span> + (myClass.otherActivity == null));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">\\Result</span><br><span class="line"><span class="number">11</span>-<span class="number">24</span> <span class="number">08</span>:<span class="number">24</span>:<span class="number">39.303</span> <span class="number">2279</span>-<span class="number">2279</span>/? E/cim: <span class="literal">false</span></span><br><span class="line"><span class="number">11</span>-<span class="number">24</span> <span class="number">08</span>:<span class="number">24</span>:<span class="number">39.303</span> <span class="number">2279</span>-<span class="number">2279</span>/? E/cim: <span class="literal">true</span></span><br><span class="line"><span class="number">11</span>-<span class="number">24</span> <span class="number">08</span>:<span class="number">24</span>:<span class="number">39.303</span> <span class="number">2279</span>-<span class="number">2279</span>/? E/cim: <span class="literal">false</span></span><br><span class="line"><span class="number">11</span>-<span class="number">24</span> <span class="number">08</span>:<span class="number">24</span>:<span class="number">39.303</span> <span class="number">2279</span>-<span class="number">2279</span>/? E/cim: <span class="literal">false</span></span><br></pre></td></tr></table></figure>
<h4 id="Executing_code_after_dependency_injection">Executing code after dependency injection</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">@EBean</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> MyClass &#123;</span><br><span class="line"></span><br><span class="line">    @SystemService</span><br><span class="line">    NotificationManager notificationManager;</span><br><span class="line"></span><br><span class="line">    @Bean</span><br><span class="line">    MyOtherClass dependency;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">MyClass</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="comment">// notificationManager and dependency are null</span></span><br><span class="line">        Log.e(<span class="string">"cim"</span>,<span class="string">""</span> + (dependency == null));</span><br><span class="line">        Log.e(<span class="string">"cim"</span>, <span class="string">""</span> + (notificationManager == null));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @<span class="function">AfterInject</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doSomethingAfterInjection</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="comment">// notificationManager and dependency are set</span></span><br><span class="line">        Log.e(<span class="string">"cim"</span>,<span class="string">""</span> + (dependency == null));</span><br><span class="line">        Log.e(<span class="string">"cim"</span>, <span class="string">""</span> + (notificationManager == null));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">Result:</span><br><span class="line"><span class="number">11</span>-<span class="number">24</span> <span class="number">08</span>:<span class="number">43</span>:<span class="number">04.980</span> <span class="number">3138</span>-<span class="number">3138</span>/? E/cim: <span class="literal">true</span></span><br><span class="line"><span class="number">11</span>-<span class="number">24</span> <span class="number">08</span>:<span class="number">43</span>:<span class="number">04.980</span> <span class="number">3138</span>-<span class="number">3138</span>/? E/cim: <span class="literal">true</span></span><br><span class="line"><span class="number">11</span>-<span class="number">24</span> <span class="number">08</span>:<span class="number">43</span>:<span class="number">04.980</span> <span class="number">3138</span>-<span class="number">3138</span>/? E/cim: <span class="literal">false</span></span><br><span class="line"><span class="number">11</span>-<span class="number">24</span> <span class="number">08</span>:<span class="number">43</span>:<span class="number">04.980</span> <span class="number">3138</span>-<span class="number">3138</span>/? E/cim: <span class="literal">false</span></span><br></pre></td></tr></table></figure>
<p>Bean中当构造函数被调用时，所有注解对象都为null，此时对象没还没有被注入。如果你需要在构建Bean时执行某些代码，应该使用@AfterInject注解一个方法，在此方法内执行。</p>
<h4 id="Warning">Warning</h4><blockquote>
<p>If the parent and child classes have @AfterViews, @AfterInject or @AfterExtras annotated methods with the same name, the generated code will be buggy. See <a href="https://github.com/excilys/androidannotations/issues/591" target="_blank" rel="external">issue #591</a> for more details.</p>
</blockquote>
<hr>
<blockquote>
<p>Also, while there is a guaranteed order about when we call @AfterViews, -Inject or -Extras annotated methods, there is no guaranteed order for calling each of the methods with the same @AfterXXX annotation (see <a href="https://github.com/excilys/androidannotations/issues/810" target="_blank" rel="external">issue #810</a>).</p>
<p>Details about when the methods with one of those annotations are called you can find <a href="https://github.com/excilys/androidannotations/wiki/%40AfterXXX-call-order" target="_blank" rel="external">here</a>.</p>
</blockquote>
<h4 id="作用域">作用域</h4><blockquote>
<p>Since AndroidAnnotations 2.5</p>
</blockquote>
<p>AndroidAnnotations目前支持两种作用域:</p>
<ul>
<li>默认范围:每次创建一个新的bean实例时必须被injected。</li>
<li>单例对象范围:新的bean实例第一次创建被injected，然后保留，之后总是注入相同的实例。</li>
</ul>
<figure class="highlight d"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@EBean</span>(<span class="keyword">scope</span> = Scope.Singleton)</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> MySingleton &#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<blockquote>
<p><strong>Caution</strong></p>
<ol>
<li><p>If your bean has a Singleton scope, it will only keep a reference to the application context (using context.getApplicationContext(). That’s because it lives longer than any activity / service, so it shouldn’t keep a reference to any such Android component, to avoid memory leaks.</p>
</li>
<li><p>We also disable view injection and view event binding in @EBean components with Singleton scope, for the exact same reason (views have a reference to the activity, which may therefore create a leak).</p>
</li>
</ol>
</blockquote>
<h3 id="CustomView">CustomView</h3><blockquote>
<p>翻译了半天，发现还是原文能更准确的传达意思，所以后面都不翻译了。Youdao，Goodbye~</p>
</blockquote>
<p>@EView and @EViewGroup are the annotations to use if you want to create <a href="http://developer.android.com/guide/topics/ui/custom-components.html" target="_blank" rel="external">custom components</a>.</p>
<h4 id="Custom_Views_with_@EView">Custom Views with @EView</h4><blockquote>
<p>Since AndroidAnnotations 2.4</p>
</blockquote>
<p>Just create a new class that extends View, and annotate it with @EView. You can than start using annotations in this view:</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@EView</span></span><br><span class="line">public <span class="class"><span class="keyword">class</span> <span class="title">CustomButton</span> <span class="keyword"><span class="keyword">extends</span></span> <span class="title">Button</span> &#123;</span></span><br><span class="line"></span><br><span class="line">	<span class="annotation">@App</span></span><br><span class="line">	<span class="type">MyApplication</span> application;</span><br><span class="line"></span><br><span class="line">	<span class="annotation">@StringRes</span></span><br><span class="line">	<span class="type">String</span> someStringResource;</span><br><span class="line"></span><br><span class="line">	public <span class="type">CustomButton</span>(<span class="type">Context</span> context, <span class="type">AttributeSet</span> attrs) &#123;</span><br><span class="line">		<span class="keyword">super</span>(context, attrs);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>You can then start using it in your layouts (don’t forget the _):</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="pi">&lt;?xml version="1.0" encoding="utf-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="title">LinearLayout</span> <span class="attribute">xmlns:android</span>=<span class="value">"http://schemas.android.com/apk/res/android"</span></span><br><span class="line">    <span class="attribute">android:layout_width</span>=<span class="value">"match_parent"</span></span><br><span class="line">    <span class="attribute">android:layout_height</span>=<span class="value">"match_parent"</span></span><br><span class="line">    <span class="attribute">android:orientation</span>=<span class="value">"vertical"</span> &gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="title">com.androidannotations.view.CustomButton_</span></span><br><span class="line">        <span class="attribute">android:layout_width</span>=<span class="value">"match_parent"</span></span><br><span class="line">        <span class="attribute">android:layout_height</span>=<span class="value">"wrap_content"</span> /&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!-- ... --&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="title">LinearLayout</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>You can also create it programmatically:</p>
<figure class="highlight armasm"><table><tr><td class="code"><pre><span class="line"><span class="label">CustomButton</span> <span class="keyword">button </span>= CustomButton_.<span class="keyword">build(context);</span></span><br></pre></td></tr></table></figure>
<h4 id="Custom_ViewGroups_with_@EViewGroup">Custom ViewGroups with @EViewGroup</h4><blockquote>
<p>Since AndroidAnnotations 2.2</p>
</blockquote>
<p><strong>How to create it ?</strong></p>
<hr>
<p>First of all, let’s create a layout XML file for this component.</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="pi">&lt;?xml version="1.0" encoding="utf-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="title">merge</span> <span class="attribute">xmlns:android</span>=<span class="value">"http://schemas.android.com/apk/res/android"</span> &gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="title">ImageView</span></span><br><span class="line">        <span class="attribute">android:id</span>=<span class="value">"@+id/image"</span></span><br><span class="line">        <span class="attribute">android:layout_alignParentRight</span>=<span class="value">"true"</span></span><br><span class="line">        <span class="attribute">android:layout_alignBottom</span>=<span class="value">"@+id/title"</span></span><br><span class="line">        <span class="attribute">android:layout_width</span>=<span class="value">"wrap_content"</span></span><br><span class="line">        <span class="attribute">android:layout_height</span>=<span class="value">"wrap_content"</span></span><br><span class="line">        <span class="attribute">android:src</span>=<span class="value">"@drawable/check"</span> /&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="title">TextView</span></span><br><span class="line">        <span class="attribute">android:id</span>=<span class="value">"@+id/title"</span></span><br><span class="line">        <span class="attribute">android:layout_toLeftOf</span>=<span class="value">"@+id/image"</span></span><br><span class="line">        <span class="attribute">android:layout_width</span>=<span class="value">"match_parent"</span></span><br><span class="line">        <span class="attribute">android:layout_height</span>=<span class="value">"wrap_content"</span></span><br><span class="line">        <span class="attribute">android:textColor</span>=<span class="value">"@android:color/white"</span></span><br><span class="line">        <span class="attribute">android:textSize</span>=<span class="value">"12pt"</span> /&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="title">TextView</span></span><br><span class="line">        <span class="attribute">android:id</span>=<span class="value">"@+id/subtitle"</span></span><br><span class="line">        <span class="attribute">android:layout_width</span>=<span class="value">"match_parent"</span></span><br><span class="line">        <span class="attribute">android:layout_height</span>=<span class="value">"wrap_content"</span></span><br><span class="line">        <span class="attribute">android:layout_below</span>=<span class="value">"@+id/title"</span></span><br><span class="line">        <span class="attribute">android:textColor</span>=<span class="value">"#FFdedede"</span></span><br><span class="line">        <span class="attribute">android:textSize</span>=<span class="value">"10pt"</span> /&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="title">merge</span>&gt;</span></span><br></pre></td></tr></table></figure>
<blockquote>
<p>Did you know about <a href="http://developer.android.com/training/improving-layouts/reusing-layouts.html#Merge" target="_blank" rel="external">the merge tag</a> ? When this layout will get inflated, the childrens will be added directly to the parent, you’ll save a level in the view hierarchy.</p>
</blockquote>
<p>As you can see I used some RelativeLayout specific layout attributes (layout_alignParentRight, layout_alignBottom, layout_toLeftOf, etc…), it’s because I’m assuming my layout will be inflated in a RelativeLayout. And that’s indeed what I’ll do !</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@EViewGroup</span>(<span class="type">R</span>.layout.title_with_subtitle)</span><br><span class="line">public <span class="class"><span class="keyword">class</span> <span class="title">TitleWithSubtitle</span> <span class="keyword"><span class="keyword">extends</span></span> <span class="title">RelativeLayout</span> &#123;</span></span><br><span class="line"></span><br><span class="line">    <span class="annotation">@ViewById</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="type">TextView</span> title, subtitle;</span><br><span class="line"></span><br><span class="line">    public <span class="type">TitleWithSubtitle</span>(<span class="type">Context</span> context, <span class="type">AttributeSet</span> attrs) &#123;</span><br><span class="line">        <span class="keyword">super</span>(context, attrs);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void setTexts(<span class="type">String</span> titleText, <span class="type">String</span> subTitleText) &#123;</span><br><span class="line">        title.setText(titleText);</span><br><span class="line">        subtitle.setText(subTitleText);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>There you are ! Easy, isn’t it ?</p>
<p>Now let’s see how to use this brand new component.</p>
<h4 id="How_to_use_it_?">How to use it ?</h4><p>A custom component can be declared and placed in a layout just as any other View:</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="pi">&lt;?xml version="1.0" encoding="utf-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="title">LinearLayout</span> <span class="attribute">xmlns:android</span>=<span class="value">"http://schemas.android.com/apk/res/android"</span></span><br><span class="line">    <span class="attribute">android:layout_width</span>=<span class="value">"match_parent"</span></span><br><span class="line">    <span class="attribute">android:layout_height</span>=<span class="value">"match_parent"</span></span><br><span class="line">    <span class="attribute">android:gravity</span>=<span class="value">"center"</span></span><br><span class="line">    <span class="attribute">android:orientation</span>=<span class="value">"vertical"</span> &gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="title">com.androidannotations.viewgroup.TitleWithSubtitle_</span></span><br><span class="line">        <span class="attribute">android:id</span>=<span class="value">"@+id/firstTitle"</span></span><br><span class="line">        <span class="attribute">android:layout_width</span>=<span class="value">"match_parent"</span></span><br><span class="line">        <span class="attribute">android:layout_height</span>=<span class="value">"wrap_content"</span> /&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="title">com.androidannotations.viewgroup.TitleWithSubtitle_</span></span><br><span class="line">        <span class="attribute">android:id</span>=<span class="value">"@+id/secondTitle"</span></span><br><span class="line">        <span class="attribute">android:layout_width</span>=<span class="value">"match_parent"</span></span><br><span class="line">        <span class="attribute">android:layout_height</span>=<span class="value">"wrap_content"</span> /&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="title">com.androidannotations.viewgroup.TitleWithSubtitle_</span></span><br><span class="line">        <span class="attribute">android:id</span>=<span class="value">"@+id/thirdTitle"</span></span><br><span class="line">        <span class="attribute">android:layout_width</span>=<span class="value">"match_parent"</span></span><br><span class="line">        <span class="attribute">android:layout_height</span>=<span class="value">"wrap_content"</span> /&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="title">LinearLayout</span>&gt;</span></span><br></pre></td></tr></table></figure>
<blockquote>
<p>As always, don’t forget the _ at the end of the component name!</p>
</blockquote>
<p>And because I’m using AA, I can just as easily get my custom components injected in my activity and use them!</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@EActivity</span>(<span class="type">R</span>.layout.main)</span><br><span class="line">public <span class="class"><span class="keyword">class</span> <span class="title">Main</span> <span class="keyword"><span class="keyword">extends</span></span> <span class="title">Activity</span> &#123;</span></span><br><span class="line"></span><br><span class="line">    <span class="annotation">@ViewById</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="type">TitleWithSubtitle</span> firstTitle, secondTitle, thirdTitle;</span><br><span class="line"></span><br><span class="line">    <span class="annotation">@AfterViews</span></span><br><span class="line">    <span class="keyword">protected</span> void init() &#123;</span><br><span class="line"></span><br><span class="line">        firstTitle.setTexts(<span class="string">"decouple your code"</span>,</span><br><span class="line">                <span class="string">"Hide the component logic from the code using it."</span>);</span><br><span class="line"></span><br><span class="line">        secondTitle.setTexts(<span class="string">"write once, reuse anywhere"</span>,</span><br><span class="line">                <span class="string">"Declare you component in multiple "</span> +</span><br><span class="line">                <span class="string">"places, just as easily as you "</span> +</span><br><span class="line">                <span class="string">"would put a single basic View."</span>);</span><br><span class="line"></span><br><span class="line">        thirdTitle.setTexts(<span class="string">"Let's get stated!"</span>,</span><br><span class="line">                <span class="string">"Let's see how AndroidAnnotations can make it easier!"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>Most AA annotations are available in an @EViewGroup, give it a try !</p>
<h3 id="@AfterXXX_call_order">@AfterXXX call order</h3><h4 id="Calls_for_method_with_the_same_@AfterXXX_annotation">Calls for method with the same @AfterXXX annotation</h4><p>The calling order of methods with the same annotation within a class cannot be guaranteed and you should NOT rely on it. If you want multiple methods be called in an ensured order, you should create one method that is annotated and use that to call the other methods in their desired order. If you have to deal with parent/child classes you could try this.</p>
<p><strong>Example</strong></p>
<figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@AfterInject</span></span><br><span class="line"><span class="keyword">protected</span> <span class="function"><span class="keyword">void</span> <span class="title">callInOrder</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    methodA();</span><br><span class="line">    methodB();</span><br><span class="line">    methodC();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="function"><span class="keyword">void</span> <span class="title">methodA</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">// your code</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="function"><span class="keyword">void</span> <span class="title">methodB</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">// your code</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="function"><span class="keyword">void</span> <span class="title">methodC</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">// your code</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="Initial_calls_(after_creation)">Initial calls (after creation)</h4><ol>
<li><a href="https://github.com/excilys/androidannotations/wiki/Extras#executing-code-after-extras-injection" target="_blank" rel="external">@AfterExtras</a></li>
<li><a href="https://github.com/excilys/androidannotations/wiki/Enhance-custom-classes#executing-code-after-dependency-injection" target="_blank" rel="external">@AfterInject</a></li>
<li><a href="https://github.com/excilys/androidannotations/wiki/Injecting-Views" target="_blank" rel="external">@AfterViews</a></li>
</ol>
<p>Note: When @AfterExtras annotated methods are called, all injections that are not related to views are done. Therefore it is safe to use fields that are annotated with e.g. @Bean. The views are only available when the @AfterViews annotated methods get called.</p>
<h4 id="Subsequent_calls">Subsequent calls</h4><ul>
<li><a href="https://github.com/excilys/androidannotations/wiki/Extras#executing-code-after-extras-injection" target="_blank" rel="external">@AfterExtras</a> methods get called every time a new Intent reaches the Activity.</li>
<li><a href="https://github.com/excilys/androidannotations/wiki/Enhance-custom-classes#executing-code-after-dependency-injection" target="_blank" rel="external">@AfterViews</a> will be called everytime setContentView() is called. (Activity only)</li>
</ul>
]]></content>
    <summary type="html">
    <![CDATA[<h3 id="Custom_Classes">Custom Classes</h3><h4 id="定义注解classes">定义注解classes</h4><figure class="highlight groovy"><table><tr><td class="code"]]>
    </summary>
    
      <category term="Android" scheme="http://gcblog.cn/tags/Android/"/>
    
      <category term="AndroidAnnotations" scheme="http://gcblog.cn/categories/AndroidAnnotations/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[AndroidAnnotations (四) - Broadcastreceivers]]></title>
    <link href="http://gcblog.cn/2015/11/24/AndroidAnnotations-%E5%9B%9B/"/>
    <id>http://gcblog.cn/2015/11/24/AndroidAnnotations-四/</id>
    <published>2015-11-24T07:32:02.000Z</published>
    <updated>2015-11-24T08:56:15.000Z</updated>
    <content type="html"><![CDATA[<h3 id="声明Broadcastreceivers注解">声明Broadcastreceivers注解</h3><figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@EReceiver</span></span><br><span class="line">public <span class="class"><span class="keyword">class</span> <span class="title">MyReceiver</span> <span class="keyword"><span class="keyword">extends</span></span> <span class="title">BroadcastReceiver</span> &#123;</span></span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="@ReceiverAction">@ReceiverAction</h3><blockquote>
<p>Since AndroidAnnotations 3.2</p>
</blockquote>
<p>默认Recevier用法里，只能通过onReceive()来分发不同的action。</p>
<p>在Annotations可以通过@ReceiverAction注解来自定义接收方法</p>
<p>根据方法参数分类：</p>
<ul>
<li>Intent</li>
</ul>
<figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="variable">@EReceiver</span></span><br><span class="line">public class MyIntentService extends BroadcastReceiver &#123;</span><br><span class="line"></span><br><span class="line">	<span class="variable">@ReceiverAction</span>(<span class="string">"BROADCAST_ACTION_NAME"</span>)</span><br><span class="line">	void <span class="function">mySimpleAction</span>(Intent intent) &#123;</span><br><span class="line">	    <span class="comment">// ...</span></span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	<span class="comment">//多个行为</span></span><br><span class="line">	<span class="variable">@ReceiverAction</span>(&#123;<span class="string">"MULTI_BROADCAST_ACTION1"</span>, <span class="string">"MULTI_BROADCAST_ACTION2"</span>&#125;)</span><br><span class="line">	void <span class="function">multiAction</span>(Intent intent) &#123;</span><br><span class="line">		<span class="comment">// ...</span></span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	<span class="variable">@Override</span></span><br><span class="line">	public void <span class="function">onReceive</span>(Context context, Intent intent) &#123;</span><br><span class="line">	    <span class="comment">// empty, will be overridden in generated subclass</span></span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>任何android.os.Parcelable 或者 java.io.Serializable 参数.</li>
</ul>
<figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="variable">@EReceiver</span></span><br><span class="line">public class MyIntentService extends BroadcastReceiver &#123;</span><br><span class="line"></span><br><span class="line">	<span class="variable">@ReceiverAction</span></span><br><span class="line">	void <span class="function">myAction</span>(<span class="variable">@ReceiverAction</span>.Extra String valueString, Context context) &#123;</span><br><span class="line">		<span class="comment">// ...</span></span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="variable">@ReceiverAction</span></span><br><span class="line">	void <span class="function">anotherAction</span>(<span class="variable">@ReceiverAction</span>.<span class="function">Extra</span>(<span class="string">"specialExtraName"</span>) String valueString, <span class="variable">@ReceiverAction</span>.Extra long valueLong) &#123;</span><br><span class="line">		<span class="comment">// ...</span></span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="variable">@Override</span></span><br><span class="line">	public void <span class="function">onReceive</span>(Context context, Intent intent) &#123;</span><br><span class="line">		<span class="comment">// empty, will be overridden in generated subclass</span></span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<hr>
<blockquote>
<p>Since AndroidAnnotations 3.3</p>
</blockquote>
<p><strong><em>Note:</em></strong> 由于BroadcastReceiver # onReceive 是抽象方法, 所以必须添加一个空的实现. 为了方便起见,可以继承AbstractBroadcastReceiver类，代码里就可以忽略此方法.</p>
<h3 id="Data_Schemes">Data Schemes</h3><figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@EReceiver</span></span><br><span class="line">public <span class="class"><span class="keyword">class</span> <span class="title">MyIntentService</span> <span class="keyword"><span class="keyword">extends</span></span> <span class="title">BroadcastReceiver</span> &#123;</span></span><br><span class="line"></span><br><span class="line">    <span class="annotation">@ReceiverAction</span>(value = <span class="type">Intent</span>.<span class="type">ACTION_VIEW</span>,dataSchemes = <span class="string">"http"</span>)</span><br><span class="line">    <span class="keyword">protected</span> void onHttp() &#123;</span><br><span class="line">        <span class="comment">// Will be called when an App wants to open a http website but not for https.</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="annotation">@ReceiverAction</span>(value = <span class="type">Intent</span>.<span class="type">ACTION_VIEW</span>, dataSchemes = &#123;<span class="string">"http"</span>, <span class="string">"https"</span>&#125;)</span><br><span class="line">    <span class="keyword">protected</span> void onHttps() &#123;</span><br><span class="line">        <span class="comment">// Will be called when an App wants to open a http or https website.</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="@Receiver">@Receiver</h3><p>注册广播：</p>
<figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="variable">@EActivity</span></span><br><span class="line">public class MyActivity extends Activity &#123;</span><br><span class="line">	</span><br><span class="line">	<span class="comment">//监听的Action</span></span><br><span class="line">	<span class="variable">@Receiver</span>(actions = <span class="string">"org.androidannotations.ACTION_1"</span>)</span><br><span class="line">	protected void <span class="function">onAction1</span>() &#123;</span><br><span class="line"></span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	<span class="comment">//Action: myAction 未指定情况下Action及方法名称</span></span><br><span class="line">	<span class="variable">@Receiver</span></span><br><span class="line">	void <span class="function">myAction</span>(<span class="variable">@Receiver</span>.Extra String valueString, Context context) &#123;</span><br><span class="line">		<span class="comment">// ...</span></span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="variable">@Receiver</span></span><br><span class="line">	void <span class="function">anotherAction</span>(<span class="variable">@Receiver</span>.<span class="function">Extra</span>(<span class="string">"specialExtraName"</span>) String valueString, <span class="variable">@Receiver</span>.Extra long valueLong) &#123;</span><br><span class="line">		<span class="comment">// ...</span></span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	<span class="variable">@Receiver</span>(value = Intent.ACTION_VIEW,dataSchemes = <span class="string">"http"</span>)</span><br><span class="line">	protected void <span class="function">onHttp</span>() &#123;</span><br><span class="line">		<span class="comment">// Will be called when an App wants to open a http website but not for https.</span></span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="variable">@Receiver</span>(value = Intent.ACTION_VIEW, dataSchemes = &#123;<span class="string">"http"</span>, <span class="string">"https"</span>&#125;)</span><br><span class="line">	protected void <span class="function">onHttps</span>() &#123;</span><br><span class="line">		<span class="comment">// Will be called when an App wants to open a http or https website.</span></span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="Registration">Registration</h3><p>在Activity, Fragment, Service的生命周期里指定BroadcastReceiver的registers 和 unregisters</p>
<p>通过registerAt来指定在哪个生命周期方法里注册和销毁，默认值是OnCreateOnDestroy</p>
<table>
<thead>
<tr>
<th></th>
<th>Register</th>
<th>Unregister</th>
<th>Activity</th>
<th>Fragment</th>
<th>Service</th>
</tr>
</thead>
<tbody>
<tr>
<td>OnCreateOnDestroy</td>
<td>onCreate</td>
<td>onDestroy</td>
<td>▲</td>
<td>▲</td>
<td>▲</td>
</tr>
<tr>
<td>OnStartOnStop</td>
<td>onStart</td>
<td>onStop</td>
<td>▲</td>
<td>▲</td>
<td></td>
</tr>
<tr>
<td>OnResumeOnPause</td>
<td>onResume</td>
<td>onPause</td>
<td>▲</td>
<td>▲</td>
<td></td>
</tr>
<tr>
<td>OnAttachOnDetach</td>
<td>onAttach</td>
<td>onDetach</td>
<td></td>
<td>▲</td>
</tr>
</tbody>
</table>
<p>example:</p>
<figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="variable">@EFragment</span></span><br><span class="line">public class MyFragment extends Fragment &#123;</span><br><span class="line"></span><br><span class="line">	<span class="variable">@Receiver</span>(actions = <span class="string">"org.androidannotations.ACTION_1"</span>)</span><br><span class="line">	protected void <span class="function">onAction1RegisteredOnCreateOnDestroy</span>() &#123;</span><br><span class="line"></span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="variable">@Receiver</span>(actions = <span class="string">"org.androidannotations.ACTION_2"</span>, registerAt = Receiver.RegisterAt.OnAttachOnDetach)</span><br><span class="line">	protected void <span class="function">onAction2RegisteredOnAttachOnDetach</span>(Intent intent) &#123;</span><br><span class="line"></span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="variable">@Receiver</span>(actions = <span class="string">"org.androidannotations.ACTION_3"</span>, registerAt = Receiver.RegisterAt.OnStartOnStop)</span><br><span class="line">	protected void <span class="function">action3RegisteredOnStartOnStop</span>() &#123;</span><br><span class="line"></span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="variable">@Receiver</span>(actions = <span class="string">"org.androidannotations.ACTION_4"</span>, registerAt = Receiver.RegisterAt.OnResumeOnPause)</span><br><span class="line">	protected void <span class="function">action4RegisteredOnResumeOnPause</span>(Intent intent) &#123;</span><br><span class="line">	</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="Local_broadcasting">Local broadcasting</h3><p>LocalBroadcastManager是Android Support包提供了一个工具，是用来在同一个应用内的不同组件间发送Broadcast的。</p>
<p>使用LocalBroadcastManager有如下好处：</p>
<ul>
<li>发送的广播只会在自己App内传播，不会泄露给其他App，确保隐私数据不会泄露</li>
<li>其他App也无法向你的App发送该广播，不用担心其他App会来搞破坏</li>
<li>比系统全局广播更加高效</li>
</ul>
<p>和系统广播使用方式类似：</p>
<p>先通过LocalBroadcastManager lbm = LocalBroadcastManager.getInstance(this); 获取实例</p>
<p>然后通过函数 registerReceiver来注册监听器</p>
<figure class="highlight aspectj"><table><tr><td class="code"><pre><span class="line">lbm.registerReceiver(<span class="keyword">new</span> BroadcastReceiver() &#123;  </span><br><span class="line">        <span class="annotation">@Override</span>  </span><br><span class="line">        <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">onReceive</span><span class="params">(Context context, Intent intent)</span> </span>&#123;  </span><br><span class="line">        <span class="comment">// TODO Handle the received local broadcast  </span></span><br><span class="line">        &#125;  </span><br><span class="line">    &#125;, <span class="keyword">new</span> IntentFilter(LOCAL_ACTION));</span><br></pre></td></tr></table></figure>
<p>通过 sendBroadcast 函数来发送广播</p>
<figure class="highlight actionscript"><table><tr><td class="code"><pre><span class="line">lbm.sendBroadcast(<span class="keyword">new</span> Intent(LOCAL_ACTION));</span><br></pre></td></tr></table></figure>
<hr>
<p>AndroidAnnotations里使用LocalBroadcast方法</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@EService</span></span><br><span class="line">public <span class="class"><span class="keyword">class</span> <span class="title">MyService</span> <span class="keyword"><span class="keyword">extends</span></span> <span class="title">Service</span> &#123;</span></span><br><span class="line"></span><br><span class="line">	<span class="comment">//local默认值是false。</span></span><br><span class="line">	<span class="annotation">@Receiver</span>(actions = <span class="string">"org.androidannotations.ACTION_1"</span>, local = <span class="literal">true</span>)</span><br><span class="line">	<span class="keyword">protected</span> void onAction1OnCreate() &#123;  </span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="annotation">@Override</span></span><br><span class="line">	public <span class="type">IBinder</span> onBind(<span class="type">Intent</span> intent) &#123;</span><br><span class="line">	<span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
    <summary type="html">
    <![CDATA[<h3 id="声明Broadcastreceivers注解">声明Broadcastreceivers注解</h3><figure class="highlight scala"><table><tr><td class="code"><pre><span class="lin]]>
    </summary>
    
      <category term="Android" scheme="http://gcblog.cn/tags/Android/"/>
    
      <category term="AndroidAnnotations" scheme="http://gcblog.cn/categories/AndroidAnnotations/"/>
    
  </entry>
  
  <entry>
    <title><![CDATA[AndroidAnnotations (三) - Services]]></title>
    <link href="http://gcblog.cn/2015/11/24/AndroidAnnotations-%E4%B8%89/"/>
    <id>http://gcblog.cn/2015/11/24/AndroidAnnotations-三/</id>
    <published>2015-11-24T06:49:59.000Z</published>
    <updated>2015-11-24T07:25:04.000Z</updated>
    <content type="html"><![CDATA[<h3 id="Service">Service</h3><p><strong>声明EService注解</strong></p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@EService</span></span><br><span class="line">public <span class="class"><span class="keyword">class</span> <span class="title">MyService</span> <span class="keyword"><span class="keyword">extends</span></span> <span class="title">IntentService</span> &#123;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span><br><span class="line">     * Creates an IntentService.  Invoked by your subclass's constructor.</span><br><span class="line">     */</span></span><br><span class="line">    public <span class="type">MyService</span>() &#123;</span><br><span class="line">        <span class="keyword">super</span>(<span class="type">MyService</span>.<span class="keyword">class</span>.getSimpleName());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="annotation">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> void onHandleIntent(<span class="type">Intent</span> intent) &#123;</span><br><span class="line">        showToast();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="annotation">@UiThread</span> <span class="comment">//在UI主线程运行</span></span><br><span class="line">    void showToast() &#123;</span><br><span class="line">        <span class="type">Toast</span>.makeText(getApplicationContext(), <span class="string">"Hello World!"</span>, <span class="type">Toast</span>.<span class="type">LENGTH_LONG</span>).show();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><strong>启动Service：</strong></p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="tag">MyService_</span><span class="class">.intent</span>(<span class="tag">getApplication</span>())<span class="class">.start</span>();</span><br></pre></td></tr></table></figure>
<p><strong>停止Service：</strong></p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="tag">MyService_</span><span class="class">.intent</span>(<span class="tag">getApplication</span>())<span class="class">.stop</span>();</span><br></pre></td></tr></table></figure>
<p><strong>IntentServices：</strong></p>
<ul>
<li>创建IntentSerive</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@EIntentService</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyIntentService</span> <span class="keyword">extends</span> <span class="title">IntentService</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">MyIntentService</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>(<span class="string">"MyIntentService"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="annotation">@ServiceAction</span> </span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">mySimpleAction</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="comment">// ...</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="annotation">@ServiceAction</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">myAction</span><span class="params">(String param)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// ...</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="annotation">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">onHandleIntent</span><span class="params">(Intent intent)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// Do nothing here</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>带自定义方法启动IntentService</li>
</ul>
<figure class="highlight 1c"><table><tr><td class="code"><pre><span class="line">MyIntentService_.intent(getApplication()) <span class="comment">//</span></span><br><span class="line">    .myAction(<span class="string">"test"</span>) <span class="comment">//对应 void myAction(String param)</span></span><br><span class="line">    .start();</span><br></pre></td></tr></table></figure>
<p><em>如果你调用多个Action,只有最后的会被执行。</em></p>
<hr>
<blockquote>
<p>   AndroidAnnotations 3.3版本后新增功能</p>
</blockquote>
<p>Note： 由于IntentService # onHandleIntent是抽象方法, 所以必须添加一个空的实现. 为了方便起见,可以继承AbstractIntentService类，代码里就可以忽略此方法.</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="annotation">@EService</span></span><br><span class="line">public <span class="class"><span class="keyword">class</span> <span class="title">MyService</span> <span class="keyword"><span class="keyword">extends</span></span> <span class="title">AbstractIntentService</span> &#123;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span><br><span class="line">     * Creates an IntentService.  Invoked by your subclass's constructor.</span><br><span class="line">     */</span></span><br><span class="line">    public <span class="type">MyService</span>() &#123;</span><br><span class="line">        <span class="keyword">super</span>(<span class="type">MyService</span>.<span class="keyword">class</span>.getSimpleName());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="annotation">@UiThread</span></span><br><span class="line">    void showToast() &#123;</span><br><span class="line">        <span class="type">Toast</span>.makeText(getApplicationContext(), <span class="string">"Hello World!"</span>, <span class="type">Toast</span>.<span class="type">LENGTH_LONG</span>).show();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    <summary type="html">
    <![CDATA[<h3 id="Service">Service</h3><p><strong>声明EService注解</strong></p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span cla]]>
    </summary>
    
      <category term="Android" scheme="http://gcblog.cn/tags/Android/"/>
    
      <category term="AndroidAnnotations" scheme="http://gcblog.cn/categories/AndroidAnnotations/"/>
    
  </entry>
  
</feed>
